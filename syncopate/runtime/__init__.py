"""M9 · Runtime 上线态 —— 从"能训练"到"能上线"。

★ 这一层和 M0–M8 的关系

M0–M8 造的是**训练用的东西**（数据、沙盒、判据、模型）。这里造的是**真服务**：
接真实请求、调真广告平台 API、把钱花出去。几乎不复用前面的代码 ——
但**契约必须是同一份**（见下面第 3 条）。

★★★ 三条贯穿整个 runtime 的纪律

1. **永不信前端。** `org_id` 一律从鉴权里注入（`Depends`），不从请求体读。
   多租户系统里"前端传 org_id"等于把越权做成了 feature。

2. **幂等是唯一一个"错了就是真金白银"的东西。** 其余组件出问题最多是服务不可用；
   重复扣款是**不可逆损失**。所以三层幂等（§38）的判据必须是**实测重复投递**，
   不能是代码 review。

3. ★★ **沙盒是 runtime 的子集，且契约由 runtime 定义。**
   沙盒（`syncopate/core`）可以简化实现，但**不能有 runtime 没有的行为** ——
   比如沙盒里"重试一定成功"。同一个工具两边行为不一致的话，
   训出来的策略在线上就不成立，那是训推不一致里最贵的一种。
   ⇒ 新增工具行为时，**先在这边定契约，再让沙盒去满足它**。
"""
