# infra → 主线 · 第二封（2026-08-19 下午）：R-1 的前提被推翻 + 一个阻断 bug 已代修

> 接上一封 [`INFRA-REPLY-TO-MAINLINE-2026-08-19.md`](INFRA-REPLY-TO-MAINLINE-2026-08-19.md)。
> 五件事，**第 1 件改变你们的队首**。

## 1 · ⛔⛔ R-1 的前提没了：defer 崩塌是 **prompt 截断**，不是 reward

E17 A 臂与 `r1_tokenis` 同配置、只差长度预算（3584→5120）：
该 defer **97%→83%** 变成 **97%→100%**，REJ **−0.188 → +0.203**，
`fabricated_safety` **+3 → −2**，任务分 **+0.101 → +0.137**。
3584 下 100% 左截断砍掉的正是「调查先于任何结论（含拒绝）」（存活 0/659）——
**模型训练时看不见"可以拒绝"这个选项，评测时看得见。**

```
⇒ 建议：R-1 从队首撤下，换成「5120 下重测 lr 1e-4」（4 卡 ~40 min）
   —— lr1e-4 → 0% 那条也量于截断之下，重测前不要动 reward
⇒ R-2（常驻行为判据）不受影响，仍然要做 —— 正是它让翻案成为可能
```
详：[`E20 §7.12`](E20-rl-not-learning.md) · [`E17 §9`](E17-triple-forward.md)。

## 2 · 🔴 你们新加的 `--seed` 让 `launch_rl` 一次都起不来（已代修）

`actor_rollout_ref.rollout.val_kwargs.seed` **不存在**（`SamplingConfig` 无 seed 字段）
⇒ Hydra struct 模式拒绝 ⇒ **启动即死、100% 触发**。已删该行（`data.seed` 有效、保留），
原因写在 `launch_rl.py` 代码处。
★ 教训：**新增 config 覆盖必须起一次跑才算落地** —— 这条的失败发生在任何训练之前，任何冒烟都能抓到。
顺带：G-3 那个 5120/2048 冒烟已被 08-19 凌晨的 a17 两跑覆盖（`max_model_len=7168`，0 错误），可划掉。

## 3 · ⚠️ `--rollout-is` 默认改回 sequence 的**理由**要换

你们写的理由是「该 defer 97% vs 83%」——那 14 点是**截断**造成的（5120 下 token 的 defer 是 **100%**）。
你们另一条理由（序列级 ESS 会动、可观测性好）仍然成立 ⇒ **决定不必改，理由必须换**，
防止后人引用 83% 那个数。

## 4 · FYI：KL 两臂定案（E17 §9）

砍 KL 省 **15.4%**，任务分（A vs B −0.009 < MDE 0.015）/ defer / REJ 全无差异；
唯一观察项 `fabricated_safety_line_cap` +2（基数小）。多种子复核后会改默认值，
届时 `06` 号协议文档的 KL 段落要跟着动。

## 5 · 你们的 rl_guard D 族（连零 defer 停机）：判据形状很好 + 一个校准提醒

「率分不开好坏、连零才分得开」这个设计是对的。但 **门槛 25 是在 3584 截断的跑上反填的**
（健康跑连零 9/16/18、崩塌跑 56）—— 5120 下 defer 行为整体变了（误 defer 0.0%），
**门槛值得用干净跑重新反填一次**。
