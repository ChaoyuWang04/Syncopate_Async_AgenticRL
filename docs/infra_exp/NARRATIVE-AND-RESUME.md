# 叙事与简历 · 两条 track 的对外表达

> 建于 2026-08-16。**这份文档回答的是「怎么讲」，不是「量到了什么」。**
> 数据在 E 报告里、进度在 TRACK-A/B 里，**这里只管两件事**：遇到了什么问题怎么解决的、简历那一段怎么写。

---

## 0 · 两条硬规矩

### 0.1 只写完成态

> **这里写的是「所有实验做完之后」的样子，不是「现在做到哪」。**
> **还没测出来的数字一律留 `〔 〕`，后面标由哪个实验填。**

**为什么只留一个版本**：进度会变、且**一定会走到完成态**；同时维护「现在能写的」和「做完能写的」两份，
前者会不断被改、后者永远没人看，最后两份都不准。⇒ **进度归 TRACK-A/B，这份只描述终点。**

### 0.2 ★★★ 每个问题必须写成「问题 → 根因 → 解法 → 指标怎么变」四段

**没有第四段的，不算解决，只算测量。**

这条规矩是被一个真实的错误逼出来的：占空比 31% 这个结论一度被当成「产出」讲，
但它其实只是**测量 + 归因**——**三个成因一条都还没消掉**（见 P5）。
⇒ **归因做完会有一种「已经解决了」的错觉。四段式就是防这个的：第四段空着，问题就还在。**

---

# 第一部分 · Track B（框架/异步线）

> 一条主线：**通用 RL 框架的默认假设，在 agentic 负载上逐条失效。我们逐条量、逐条改。**
> 业务背景：手游买量投放的多轮工具调用 agent，轨迹长短天然不齐（1–8 轮），
> 长链路任务又慢又关键；硬件是 4×消费级显卡，无 NVLink、无 P2P、卡间 6.4 GB/s。

## 1 · 遇到的问题，和怎么解决的

### P1 · 同批轨迹长短不齐，同步训练时快的都在干等 ✅ 已解决

| | |
|---|---|
| **怎么发现** | 实测同批**最慢/平均 = 1.37–2.75×**（多轮工具往返导致），而同步模式有 barrier，**必须等最慢那条** |
| **根因** | 通用框架假设「rollout 时长大致齐整，barrier 的代价可接受」——**单轮数学题成立，多轮 agent 不成立** |
| **解法** | 上全异步（3 卡训练 + 1 卡生成），让训练和采样重叠；配 decoupled PPO + 陈旧度上限 + 可打断 rollout 保证可训 |
| **★ 指标** | **每步 117.8 → 74.1 秒（1.59×）**，同机同配置对照 |

★ **这里有个容易被跳过的动作**：手上原本有个 32.6 秒的老基线，看着漂亮，
**但那次用的是另一套算法（bypass vs decoupled），跟现在不可比**，所以专门补了同机分母。
⇒ **加速比必须有同机分母，否则就是在自己骗自己。**

⇒ **三种摆法的完整对照**（除摆法外配置全同）：
```
colocate       1 卡    117.8 s/步    1.00×
one_step_off   3+1     〔B3〕        〔B3〕
fully_async    3+1      74.1 s/步    1.59×
```

### P2 · 权重同步要 55.8 秒，而要传的只有 132 MB 🟡 根因已定位，改动待做

| | |
|---|---|
| **怎么发现** | 把一步的时间拆开，权重同步占 **18.8%**。而 132 MB 在 6.4 GB/s 上理论只要 **0.02 秒**——**差 2500 倍** |
| **根因** | **四步查因**：<br>**两点反解**（首次推 8 GB / 稳态推 132 MB，数据量差 60× 而耗时只差 1.85×）⇒ 传输 0.8 s、固定开销 55.0 s = **98.6% 不是传输**，**这一步零 GPU 成本，纯用已有日志**；<br>**微基准称重** ⇒ 排除 buffer 分配（2.6 ms）与清缓存（12.2 ms）；<br>**分步计时** ⇒ 编排 8 步里 5 步合计 0.038 s（0.06%），**99.94% 在第 5 步**；<br>**A/B 排除** ⇒ 怀疑"取参数"路径，关掉开关只变这一个变量：60.13 vs 61.44 **没动**，而两条路径完全不同却都是 60 秒 ⇒ **成本不在取参数**。最终根因〔**B1**〕 |
| **解法** | 〔**B1**〕 |
| **★ 指标** | 权重同步 **55.8 → 〔X〕秒**，占步耗时 **18.8% → 〔Y〕%** |

★ 这个过程**推翻了自己提出的 4 个看起来很合理的假设**。
⇒ **教训：A/B 的负结果能一次排除一整片可能性，比"再读一遍代码找可疑处"有效得多——
读码只能生成假设，不能排除假设。**

### P3 · 每步把整个模型存一遍，只为取其中 3% ✅ 已解决

| | |
|---|---|
| **怎么发现** | 查 P2 途中顺手看到：算法每步要做一次"存档"（把模型换成旧版本参数算一遍、算完换回来），实测 **4.34 秒/步** |
| **根因** | 数了一下——**存了 902 个张量、8.309 GB，而真正会变的只有 3.18%**。剩下 96.8% 是**冻结的基座**，与上一版**逐字节相同**，存了纯属白存 |
| **解法** | 一行 `if param.requires_grad`；配 3 条测试，其中一条专门钉「全参微调时自动退回全量」 |
| **★ 指标** | 该动作 **4.34 → 0.083 秒/步**（52×），**端到端节省 5.7%**；`old_log_prob/ref` 比值 **1.941 → 1.069** |

★ **这笔钱比整套自写算子的预期收益（4.3%）还大，而改动是一个 `if`。**
★ **前提失效要靠测试守，不是靠注释提醒后人。**

### P4 · 传输缓冲区按 4 GB 定长分配，实际只推 132 MB ⇒ 分卡模式偶发 OOM 🟡 修法已定

| | |
|---|---|
| **怎么发现** | 两跑死在同一个地方：生成卡上 vLLM 占 24.65 GB + 同步组件 4.71 GB，**剩 1.99 GB 要分配 2.00 GB，差 0.01 GB** |
| **根因** | 缓冲区默认 2048 MB **双缓冲 = 4 GB**，且**每个 worker 各一份**，其中一份就住在被 vLLM 占满的生成卡上——**而实际只推 132 MB** |
| **解法** | 让缓冲区匹配真实传输量（2048 → 512 MB）〔**B2**〕 |
| **★ 指标** | 分卡模式的偶发 OOM **消除**；若耗时同步按比例下降，则坐实"广播整个定长 buffer"的猜想〔**B2**〕 |

⛔ **一条要更正的旧结论**：`gpu_util 0.75` 曾被记成"安全值"。
**实测它只是运气好擦过去，第 4 次同步照样 OOM。真正的解法不是压 gpu_util，是让缓冲区匹配真实数据量。**

### P5 · ★★ 整机占空比只有 31%，生成卡 82.5% 在待机 🔴 已归因，**成因一条都还没消掉**

| | |
|---|---|
| **怎么发现** | 轮询四张卡的利用率，154 个采样点、覆盖一个完整周期：**训练卡空闲 54–57%、生成卡空闲 82.5% 且功耗只有 47.7 W**（这张卡满载能到 450 W，它基本在待机）。⇒ **整机平均 31%**<br>★ **这个数和 P1 的 1.59× 互相印证**——一个从空间维度量、一个从时间维度量，指向同一件事 |
| **根因** | **四条，逐条对上实验**（★ 这张表最重要的是最后一列）：<br>① 权重同步 59.8 s / 4 步 ⇒ 占步 18.8% —— 由 **P2/B1** 打<br>② 陈旧度阈值节流，攒够就停下等训练 ⇒ **量级未知** —— 由 **B10** 打（**此前是空白**）<br>③ 训练侧三次前向串行 ⇒ 占步 **72%** —— 由 **P3(已落) + E11 切片** 打<br>④ **配比本身**：生成只占一步耗时的 **6.3%**，却独占一整张卡 ⇒ 3+1 是当初拍的，从没测过 —— 由 **B11** 打（**此前连成因都没列**） |
| **解法** | **不是单点优化，是四条并行**：B1（同步）+ B10（把写死的阈值变成一条可解释的权衡曲线）+ B11（重新配比/放置）+ 已落地的 P3 |
| **★ 指标** | **整机占空比 31% → 〔W〕%**（由上述四项共同填）；**每步 74.1 → 〔Z〕秒** |

⇒ ★★ **这一条是整个 Track B 最大的一块，也是最容易被自己骗过去的一块**：
**归因做完会有一种「已经解决了」的错觉。**
成因列表必须标上「谁在打」——**空着的格子才显形**（②④ 就是这样被找出来的）。

### P6 · 异步可能系统性丢掉长任务（这是正确性问题，不是吞吐问题）🟡 仪器已修正，测量待做

| | |
|---|---|
| **怎么发现** | 异步下短任务先回、长任务可能被切断，**而长链路正是 agent 的核心能力**。按轮数**逐桶**比"发出去的"和"训练到的"（不比均值，均值会把问题藏起来）⇒ **零差：7200 对 7200，14 个桶全同** |
| **★ 根因** | **零差是假的——仪器装错了位置。** 记录点在"轨迹跑完并算完分之后"，所以它记的是「**跑完了**」不是「**发出去了**」⇒ 我们量的是「完成 → 训练」，**而漂移发生在「发出 → 完成」那一段：半路被杀的轨迹根本走不到那行代码，在账上从来就不存在** |
| **解法** | ① 观测点移到 case 真正被交给 agent loop 那一刻（带版本号），与完成端配对；<br>② **主动把系统推到触发条件上**（收紧陈旧度 / 调小同步周期 / 用更长轨迹）——**要测出漂移，得先让它发生**；<br>③ 量出来之后，用**动态分池的采样权重**对系统性欠采样的样本做补偿 |
| **★ 指标** | 长轨迹存活率〔**B4**〕、下发与训练到的长度分布差〔**B4**〕、漂移与轨迹长度的相关性〔**B4**〕；补偿后残余漂移〔**B6**〕 |

⇒ **这个零差是"什么都没发生"的零差，不是"发生了但被修好"的零差。**
⇒ ★ **教训：设计观测点之前先问一句——我要量的那个差，物理上发生在哪两点之间？**
⇒ **顺序钉死：先量出漂移，再用分池补。别反过来。**
⇒ ★ 这一格是 **AReaL 两篇论文连着列为 future work、都没做的那一格**。

### P7 · 训练和推理的分布差，全生态只用 PPL 量，而 PPL 量不出来 ⬜ 待做

| | |
|---|---|
| **怎么发现** | 三种失配（量化、MoE 路由抖动、陈旧度）都会让**采样分布**偏掉，但它们在 PPL 上几乎看不出来 |
| **解法** | 用 **ESS / TIS / 逐 token logprob 差**做统一度量；并把我们的陈旧度阈值折算成 AReaL 的 η，回答**「我们在他们那条消融曲线的哪个位置」** |
| **★ 指标** | ESS/N〔**B7**〕、η 折算值〔**B7**〕；ESS/N < 0.3 作为停止条件 |

### P8 · 「优化完精度掉了没人知道」⬜ 待做

| | |
|---|---|
| **根因** | 只报吞吐不报任务精度，是本项目自己定的纪律里明确禁止的，**但至今一次没过** |
| **解法** | 每项优化都过冻结 EVAL 128×8 **配对**比较（自报 MDE ≈ 0.05）+ 能力构成 + 决策位熵 |
| **★ 指标** | 任务级配对回归**无显著差异**〔**B5**〕 |

---

## 2 · 收束：一句题眼

一路上三次撞到同一个形状：
- 为了取 **3%** 的参数，存了整个模型（P3）；
- 为了用 **4%** 的 token，给全部 token 算了分（Track A P2）；
- 为**不分片**的机器，走了一条为**分片**设计的路径（P2 查因途中）。

> **「机制本身是对的，但它假设的运行条件和我们的不一样。」**
>
> 通用框架的默认值是在 **NVLink + 稠密监督 + 单轮任务**的假设下调出来的。
> **我们的活儿三条都不满足，所以要逐条量、逐条改。**

## 3 · ⚠️ 讲这个故事时的三条措辞纪律

| # | 纪律 | 为什么 |
|---|---|---|
| **①** | **decoupled PPO / 陈旧度上限 / 可打断 rollout 一律写成「在 agentic 负载上复现并验证」** | 它们是 AReaL 已发表的做法（arXiv 2505.24298），在框架里只是三个配置项。**暗示自研，读过论文的人一问就穿。**<br>真正属于我们的是四件：① 长度漂移的定量测量（论文明说是 future work）② 分池 × 异步的补偿组合 ③ 权重同步的根因 ④ 消费级无 P2P 拓扑上的这条曲线 |
| **②** | **「31%」必须和它的 after 一起出现** | 单独拿出来是**测量结果不是成果**。⇒ 见 §0.2 |
| **③** | **结论要带成立范围** | 例：「零漂移」只在轨迹时长 ≪ 同步周期时成立；「慢 6 倍」只有首步支撑。**范围丢了，结论就变成错误陈述** |

## 4 · Track B 的简历

```
模型 + Harness + Infra Codesign          异步 Agentic RL 训练系统优化    2026.08 - 至今

背景: 手游买量投放的多轮工具调用 agent，轨迹长短天然不齐（1–8 轮，同批最慢/平均 1.37–2.75×），
     长链路任务又慢又关键。在 4×消费级显卡（无 NVLink、无 P2P，卡间 6.4 GB/s，为数据中心互联的
     1/140，分片并行已实测净负因而固定为数据并行）上做异步 RL 训练——通用 RL 框架针对高速互联与
     单轮稠密监督设计，其权重同步、状态管理与采样调度在该负载与拓扑上逐条失效。

核心工作：
○ 异步化与同机分母：针对多轮工具调用导致的 rollout 长尾（同批最慢/平均 1.37–2.75×），
  将训练与采样解耦重叠，并建立同机同配置的三模式对照基线（colocate / one-step-off / fully-async）
  ——每步 117.8 → 74.1 秒（1.59×）；相关算法机制（decoupled PPO / 陈旧度控制 / 可打断 rollout）
  在 agentic 负载上完成复现与验证。
○ 权重同步优化：以"两点反解 → 微基准 → 分步计时 → A/B 排除"四步法，证明该开销 98.6% 与传输量
  无关（132MB 权重耗时 55.8 秒，理论 0.02 秒），定位根因为〔B1〕并优化至〔X〕秒，
  占步耗时 18.8% → 〔Y〕%；同时让传输缓冲区匹配真实数据量，消除分卡模式下的偶发 OOM。
○ 冗余状态消除：定位到算法侧每步对全模型做 CPU 快照，而其中仅 3.18% 参数可训，
  改为按可训练性筛选后该动作 4.34 → 0.083 秒/步，端到端节省 5.7%（3 条回归测试守护前提失效）。
○ 占空比治理：以逐卡功耗与利用率采样量出整机占空比仅 31%（生成侧空闲 82.5%、47.7W/450W），
  归因为权重同步、陈旧度节流、训练侧前向串行、以及生成侧资源配比四项，
  逐项消除后整机占空比 31% → 〔W〕%、每步 74.1 → 〔Z〕秒。
○ 异步正确性度量与补偿：发现原观测点位于轨迹完成之后，实际测量区间与漂移发生区间不重合；
  重建下发侧观测并主动将系统推至触发条件，量出长轨迹存活率与下发/训练的长度分布漂移〔B4〕，
  并以采样权重对系统性欠采样的样本做补偿〔B6〕；配套 ESS/TIS 一致性尺子，
  统一度量量化、路由抖动、陈旧度三种失配〔B7〕。

成果:
○ 端到端每步 117.8 → 〔Z〕秒、整机占空比 31% → 〔W〕%；
  任务级配对回归（冻结 EVAL 128×8，自报 MDE 0.05）无显著差异〔B5〕；
○ 补上了原论文列为 future work 的长度漂移定量测量；
  该套归因方法同样适用于线上推理侧的权重热更新路径。
```

### 4.1 占位符 → 由哪个实验填

| 占位 | 由谁填 | 出现在 |
|---|---|---|
| 〔B1〕〔X〕〔Y〕 | **B1** · E12 最后一刀（切开 trainer 侧 / rollout 侧）+ 据此优化 | P2、简历第 2 条 |
| 〔B2〕 | **B2** · 缓冲区 512 vs 2048 A/B | P4、简历第 2 条 |
| 〔B3〕 | **B3** · 补上 one_step_off 那一格 | P1 的三行表 |
| 〔B4〕 | **B4** · 仪器移到真下发点 + 推到触发条件 | P6、简历第 5 条 |
| 〔B6〕 | **B6** · 动态分池接上 + 采样权重补偿 | P6、简历第 5 条 |
| 〔B7〕 | **B7** · ESS/TIS 尺子 + η 换算 | P7、简历第 5 条 |
| 〔B5〕 | **B5** · 任务级尺子（冻结 EVAL 128×8 配对） | P8、成果栏 |
| **〔W〕** | **B1 + B10 + B11 + P3(已落)** 四项共同填 | P5、简历第 4 条 |
| **〔Z〕** | 上述全部做完后重跑一次端到端 | P5、成果栏 |

---

# 第二部分 · Track A（软硬结合线）

> 一条主线：**不是「我会写 kernel」，是「我能证明该写哪个 kernel」。**
> 判据是两把筛子：**① 负载的稀疏结构**、**② 硬件的拓扑约束**，任何一道菜进菜单前必须至少过一把。

## 5 · 遇到的问题，和怎么解决的

### P1 · 长轨迹训不了——而"能走完长链路"正是业务能力本身 ✅ 已解决

| | |
|---|---|
| **怎么发现** | 训练直接 OOM：**T=12288 / 16384 跑不起来，bs=2 就吃掉 25.54 GB** |
| **根因** | 显存的大头是那张"打分表"——模型每读到一个位置都要给词表 **151936** 个词各打一个分，`[16384 × 151936]` bf16 = **4.98 GB**，前向一份反向一份约 10 GB，**是全模型最大的瞬态张量**。<br>★ 再往下问一层：**这张表有多少是有用的？** 去数真实数据——**SFT 每条 5402 token 只有 204 个参与学习（3.8–4.9%），RL 侧 4.17%**（1755 条轨迹：prompt 88.3% / 工具返回 7.6% / 模型自己说的 4.2%）。**两个阶段同一量级 ⇒ 这是这类负载的结构性特征，不是偶然**。⇒ **那张巨表 96% 生下来就注定被丢掉** |
| **解法** | 稀疏投影：先只算到 hidden，按监督 mask **挑出真正需要的那几行**再做投影。**数学上完全等价，不是近似**——被丢弃位置的梯度本来就恒为 0；4 条与朴素实现的对拍测试守着 |
| **★ 指标** | 峰值显存 **16.94 → 10.40 GB（−39%）**；**T=12288/16384 与 bs=4 从 OOM 变为可训**（序列长度上限翻倍以上） |

★ 通用框架**没有这个问题**：它们跑单轮数学/代码，response 占 50%+。**负载画像决定该优化什么。**
★ **附带一条反直觉发现**：**bs=1 反而最快**（4400 token 已喂饱 170 个 SM）
⇒ **省下来的显存该换更长的序列，不是更大的 batch。**

### P2 · RL 侧同样在算白算的东西——但先量了分母，然后主动降级 🟡 落地待做

| | |
|---|---|
| **怎么发现** | RL 侧密度同样只有 **4.17%**，直觉上该照搬 P1 的做法、甚至自己写个融合算子 |
| **★ 根因（这里是关键）** | **先量分母**：微基准显示 **lm_head 在整个前向里只占 4.28%**。⇒ 哪怕把 95.8% 的浪费全省掉，**端到端也只快 4.3%**；而最笨的做法（按 mask 切片再喂现成算子）**就有 4.0%** |
| **解法** | **不自己写算子**，落地那个最笨的切片方案，并与框架内置融合算子 / Liger / 朴素实现同台跑分 |
| **★ 指标** | RL 侧峰值显存与耗时〔**A4**〕；三方跑分结果与归因〔**A4**〕 |

⇒ ★ **教训：「浪费掉的比例」和「能拿回来的收益」，中间隔着一个分母。**
96% 白算 ≠ 能快 96%，因为这件事本身只占全局的 4%。

⇒ 同一个实验还有一次自我更正：原预测密度 10–20%，**实测 4.17%，错了 2.5–5 倍**。
错因是拿配置里的 `max_response_length=1536` 当实际值，而实测 response 中位只有 **422**。
★ **估算里出现配置项的数字，先问一句「实际值是多少」。**

### P3 · 多给三张卡，反而慢 6 倍 ✅ 已解决

| | |
|---|---|
| **怎么发现** | 按主流做法把模型切开分片：**三卡每步 1182 秒，单卡 198 秒** |
| **根因** | 这台机器四张卡之间**没有直连**（P2P 被驱动关掉，GeForce 从 4090 起都这样），数据要绕主机内存，只有 **6.4 GB/s**——**NVLink 的 1/140**。分片意味着每算一层都要把参数从别的卡拼过来 |
| **解法** | 改成数据并行（每卡一整份模型，每步只同步 132 MB 的 LoRA） |
| **★ 指标** | 每步 **1182 → 91.6 秒**，**3.00× 线性扩展** |

⇒ ★ **"把模型切开、每步再拼回来"整一类方案在这台机器上不是慢一点，是不能用。**
⚠️ 口径要带上：1182 vs 198 **两边都是首步**（分片那次跑完一步就崩了，稳态数据不存在）。

### P4 · 想上 30B MoE，但装不下也不敢分片 🟡 前置探查全过，实测待做

| | |
|---|---|
| **怎么发现** | 4B 模型的 agent 能力不够；30B MoE 光权重 bf16 就 **61 GB**，而单卡 32 GB，且 P3 已证明分片不可用 |
| **★ 根因/前置探查** | **半天、纯 CPU、零 GPU 拦下三件事**：<br>① 读 config 的 `architectures` 字段 ⇒ 原定模型**当前栈根本不支持**，换模型（**★ "day-0 支持"必须落到字段验证，不能引用新闻稿**）；<br>② 用真 config 核算 ⇒ 分片时每个 micro-batch 要拼 **61.1 GB 而只用得上 10%**（MoE 每次只激活 128 个专家里的 8 个）⇒ 分片路线彻底堵死；<br>③ 在 meta 设备上数模块 ⇒ **18,673 个线性层里 18,432 个（98.7%）在专家里** |
| **解法** | ① 4bit 基座**每卡一整份**，纯数据并行，每步只同步 132 MB ——<br>★ **量化的目的是省通信，不是省显存本身**；<br>② LoRA 不能挂 `all-linear`，改挂「注意力 + router」 |
| **★ 指标** | **③ 已兑现**：可训练参数 **1696M → 30.1M（26×）**、LoRA 张量 **37,346 → 504 个（74×）**、每步同步 **3.39 GB → 132 MB**——**这笔账在上机之前就避免了**。<br>①② 待实测：每步训练侧通信〔**A2**〕→ 132 MB、训练侧吞吐〔**A2**〕、采样分布 ESS〔**A2**〕、任务级配对回归〔**A2**〕 |

⇒ ★ **教训：继承来的默认值，换了模型结构要重新审一遍**（两分钟、零显存，省下的是几天）。
⚠️ **一条不许复活的理由**：量化的动机只能是省通信。别退回「省显存换 KV 池」——
那条因果链已被实测整条推翻（命中率 96.7–97.5%、KV 池用不到一半、零 preemption）。

### P5 · 这块卡上什么是真的、什么是假的，没人知道 ⬜ 待做

| | |
|---|---|
| **怎么发现** | sm_120 是消费级 Blackwell：**有原生 FP4/FP8，但没有 TMEM**（数据中心 Blackwell 才有）⇒ 编程模型退回 Ampere 式 `mma.sync`，**Triton + inline PTX 全覆盖，没有够不着的指令**。但生态几乎没人点亮它——Triton 的低精度路径在这块卡上会**静默退化成 bf16** |
| **解法** | 能力探底：静默退化复现 / FP8 GEMM roofline / FP4 `mma.sync` inline PTX |
| **★ 指标** | 退化复现的最小用例〔**A3**〕、FP8 实测 TFLOPS 与 roofline 占比〔**A3**〕、FP4 inline PTX 实测〔**A3**〕 |

⇒ ★ **「这块卡上什么是真的」本身就是没人做过的测量**，而且**不依赖任何被推翻的因果链，是最健壮的一道菜**。

### P6 · decode 阶段 GPU 在空转 ⬜ 待做（门槛在拆解）

| | |
|---|---|
| **解法** | nsys/ncu 定位空转根因，按瓶颈类型逐项归因与消除 |
| **★ 指标** | 逐项归因表与消除后的收益〔**E14**〕 |

⚠️ **门槛是先把一步的时间完整拆开**〔**A5** · E01〕——**没有拆解就谈消泡是编。**

---

## 6 · 收束：这条线的纪律

> **不是「我会写 kernel」，是「我能证明该写哪个 kernel」。**

反面教材就在本项目里：早期规划过一条因果链（前缀缓存不够 ⇒ 量化扩容 ⇒ 写 kernel），
**被实测整条推翻**（命中率 96.7–97.5%、KV 池只用不到一半、零 preemption）。
**当时那道菜没过任何一把筛子，靠的是推演。**

⇒ **这条 track 已经两次用实测否决了自己原定的开发计划**（P2 主动降级、上面那条因果链）。

## 7 · ⚠️ 讲这个故事时的两条纪律

| # | 纪律 | 为什么 |
|---|---|---|
| **①** | **「我们为什么没写那个 kernel」要主动说，别等被问** | 面试官看到「稀疏计算」一定会问 RL 侧。**准备好那句话**：「我们量了分母——那一层只占前向 4.28%，端到端上限 4.3%，而最笨的切片就有 4.0%，所以没写。」**主动说是加分，被问出来是减分**。<br>★ **「证明了不该做」和「做成了」在工程上都是产出**，只是前者写不进成果栏 |
| **②** | **P3（DDP vs FSDP）归 Track A，Track B 里只作背景句** | 它同时是 A 的地基和 B 的起点，**但同一份素材写进两个项目会被当成灌水**。B 里写成「分片并行已实测净负因而固定为数据并行」即可 |

## 8 · Track A 的简历

```
模型 + Harness + Infra Codesign      Agentic 负载的稀疏计算与拓扑感知优化    2026.08 - 至今

背景: agent 训练负载有一个结构性特征——绝大多数 token 是提示词与工具返回，不参与学习；
     实测 SFT 侧监督密度 3.8–4.9%、RL 侧 4.17%（1755 条真实轨迹，两阶段同量级）。
     而训练栈对全部 token 物化词表投影，该张量是最大瞬态显存来源，长轨迹因此被显存判死刑；
     叠加机器无 P2P、卡间 6.4 GB/s（数据中心互联的 1/140），"把模型切开再拼回来"整类方案不可用。

核心工作：
○ 负载画像驱动的稀疏计算：按监督 mask 先挑出真正参与学习的行再做词表投影
  （数学等价而非近似，4 条与朴素实现的对拍测试守护）；SFT 侧峰值显存 16.94 → 10.40 GB（−39%），
  T=12288/16384 与 bs=4 从 OOM 变为可训；RL 侧同一路径落地后〔A4〕，
  并与框架内置融合算子 / Liger / 朴素实现同台跑分〔A4 结果与归因〕。
○ 收益上界的量化与主动降级：先量分母再决定动手——微基准显示该投影层仅占前向 4.28%，
  自写算子端到端上限 4.3%，而朴素切片即可拿到 4.0% ⇒ 如实给出"不应自写 kernel"的结论并停止投入。
○ 拓扑驱动的并行与量化：实测分片并行净负（三卡 1182 秒/步 vs 单卡 198 秒），改数据并行后
  每步 1182 → 91.6 秒、3.00× 线性扩展；在此之上以 4bit 基座复制换取"不通信"——
  每步训练侧通信从〔A2〕降到 132MB，训练侧吞吐〔A2〕；
  精度侧不止 PPL，另加任务级配对回归（MDE 0.05）与采样分布 ESS〔A2〕。
○ 上机前的零 GPU 评估：以纯 CPU 探针完成 30B MoE 可行性论证——校验架构支持性、
  用真实 config 核算显存与通信、在 meta 设备上统计模块构成，提前拦下 LoRA 默认配置在 MoE 上
  导致可训练参数 26×（1696M → 30.1M）、每步同步量 26×（3.39GB → 132MB）的陷阱。
○ 消费级 Blackwell（sm_120）能力探底：复现 Triton 低精度路径的静默退化、
  FP8 GEMM roofline〔A3 实测 TFLOPS〕、以 inline PTX 点亮 FP4 mma〔A3〕；
  并以 nsys/ncu 定位执行层空转根因、按瓶颈类型逐项归因与消除〔E14〕。

成果:
○ 长轨迹训练能力解封（序列长度上限翻倍以上、显存 −39%），数值等价由对拍测试保证；
○ 30B MoE 在 4×32GB 无 P2P 机器上可训〔A1/A2〕，训练侧吞吐〔A2〕，任务精度回归无显著差异；
○ 建立"先证明该优化什么、再动手"的判据，并两次据此否决自身原定的算子开发计划。
```

### 8.1 占位符 → 由哪个实验填

| 占位 | 由谁填 | 出现在 |
|---|---|---|
| 〔A4〕 | **A4** · E11-b 切片对照组落地 + 三方跑分 | P2、简历第 1 条 |
| 〔A1〕 | **A1** · E07 P2 探针（30B MoE + LoRA 前向一步，go/no-go） | 成果栏 |
| 〔A2〕 | **A2** · E07 三摆法实测（通信量 / 吞吐 / ESS / 任务级回归） | P4、简历第 3 条 |
| 〔A3〕 | **A3** · E16 sm_120 能力探底 | P5、简历第 5 条 |
| 〔A5〕〔E14〕 | **A5** · E01 拆解 → E14 消泡 | P6、简历第 5 条 |
