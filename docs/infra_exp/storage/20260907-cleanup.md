# 2026-09-07 · Modal 存储清理

状态：完成；这是存储维护，不占用 infra-probes/B00 编号。用户已授权清理确认不再复用的大产物。

## 范围与结果

仅处理 `syncopate-home`（`vo-elvfcHEZBzF6Ylm42h79OI`），未操作 OASIS 或两个 Lab 的资源。删除前再次列出所有容器，结果为空；目标文件路径、大小和修改时间与盘点一致才执行。

| 项目 | 文件逻辑大小 |
|---|---:|
| 清理前 | 269.34 GiB |
| 旧业务 SFT 合并模型 | 删除 64.58 GiB |
| 12 个已结束运行的私有编译缓存 | 删除 1.91 GiB |
| 15 个旧训练权重、optimizer 和中间 adapter 文件 | 删除 2.00 GiB |
| 合计删除 | **68.49 GiB** |
| 清理后 | **200.85 GiB** |

大小来自 Volume 文件清单，不是平台计费占用；清理后数值含备份元数据、不含最后几 KiB 的结果账。没有承诺账单立即变化。当前 [Modal 公开价格](https://modal.com/pricing) 包含每月 1 TiB 免费存储额度、其后 $0.09/GiB/月；账户实际计费仍以账单为准。

## 保留与影响

保留所有公开基模、业务数据、共享编译缓存种子、审计/rollout/逐 token 概率证据；另外保留选中 SFT adapter、最终 OPD adapter 和已有 RL 导出，作为小型历史复核资产。SFT adapter 与公开 base 可用于必要时重建旧合并模型，但新实验不依赖它们。

旧 GRPO 完整训练状态已不再可恢复，旧合并模型路径已不存在；原 REPORT 仍描述当时实测结果，不能因报告存在而假定原大产物仍在。删除没有改写旧实验的源码身份与结果。模型来源、配置和权重索引已另存小型备份；未重新读取大权重计算内容 SHA，不把元数据指纹冒充权重指纹。

## 验证与证据

删除后递归重列 Volume，确认 28 个目标全部不存在；其余原有普通文件的路径/大小/修改时间元数据保持一致。云端 plan 与 result 写入后逐字节回读一致。没有分配 GPU，也没有停止任何其他任务。

- [精确删除清单与核验索引](20260907-cleanup.json)
- 云端：同一 Volume 的 `/vol/_audit/storage/20260907-cleanup/`，保存 `plan.json`、`result.json` 和 `merged_metadata/`。
- 今后按 [Compute](../../syncopate/05-COMPUTE.md) 的收尾规则处理；这不是已部署的自动清理服务。
