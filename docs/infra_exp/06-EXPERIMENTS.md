# Infra · 实验协议

> 本文规定 B 系列实验怎样编号、预注册、落证据和归档。
> 哪个实验先做只看 [01-TASKS.md](01-TASKS.md)。

## 1. 编号

- 新一代实验从 `B01` 开始，按 `B02、B03……` 递增。
- `B` 表示 Modal B200 实验；CPU 前置检查也计入同一实验。

## 2. 文件生命周期

```text
当前队列         docs/infra_exp/01-TASKS.md
施工报告         _audit/infra/Bxx/REPORT.md
原始证据         _audit/infra/Bxx/<arm>/
验收后的完整报告 docs/archive/infra_exp/b-series/Bxx-<slug>.md
现行结论         docs/infra_exp/03、04 或 05
对外材料         docs/narrative/infra-resume.md
```

当前 `docs/infra_exp/` 根目录不放一实验一文档，避免再次变成施工记录堆。
施工报告和机器证据放在同一个实验目录，验收归档说明见
[B 系列归档](../archive/infra_exp/b-series/README.md)。

当前已有施工报告：

- [B01 上云前管线认证](../../_audit/infra/B01/REPORT.md)：通过。
- [B02 2×B200 全链 smoke](../../_audit/infra/B02/REPORT.md)：机械链路通过、质量有 WARN、性能无结论；尚不归档成性能成果。
- [B03 固定源码与训推身份基线](../../_audit/infra/B03/REPORT.md)：35B 身份证据和小模型梯度/恢复已有局部通过；35B 概率、路由和三次重复未完成。
- [B04 SFT DP=2 / TP=2](../../_audit/infra/B04/REPORT.md)：已取回当前安装版 TP/PEFT 源码；tiny 正确性与正式对比未运行。
- [B05 RL 官方模式与单因素开关](../../_audit/infra/B05/REPORT.md)：仅有计划，等待 B03。
- [B06 OPD 正确性与有效更新率](../../_audit/infra/B06/REPORT.md)：CPU 数学前置和证据规格已有结果；最终复审、目标 CPU/GPU 未完成。
- [B07 采用项组合](../../_audit/infra/B07/REPORT.md)：仅有计划，等待前置实验。
- [B13 双卡通信与 BF16 GEMM](../../_audit/infra/B13/REPORT.md)：首轮微测试通过并有初始读数；完整拓扑、利用率、能耗和跨机器重复未完成。

## 3. 跑前预注册

每个实验在启动前必须写清：

| 项目 | 必填内容 |
|---|---|
| 问题 | 这次只回答哪一个问题 |
| 兑现物 | 它解决哪个实测工程问题，服务正确性、速度、Serving SLO、成本或学习观察哪一项 |
| 前置 | 主线阶段、模型、数据、代码和已有探针是否就绪 |
| 环境 | 镜像、依赖、GPU/拓扑、模型、dtype、shape、并行与缓存 |
| 对照 | before、after、唯一变化，以及为什么两臂可比 |
| 判据 | 正确性、性能、质量和停止线的具体读数 |
| 正负对照 | 什么应当通过，故意改错什么必须失败 |
| 证据 | 每项读数写到哪个 JSON、日志、trace 或 checkpoint |
| 费用 | GPU 型号、数量、预计时长；付费运行的用户授权 |
| 收尾 | 服务如何停止，哪些产物保留，失败如何分诊 |

跑完之后才补阈值，等于没有判据。

## 4. A/B 纪律

- 默认 before 是“当前官方稳定、适合生产的优化已开启”的配置；不用全部关闭的慢配置制造夸张加速比。
- “官方支持”不等于“在本模型、本硬件、本版本上正确且更快”。每个开启的官方开关至少做一次 OFF/ON 认证：真实调用路径有判据、输入/输出或数值差符合预期、端到端收益超过噪声、任务质量不过退化线。
- 本仓库自定义、旧栈遗留或尚未复验的机制默认关闭，只能作为单因素 after。全部关闭臂只用于定位问题，不作为简历主 baseline。
- 一次只改变一个关键变量；组合方案先做拆分消融。
- 两臂使用同一镜像、代码、数据、模型、种子、预热、计时窗口和质量尺子。
- 边跑边修后拼成的多段证据可以验最终机制，但不能验性能；任何一段源码或机器身份变化，整轮最多叫施工 smoke，性能 baseline 必须固定源码重跑。
- 正确性通过后，独立 A/B 尽量并行。每臂各用 CPU、单卡或双卡，记录独立源码与拓扑身份；同一工作量至少三个窗口，机器差异通过交叉运行复核。
- 并发臂各自使用 checkout、编译缓存、审计与 checkpoint 目录；冻结数据和模型只读。不能同时写同一个 Volume 路径。
- TASKS 只写任务状态。完整计划、具体资源批次、读数和采用/拒绝决定写在实验 REPORT。
- 性能至少报告中位数和离散度；异常跑不能静默删除。
- microbenchmark 先证明机制，真实负载决定端到端是否晋级。
- 冷启动、JIT/AOT、缓存预热和稳态必须分开报告；不能只挑最快窗口，也不能把编译时间静默算掉。
- 若 before 本身不稳定，先修尺子，不解释 after。
- 固定 token 工作量用于比较计算/调度；真实 on-policy 生成用于比较有效更新与行为。两类结果分开，回放不能冒充 on-policy，回答变短或目标改变不能直接算 infra 加速。
- 至少三个独立重复，窗口内步数不能冒充独立样本。主要优化目标为端到端速度或同工作量成本改善至少 10%，且配对差异的置信区间不跨零；若重复太少或波动大，只报初筛读数。小收益、负结果和正确性修复照样保留。
- 行为质量差在 observe 下继续收集，不再作为 B03 起跑前置。数值/身份/张量错误仍停止；改变目标或工作量的实验必须另列，不能混进纯性能结论。

## 5. 四类结论

报告只能落入以下一种：

- **采用**：正确性、端到端收益和质量边界全部满足预注册门槛。
- **拒绝**：机制成立，但性能、质量或成本明确不过线。
- **不适用**：目标现象在当前模型、硬件或负载不存在。
- **无结论**：尺子、环境、样本或运行失败，不能把它写成正负结果。

退出码 0、程序跑完或图看起来更好，都不是独立结论。

## 6. 报告最小结构

```text
# Bxx · 标题
结论卡：状态 / 一句话 / 适用边界 / 是否改变默认
1. 问题与兑现物
2. 跑前预测和判据
3. 环境指纹
4. 方法与唯一变量
5. 正确性及正负对照
6. 性能、质量和成本结果
7. 解释、替代解释与负面结果
8. 产物索引
9. 对 TASKS、专题、代码和简历的影响
```

## 7. 跨线结论

实验建议改变主线时，不新建交接文件：

1. 把完整结论和证据落盘；
2. 由唯一负责方更新自己的 TASKS 和专题；
3. 用户明确要求跨独立任务协调时，再使用任务消息工具并读取实际回复。

工具投递成功不等于对方确认成功。历史消息不是当前状态来源。
