# TRACKS — 两条线的兑现物与完成度（视图，不是队列）

> **E 编号是身份，track 是叠加的索引视图。** 本文只答「这条线要兑现什么、现在在哪」；
> 量到了什么去 E 报告，下一步做什么去 [`01-TASKS.md`](01-TASKS.md)。
> （2026-08-19 由 TRACK-A + TRACK-B 合并精简；叙事段落已删，历史去 git log。）

---

## A · 负载形状 × 硬件拓扑 决定该写什么算子

**论点**：agentic 负载有极端的结构性稀疏（监督密度 ~4%、MoE 激活 ~10%、题面重复 87%），
消费级拓扑（P2P 全关、PCIe）把通信变成硬约束 ⇒ **该写什么算子由「负载画像 × 拓扑」推出**，
不是由「哪里像能优化」推出。

| 兑现物 | 状态 | 证据 / 还欠 |
|---|---|---|
| ① 负载稀疏驱动的稀疏计算 | 🟢 **SFT 侧已兑现**（16.94→10.40 GB、T=12288 OOM→可训，数学等价 4 对拍）；RL 侧只剩切片（A4，~3.6%） | commit a076e03 · E11 |
| ② 负载稀疏驱动的量化（MoE：分片 all-gather 61 GB vs 4bit 复制 0.13 GB = **470×，与带宽无关**） | 🟡 账算完、go/no-go 过（E07 GO）、**一次没跑**（A19 → A2） | E07 |
| ③ sm_120 能力探底 | 🟡 FP8 第一枪完（真实形状 1.70–2.22×）；Triton 退化复现 / FP4 PTX 未做（A3=E16） | E16 · E19 |
| ④ 执行层消泡 | ⬜ 空档占 22–25%，成因未归属（A18；门槛 E01/nsys 已通） | E01 |

**这条 track 特有的坑**（历史证明过的，四条全保留）：
别为不存在的问题写 kernel（先问过了哪把筛子）；「一行开关」的对照组经常就是答案
（手写 kernel 的价值必须建立在「开关开完了还不够」上）；结论要带成立范围
（dynamic_bsz 的符号由 attention 后端决定）；「简历价值」和「关键路径价值」不许混排。

**已证明不该做**（工程产出，进面试故事不进成果栏）：E11 手写 kernel（分母教训）·
ostinato §4.0 因果链推翻 · E04 TP=2 净负。

## B · agentic RL 训练系统的框架级改造

**论点**：通用 RL 框架的假设（单轮、稠密监督、生成为主的负载）在 agentic 负载上逐条失效
—— 权重同步、状态管理、采样调度、批组织，每一层都要按负载形状重接。

| 兑现物 | 状态 | 证据 / 还欠 |
|---|---|---|
| 正确性：异步 RL 真的在学 | 🟢 **已兑现**：E21（梯度不同步）+ E22（权重没推）修复后任务分 +0.101→+0.137 | E21 · E22 · STORY |
| 吞吐：训练侧共享前缀去重 | 🟢 **已兑现（B12）**：端到端 **2.31×**、三次前向 2.86×、占步 91→74% —— **本 track 第一个 before→after**；欠 B5 | E26 |
| 权重同步 | 🟢 已到地板（252 MiB / 0.97 s / 0.8%）⇒ B1 停放 | E22 §6.4 |
| 陈旧度的代价曲线 | 🔴 旧曲线作废（旋钮当时没接上）；**E26 后剂量条件才真正具备** ⇒ R2 | E08 |
| 漂移测量与分池补偿（AReaL 没做的洞） | 🟡 仪器已写（下发侧三事件）；触发条件从没出现过 ⇒ 与 R2 一起推 | E08-c |
| 任务级尺子 B5 | 🟢 已过一次（+0.101，t=9.3，非 reward hacking）；PG 的 B5 待跑（队首） | E20 §7 |
| 配比与放置 B11 | ⚠️ E26 后 gen 占 26%，问题形状已变，重新设计再跑 | — |
| ESS/η 换算（对上 AReaL 坐标系）B7 | ⬜ 等 R2 的干净数据 | — |

### 占空比四成因（每条都要有「谁在打」，空白才显形）

| 成因 | 现值 | 谁在打 |
|---|---|---|
| ① 权重同步 | 0.8% | ✅ 结案（E22） |
| ② 陈旧度节流 | 没量过（剂量条件刚具备） | R2（01 §2） |
| ③ 三次前向 | 31.5→11.0 s/gstep，占步 73.9% | ✅ E26；剩 ref 方向（E17 多种子后） |
| ④ 配比 | gen 占步 26%（形状已变） | B11 重设计 |

## 完成度（面试官视角，08-16 立的尺子）

```
Track B   诊断完 + 第一个 after（E26 2.31×）+ 正确性两案（E21/E22）  ← 故事完整度 ~75%
Track A   四条兑现物：①落地 ②账完未跑 ③半枪 ④未拆              ← ~40%
对外口径  NARRATIVE-AND-RESUME.md（完成态，未测留〔 〕）；
          「证明了不该做」的三条要主动讲，别等被问
```
