# E30 · A3 正餐 · TileLang 块缩放 GEMM（sm_120）

> 判据（01 §1-3 立）：**对拍等价 + 距 1026(FP8)/2055(FP4) 峰值的百分比**。
> 环境：隔离 venv `/workspace/venvs/tilelang`（tilelang 0.1.13 · torch 2.13+cu130），
> 上游示例与垫层在 `/workspace/a3_tilelang/`（守则⑧：不碰生产 venv）。
> 前情：E16 §7 峰值尺（PTX 实测 1026/2055）· E16 §8 库基线（cuBLAS-cu13 523 · Triton 主线 fp8 304/fp4 519）。

## 1 · 2026-08-27 · 第一天：工具链验证 + 基线树立（跑的是上游 kernel，不是我们的）

**侦察结论**：tilelang 0.1.13 的 sm120 块缩放路径**只接了 NVFP4**
（`mma_gemm_blockscaled`，m16n8k64.kind::mxf4nvf4，E2M1 操作数 + UE4M3 缩放，
配置表 `_SUPPORTED_BLOCK_SCALE_MMA_CONFIGS` 仅一条）；**MXFP8 未接线**
⇒ FP8 GEMM 要么扩展该配置表（= 给 TileLang 的 PR 入口，与 CUTLASS #2867 同缺口），要么先做 FP4。

**上游官方示例实跑**（`examples/gemm_sm120/sm120_nvfp4_blockscaled_gemm.py`，--verify 全过）：

| 形状 | TFLOPS | 距 FP4 峰 2055 |
|---|---|---|
| 2048³ | 689 | 33.5% |
| 4096³ | 1173 | 57.1% |
| **8192³** | **1308** | **63.6%** |
| lm_head 4096×151936×2560 | 600 | 29.2%（瘦长形状，未调） |

官方基准（warp 特化版 + 真 bf16 量化输入）8192³ = 1261，与示例互证。

**当日阶梯**（同卡）：`bf16 258 → cuBLAS FP8 523 ≈ Triton主线 FP4 519 → TileLang NVFP4 1308 → 峰 2055`
⇒ 上游默认配置已比最好的库路径快 **2.5×**；剩余 36% 是手写空间。

## 2 · 配置扫描：默认即最优，墙是共享内存

11 组 block_m/n/k × stages 扫描（`logs/a3_nvfp4_sweep1.log`）：

```
8192³   128/128/256/s2 = 1266 最优;  64/256 掉到 1111;
        所有更大 tile 或 s3 ⇒ "Failed to set dynamic shared memory 110592"
        —— 🔴 sm_120 消费卡共享内存 ~100KB/块（数据中心 227KB），是硬墙
lm_head block_n=256 全断言死：151936=128×1187（1187 奇数）不能被 256 整除
```

⇒ **结论①**：现 schedule 的参数空间已被 smem 预算钉死，1266–1308 就是它的顶。
   再往上 = 在 100KB 内重排流水线（TMA 直达 smem、更细的 stage 切分、
   缩放字压缩布局）——这是"我们的 kernel"要做的事，不是调参能到的。
⇒ **结论②**：lm_head/稀疏投影是"小 M × 巨 N"访存受限形态，与方形 GEMM 是两个问题；
   FP4 权重把 B 流量砍半在该形态**天然对症**，kernel 设计要按访存而非算力做。

## 3 · 下一步（按序）

1. 读懂上游 schedule（smem 布局/缩放字打包/发射循环）→ 自己的变体冲 >70% 峰值；
2. lm_head 形状专用 schedule（N 巨大 ⇒ B 常驻 L2 分条带 + 输出写合并）；
3. MXFP8 配置表扩展（mxf8f6f4 / ue8m0 / m16n8k32）→ 上游 PR 候选；
4. 出成果后回帖 triton#7550 / CUTLASS#2867（E16 §8 圈子地图）。

## 4 · 2026-08-27 · MXFP8 正餐：自有 mxf8f6f4 kernel 首通（v0.2 = 543 TFLOPS，超 cuBLAS）

**这次是我们自己的 kernel**（`scripts/tl_mxfp8_gemm.py`）：TileLang 管分块/TMA/流水，
MMA 内层 = 自写设备函数发射 `mma.m16n8k32.kind::mxf8f6f4.block_scale.scale_vec::1X`
（T.import_source 逃生门，绕开 tilelang C++ 校验只认 NVFP4 的限制）。

**三件此前不存在的东西**：
① **缩放 lane 映射硬件反演**（`scripts/probe_mxf8_scale_mapping.cu`，全域唯一指数法）：
   A 行 r(<8)←lane(4r+tid·2)·byte(bid)，行 r+8←lane(4r+tid·2+1)；B 列 n←lane(4n+tid)。
   bid=字节窗 ⇒ 一个 uint32 装 4 个 k-block 缩放，内层 4 发 mma 各用 bid 0..3；
② **MXFP8 量化器**（OCP：块 32 · ue8m0 幂缩放 · e4m3, fp8_max=448）+ 逐块反量化 fp32 参考；
③ **kernel 原生数据布局**：行内 128B 组 16B 块 `chunk^=(row&7)` 预置换（宿主做），
   TMA 线性搬运，设备 ldmatrix 地址同款 XOR ⇒ bank 冲突消除。

**性能阶梯（8192³，全部 --verify 过，err ~2.9e-3 = bf16 输出舍入带）**：

| 版本 | TFLOPS | 距峰 1026 |
|---|---|---|
| v0.1 标量 smem 加载 | 296 | 28.9% |
| + ldmatrix（x4/x2） | 293 | ——（指令数非瓶颈） |
| **+ XOR swizzle（v0.2）** | **543** | **52.9%** |

⇒ **543 > cuBLAS-cu13 523（老指令路径顶棚）**：本机实测最快 FP8 GEMM，新指令路径首个可用实现。

**踩到的上游坑（两个，各值一个 issue）**：
- TileLang warp 特化把消费者放在 threadIdx 256..511——extern 设备函数必须用 `tx&255` 取
  逻辑线程号（首跑 nan 的根因）；
- 撤掉任一 smem 拷贝会让生产者分区缩到 128 线程而释放 barrier 计数仍 256 ⇒
  相位竞态、散点错块（4096+ 才显形，2048 以下侥幸过——**「小尺寸对拍过」不等于「没竞态」**）。
  已在 kernel 注释钉死"缩放必须走 smem"。

## 5 · 峰值冲刺三连：两负一定界（负结果照登）

| 弹药 | 结果 | 病因（实测） |
|---|---|---|
| 块 128×256（B 复用×2） | 🔴 94 TFLOPS | ptxas 被 launch_bounds(512) 钉在 128 寄存器/线程，acc 128+fragment≈190 ⇒ 溢出 1124B（-Xptxas -v 实锤）；`annotate_*_reg_alloc` 强开重分配在 256/256 分区**挂死**；threads=384 更糟（14.5）——正路被上游卡（DRAFT-tilelang-warpspec-barrier-count 关联） |
| 3 级流水（SFB 转全局挤 smem） | 🔴 527 < 543 | 流水不是瓶颈；全局缩放读还倒贴 |
| ldmatrix（§4 已记） | 🔴 293≈296 | 指令数不是瓶颈 |

⇒ **v0.2 = 543（52.9%）就是本骨架在"512 线程/128 寄存器"约束下的顶**；再往上唯一解锁
= 更宽 warp tile + 寄存器重分配，被 TileLang 上游机制挡住（已入 DRAFT 包，修复后可复活）。

## 6 · 瘦长形状（lm_head/稀疏投影）：轴序一改 +32%

病根（算的账，实测证实）：N=151936 时 B 权重 194MB；默认 N 维最快轴 ⇒ 每个 M 块把 B
从显存整个重拉一遍（32×194MB≈6.2GB）。**M 维改为 blockIdx.x（`--grid-m-fast`）**：
同一 B tile（320KB）被 32 个连续 M 块复用而常驻 L2，B 流量掉回 ~194MB。

| 形状 | 默认轴序 | M 最快轴 | 备注 |
|---|---|---|---|
| 4096×151936×2560 | 370 | **487（+32%）** | +sfb-global 无增益（486） |
| **256×151936×2560**（真实稀疏投影 M） | — | **440** | 访存受限形态的强读数 |

对拍全程 3.1–3.2e-3 ✅。⇒ FP4/FP8 在此形态的本质红利=权重流量减半/减四，
与稀疏投影融合（gather 进 kernel）挂 A4 线，等训练侧接线时一并做。

## 7 · 收官状态（2026-08-27）与复活条件

```
✅ 已交付   MXFP8 GEMM v0.2：8192³ 543 TFLOPS=峰值 52.9%（超 cuBLAS-cu13 523）·
            瘦长形状 487/440 · 对拍常驻 · 负结果三连入档 · 上游两 DRAFT 包
⏸ 停放     >53% 峰值：等 TileLang 寄存器重分配修复（DRAFT 包）或从源码构建自改
⏸ 停放     NVFP4 版自有 kernel（上游示例已 63.6%，我们复刻收益有限）；MXFP8 移植进
            tilelang 正规路径 = 上游 PR（DRAFT-tilelang-sm120-mxfp8-support）
→ 接力     仅"训练梯度路径"（dgrad/wgrad 反向 kernel + autograd 接入）挂 A4；
            ⚠️ 08-27 范围修订（Chaoyu）：**端到端前两层收回本线**——
            ①真权重/真隐状态离线对拍+计速（lm_head 2560×151936 量化 vs 现行 bf16 稀疏投影，
              logprob 误差对 E19"数值地板 316×"尺子读）
            ②接入 old_log_prob 无梯度路径实跑冒烟（步速+kl/ESS+任务分；
              顺带给 side-quant-mismatch 送"训练侧量化"数据点）
```

## 8 · 距峰值 53% 的差距账（08-27 Chaoyu 追问后补记）

```
cuBLAS 523 ≈ 老指令顶棚 516 的 101% —— 库在自己的路上是满分；我们在新路上 53%
差距两半：① 结构性 = 喂料指令与 mma 抢发射口，解药是更大 warp tile（70-85% 都这么堆）
         ② 工具性 = 更大 tile 需 ~190 寄存器，TileLang 静态钉 128 + 重分配挂死（DRAFT 包）
旁证：上游 NVFP4 在重分配正常（224 regs）时 63.6% ⇒ 修好那扇门预期 +10pp 起
NV 刀法自洽（背景）：FP32 累加半速刀=拦训练（图灵时代定）；块缩放新指令=推理格式、
消费卡 AI 营销主角（5090 "3352 AI TOPS"即 FP4），砍它砍自己；数据中心区隔已换轴
（tcgen05/NVLink/显存）。〔新指令仅 f32 累加一种形态、无高低配可分——推断标注〕
```

## 9 · 2026-08-27 · 端到端①：真数据任务口径对拍（8bit 训推第一层证据）

材料：v13r2 真模型 8 条 SFT 验证序列（38,624 真 token · 监督位 1,824=4.7%）· 真 lm_head
权重 [151936,2560]；三路同算 logprob（fp32 真值 / bf16 torch=训练器近似 / MXFP8 kernel，
激活+权重双 8bit）。工具 `scripts/tl_mxfp8_e2e_lmhead.py` + dump 流程见文件头。

| 任务口径读数 | bf16 本底 | MXFP8 | 判读 |
|---|---|---|---|
| SFT loss Δ（监督位 CE） | −4.5e-5 | **+1.5e-4** | 本底的 ~3×，绝对量级微小（loss 0.0093） |
| 监督位 \|Δlogprob\| 中位/均值 | 1.2e-7 / 2.6e-4 | 2.4e-7 / 5.1e-4 | 确信位置撼不动 |
| 监督位 top-1 翻转 | 0% | **0%** | 答案位一个没翻 |
| 序列级 IS 扰动 exp(Δ) | ±10% | **0.88–1.03** | **与 bf16 本底同量级** |
| 全位置 top-1 翻转 | 0.97% | 2.3% | 误差集中在模型不确定的 prompt 位（无任务意义） |
| 速度（4096×151936×2560） | 13.15ms/242TF | **5.90ms/540TF = 2.23×** | 旁证（不含量化，可预做/融合） |

⇒ **8bit 参与训练侧 lm_head 计算，任务层面代价与 bf16 kernel 本底同量级**——
「刀法漏了口子」的实证第一层（新指令 FP32 累加全速 ⇒ 消费卡可跑训练级低精度）。
⚠️ 边界：单层证据（全前向逐层量化会复合）；n=8。

**端到端②（old_log_prob 实跑）拦路石与路线**：kernel 依赖 tilelang JIT，生产 venv
禁装（守则⑧）⇒ 定路线 = **cubin 导出 + 自写宿主加载器（TMA 描述符自配）**，
它同时是「裸 CUDA 消费卡真极限」探索（§8 差距账的工具性那一半）的第一块砖：
256 线程平铺 ⇒ ptxas 上限 255 寄存器/线程（TileLang 512 线程下的 2 倍），
64×64+ warp tile 无需重分配即可装下；tile 形状做模板参数直接扫表。

### 9b · 端到端②（测量形态）：真 RL 轨迹上 8bit 头 × 序列 IS = 裸接必死（决定性负结果）

16 条真 RL 长轨迹（rollout_dumps 最长组，response 1572–2018 token，共 28,668）重算
trainer 侧 logprob，bf16 头 vs MXFP8 头：

```
逐 token   |Δ| 均值 1.6e-2 · p99 0.25 · ★偏置系统性 +2.9e-3（8bit 头 logprob 平均偏高）
序列级     偏置×长度复利：ΣΔ = +3.2 ~ +8.7 ⇒ IS 权重扭曲 11×~5832× ⇒ 16/16 破序列截断阈(2.0)
```

⇒ **判读表**：SFT/逐 token 用途 ✅（§9 上半）；`old_log_prob`+序列 IS（现默认）🔴 裸接必死；
token 级 IS 🟡（逐 token 扰动 ≈1.003，正是 NVIDIA e2e FP8 选 token-TIS 的机理显形）；
训推同侧同量化（Jet-RL 路线）🟡 偏置理论对消、待测。
⇒ 与 E20（序列 IS 长序列崩塌）、E19（FP8 误差=数值地板 316×）三角互证：
**长度放大量化偏置，序列级 IS 引爆它**。8bit 训推的正确形态 = token 级修正 或 两侧同量化。
⇒ 副产品：side-quant-mismatch 项目"长度复利"空角的首个实测数据点（本节可直接引用）。
⇒ 活体接线（cubin 导出路线）在偏置未处理前无意义 ⇒ 停放，随「token 级 IS 或同侧量化」
   的决策一起复活。

## 10 · 2026-08-27 · 裸 CUDA 真极限：627 TFLOPS（61.2%），消费卡包络画定

两型探针（`scripts/mxf8_gemm_limit.cu` / `mxf8_gemm_limit_tma.cu`，全配置 512³ CPU 对拍 ✅，
误差跨配置逐位一致=累加序确定性验证）：

**§10a cp.async 平铺 256 线程**（寄存器 255/线程但计算线程自扛搬运）：
441 TFLOPS < tilelang 543 ⇒ 负结果定界：**寄存器余量单独无用，搬运抢 mma 发射口**；
大 tile 在此设计零红利（C1-C4 全 ≤C0）。

**§10b TMA + warp 特化 + 大寄存器块（合体）**：1 生产 warp（TMA 裸 PTX：cuTensorMapEncodeTiled
+ mbarrier 满/空双向握手自管）+ 4 消费 warp 64×64（160 线程 ⇒ ptxas 255 寄存器/线程）：

| 配置 | TFLOPS | 峰值% | 教训 |
|---|---|---|---|
| T0 同 tilelang 形状 | 560 | 54.6% | 精简流水已胜 tilelang 543 |
| **T1 128×128 w64×64 s2** | **627.4** | **61.2%** | ★ 冠军=寄存器包络贴满 |
| T5 =T1 但 B 缩放全局 | 534 | 52% | 缩放走 L2 付 15% |
| T2/T4 大块（288 线程） | 304–321 | 30% | 寄存器跌回 227 ⇒ 溢出 |
| T7 小块高占用 | 364 | 35% | TMA/barrier 摊薄失败 |
| s3 各式 | ≤533 | — | smem 塞不下或反慢 |

**包络结论**（= 给 tilelang PR 的极限参考数据）：sm_120 消费卡无 tcgen05 张量内存，
累加器只能住寄存器 ⇒ 可行域顶点 = **4 消费 warp/SM × 255 寄存器/线程 × 64×64 warp tile**
（64K 寄存器堆 ÷160 线程），实测 **627/1026 = 61.2%**；再想上只能动指令调度层
（kb 内 fragment 双缓冲等，边际小）或等硬件。TileLang 修好寄存器重分配后的预期天花板
≈ 本数字；当前 543 的缺口（~15%）= warp 特化重分配失效的代价，量化完毕。

阶梯定稿：`Triton主线 304 → cuBLAS-cu13 523 → tilelang(我们) 543 → 裸CUDA(我们) 627 → 纯发射峰 1026`

## 11 · 2026-08-27 · 偏置归因闭环：量化=微升温度，偏置=采样 token 低于分布均值（全实验解剖）

§9b 的 +2.9e-3/token 系统偏置，六组消融（同 28,668 真 RL token）定罪：

```
① 量化器增益     ‖q(x)‖/‖x‖ = 0.99945（收缩 0.055%/侧）——方向固定的种子
② 分解           Δlp = Δz_t(−0.074) − ΔLSE(−0.077)：全体 logit 都在降，归一化项降得更多
③ 机理           等比收缩 logit ≡ 温度微升 ≡ 分布变平 ⇒ 抬高一切低于 softmax 加权均值的 token；
                 RL 采样 token 平均低于均值（gap=+0.449 实测）⇒ 正偏置；
                 SFT 教师强制位 p→1 即均值本身 ⇒ 零偏置（§9 实测吻合 ✓）
④ 剂量-响应      floor 缩放（饱和截断，收缩狂增）⇒ 偏置 ×16（+4.6e-2）✓
⑤ 定量闭环       回归 Δlp≈ε·gap：ε̂=4.8e-3，预测偏置 4.8e-3×0.449=2.2e-3 ≈ 实测 2.9e-3 ✓
⑥ 收缩成分       模长仅 0.11%；大头是舍入误差与信号的相关项 E[εx]<0（密度递减分布下
                 RN 系统性缩点积，~0.4-0.5%）——逐行模长补偿因此只消 20%（实测）
```

**补偿实验**（解药谱系）：

| 方案 | 逐 token 偏置 | 序列 \|ΣΔ\| 中位 | 超阈/16 |
|---|---|---|---|
| 无补偿 | +2.9e-3 | 5.22 | 16 |
| 全局逆温 c=增益积（1.0011） | +2.4e-3 | 4.27 | 16 |
| 逐行模长去收缩 | +2.3e-3 | 3.79 | 16 |
| **全局逆温 c*≈1.006（实测标定）** | **+1.8e-4** | **0.82** | **10** |

⇒ 判读更新：**c* 一个标量（每 ckpt 校准一次）把损伤压 6×**，token 级 IS 下残余 ×1.0002
完全安全；序列级 IS 仍边缘超阈（残余=逐 token 噪声 σ≈1.6e-2×√1800≈0.7/σ 的天然方差）——
**序列 IS 的最终判决不变（不裸接），但从"必死"降级为"差一口气"**；c* 校准 + 阈值口径
讨论移交 side-quant-mismatch（这也是他们"量化项分解"空角的完整方法论样板）。

## 12 · 2026-08-27 · 反向两 GEMM 打通：FP8 训练三件套齐（前向+dgrad+wgrad）

设计要点：**两个反向都复用前向 kernel**——矩阵乘换收缩维（dgrad 收缩 V、wgrad 收缩 M），
只需"操作数转置 + 沿收缩维 MXFP8 量化"。真正的新考题是**梯度量化**（dZ=softmax−onehot，
动态范围极端 + 行内相消），MXFP8 的 32 块幂缩放实测扛住了。
探针 `scripts/tl_mxfp8_backward_probe.py`（28,668 真 RL token、真 CE 梯度）：

| GEMM（收缩维） | bf16 本底 cos/rel | **MXFP8 cos/rel** | 速度 vs torch bf16 |
|---|---|---|---|
| dgrad dH（V=151,936 深收缩） | 0.999998 / 2.2e-3 | **0.99928 / 3.8e-2** | **2.11×**（467 TF） |
| wgrad dW（M=28,672 浅收缩） | 1.000000 / 5.7e-4 | **0.99996 / 8.6e-3** | **1.69×**（383 TF） |

判读：梯度**方向**几乎无损（cos≥0.9993，远高于 minibatch 梯度噪声的典型批间余弦）⇒
8bit 反向对 LoRA+Adam 大概率可训（与 FP8 训练文献一致）；dgrad 误差偏大源于
p−y 行内相消放大相对误差（绝对量仍小）。⇒ **lm_head 层的 FP8 训练三件套
（前向 543/627 · dgrad 2.1× · wgrad 1.7×）全部有实测对拍的可用实现**；
剩余工程 = autograd Function 封装 + cubin 脱离 tilelang 运行时 + 端到端 loss 曲线对照（挂 A4）。

## 13 · 2026-08-27 · A4 收官：MXFP8 lm_head 完整训练打通（首个 8bit 参训的真训练）

工程三件（commit 2e86064）：裸 CUDA T1 kernel → torch 扩展 JIT 进生产 venv（自有源码
零依赖，编译 50s 落重定向缓存）· autograd 三件套封装（冻结权重量化整训缓存、wgrad 按
requires_grad 跳过）· SFT 稀疏投影开关 `SYNCOPATE_LMHEAD_MXFP8`（默认关，bf16 逐位不变）。

**完整 2-epoch SFT A/B（同种子同数据，v13 · Qwen3-4B+LoRA r32）**：

| | bf16 基线 | MXFP8 lm_head | Δ |
|---|---|---|---|
| train/loss 终值 | 0.00036 | **0.00031** | −6e-5（略优） |
| val/loss 五行为 | 0.00072–0.0477 | 0.00059–0.0528 | −2e-4 ~ **+5.1e-3** |
| epoch 秒 / steps/s | 585.5 / 0.1846 | 585.3 / 0.1845 | 持平 |

判读：**收敛曲线与终态同带**（answer/reject 两桶 +5e-3、clarify/defer 反而更低——
种子级噪声带内，方向不一致 ⇒ 无系统性退化信号）；速度持平符合预期
（lm_head 仅占前向 ~4%，量化开销与 kernel 增益相抵——**本次目标是精度可行性不是加速**，
加速要等内层 GEMM 8bit 化）。wandb: a4_bf16(vy5mzrw9) / a4_mxfp8(fnvdzx9q)。

⇒ **A4 定案：8bit（激活+权重）lm_head 的前向+反向参与真实训练，任务口径无退化。**
FP8 训练拼图现状：lm_head 层全通（fwd 627 · dgrad 2.1× · wgrad 1.7× · 真训练 A/B 同带）；
下一步（Chaoyu 方向已定=训推全 8bit）：①vLLM compute_logits 同量化器 monkeypatch
（两侧 lm_head 位一致 ⇒ 量化项在 IS 比率对消，Miles 路线的消费卡最小复刻）
②内层 GEMM 渐进 8bit（融合栈独立线，Miles/DeepSeek-V3 配方为参考）。
