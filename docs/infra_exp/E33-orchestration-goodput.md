# E33 · B-5：业务调度层提升——goodput 64→192（3×），膝点所有权移交引擎

> 状态：✅ **单日收官（2026-08-28 下午，S0–S5 全档）**   最后更新：2026-08-28
>
> 立项：Chaoyu 2026-08-28（E32 收官当天，用户点单）。**跨线作业**：改的是主线 runtime
> 代码（worker/api/db），开工前 MAINLINE 登记，每处改动带回归测试、按路径提交。
> 本文遵守 B-4 同款纪律：**预测跑前写死 · 每步门槛预注册 · 数字不达标不许进下一步**。

## 0 · 结论卡片

| | |
|---|---|
| **需求从哪来** | E32 实测：业务 goodput 膝点 C=96（I01 P95 5.3–5.7s 破 5s 预算）。立项依据里的"单卡=四卡等值⇒纯调度层瓶颈"后被本案 §6③ 部分翻案（after 臂路由塌缩）——需求本身仍成立：调度层确有三大真瓶颈（池 29%/GIL/轮询） |
| **问题** | 调度层（worker/API/PG）哪几站在吃时间；逐站拆掉后膝点能推到多少 |
| **答案** | **goodput@SLO 64 → 192（3×，三遍复核 I01 P95 4842/4344/3906ms·吞吐 2154–2211 runs/min）**。五刀：扩池（PG 300/池 env）×多进程（API×4+worker×4，两刀必须连落——单独扩池实测负收益）+ SSE 门铃（触发器 pg_notify+LISTEN，轮询 0.25s→2s 兜底=构造性 −87.5% 查询）+ router v2 零解析（v1 的 JSON 全解析税 2.3s/单）+ SLO 优先级（decider 按意图预算传 priority）。**C=256 判死于引擎**（llm 8.55s/91.3%，调度层仅 0.81s/8.7%）——终极门槛 ≥256 未达，按预注册出口归因收口：**调度层已从瓶颈位退出，膝点所有权移交引擎容量** |
| **红线** | 全部保住：pytest 722 净值（10 个"失败"=舰队占卡的 CUDA launch 冲突，空卡复验 25/25）· **杀 1/4 worker 零丢单 24/24**（SIGKILL 后 10s 内 lease 交接完毕）· loadtest **22/22 全绿**（比 E32 的 21/22 还多绿一行——延迟形状改善让历史判据病自愈）· 审批/成本语义零改动 |
| **下一步** | 无（收口）。要再抬膝点的两条路都在引擎侧：加卡，或降低每单 LLM 用量（产品侧） |

## 1 · 现在的问题（每条都有代码出处，不是推测）

用大白话说：一单进来要过五道关，每道关都有"人为等待"——

```
① 没有门铃，全靠打转问。
   worker 领单：每 0.2 秒查一次数据库"有新单吗"（worker.py poll_interval=0.2）
   用户看进度：每条 SSE 流每 0.25 秒查一次"有新事件吗"（api.py:398）
   ⇒ 每个环节平白多等 0~250ms；一单五六个环节，固定税约 0.5~1 秒；
     96 个用户在线时，光"看进度"就是每秒近 400 次无效查询砸向数据库。

② 传菜口只有 10 个。
   worker 与 API 的连接池都是默认 max_size=10（db.py:41），而 worker 开 512 个并发槽。
   每一步的每一笔读写（领单/写事件/记账/查幂等）都要先排队借这 10 条连接之一
   ⇒ 延迟随并发线性上涨的头号嫌疑。

③ 两只手干所有活。
   API 是一个 Python 进程、worker 也是一个进程（GIL：同一时刻各只有一只手）。
   96 路 SSE 推送 + JSON 序列化 + 全部编排逻辑挤在两颗核上，四张 GPU 在旁边看戏。

④ 记账挡在路中间。
   每走一步都同步写完事件/审计/用量/幂等才走下一步。这些账是产品卖点不能砍，
   但"记完账才干活"让账本排队时间全进了用户延迟。

⑤ （测量欠账）以上四条的**份额**还没量过——E32 只证明了"总量在调度层"。
```

已知的好底子（不用修）：领单用 `FOR UPDATE SKIP LOCKED`（并发安全的正规写法）；
SSE 断线补发从数据库按 `Last-Event-ID` 重放（**不依赖进程内存 ⇒ 多进程化天然安全**）。

## 2 · 预测（跑之前写死，错了如实记档）

```
P1  S0 分账表将显示：C=96 时"轮询税 + 连接池等待"合计占 e2e 的 50% 以上，
    真正的模型计算占比 < 25%。若不是——说明还有没想到的第五站，先归因再动刀。
P2  S1（门铃）单独把 C=8 的 I01 P95 改善 300ms 以上（固定税兑现）；
    S1+S2（扩池）把膝点 96 → 150~200；S3（多进程）后 ≥ 256。
P3  S4（账本异步）大概率不需要做——预计 S0 会显示账本同步写份额 < 10%。
P4  全程四红线零退化（这不是预测是要求；写在这里是提醒：任何一步让红线专项
    变红，收益再大也回滚）。
```

## 3 · 基线与尺子（沿用 E32 的，不重造）

```
before（已在库，logs/b4/）：goodput@SLO=64 · 膝点 96（I01 5.3–5.7s）·
  C=8 I01 P95 ≈1.5s · loadtest 服务面 21/22 · 单流 TPOT 2.94ms（ngram 后）
尺子：b4_goodput.py（只认合法业务终态，终态构成入产物）· runtime_loadtest 子集 ·
  全量 pytest（**08-28 实测基线 722 passed / 1 xfailed / 0 skipped**，PG 起着=验收口径；
  11 §6 的 365 是 08-20 旧数，以实跑为准）
冻结 serving 口径（全程不变）：4×亲和 fleet + mnbt16384 + ngram（=E32 后的生产高吞吐模式，
  引擎余量确保一切变化归因调度层；S0 在此口径下重立 C=8/64/96 自基线）
四红线专项：test_idempotency（10 条真投两次）· test_sse（断线补发）·
  phase_crash（kill -9 后 3/3 恢复）· 成本触顶降级事件链可见
⚠️ S0 要补的：goodput 阶梯自己的噪声地板（E32 每级只跑过一遍）
```

## 4 · 分步施工（★ 每步末尾的门槛数字不达标，不许进下一步）

### S0 · 装表分账（半天–1 天）——先测量后动手，本线铁律

给一单旅程的每一站装计时，把 e2e 拆成：

```
t_queue   下单 → 被领走          （查 run_events 的 created/started 时间差即得，零改码）
t_llm     模型调用总时长          （decider 侧计时，挂在 usage 记录旁）
t_tool    工具执行总时长          （worker 已有 tool 计时口径）
t_db      借连接的等待 + 读写时长  （db.py 包一层计时器，按调用方分桶）
t_sse     事件落库 → 客户端收到   （b4_goodput 客户端侧：收到时间 − 事件 created_at）
t_rest    余数                   （解释不了的部分，必须显式打出来）
```

产物：C = 8 / 64 / 96 三档各一张分账表 + goodput 阶梯噪声地板（C=64/96 各重跑 ×3）。

**门槛（过了才许开刀）**：
- ① 分账覆盖率 ≥ **90%**（六项加总 ÷ e2e；解释不了 10% 以上 = 表装错了，先修表）
- ② 同档重跑 ×2 各站份额漂移 < **5 个百分点**（表自己得是稳的）
- ③ 三档对比能指出**随并发涨得最快的那一站**（这就是第一刀的靶子；若与 P1 不符，先改预测记档再动刀）

### S1–S5 · 实施结果（施工计划已被结果取代；判定数字见 §5 表，翻案见 §6）

```
S1 扩池      PG max_connections 100→300 · shared_buffers 128MB→2GB · worker/API 池 env 化
             （默认 10 不变；栈传 32-40/12 per 进程）——⚠️ 单独落=负收益（§6②），与 S2 连落
S2 多进程    API uvicorn --workers 4 · worker ×4 进程（FOR UPDATE SKIP LOCKED 天然互斥，
             lease 交接实测 SIGKILL 零丢单）；PER=CONC/4
S3 门铃      schema.sql 触发器 AFTER INSERT ON run_events → pg_notify('run_events', org|run)
             覆盖全部写入路径；API 每进程一条 LISTEN + 断线重连（[sse-bell] 判据行）；
             SSE 循环挂铃→查库→等铃 2s 兜底（拔铃仍活=红线语义不变）
S3.5 路由    router v2：零解析转发（原始字节亲和哈希+自适应窗、非流式走缓冲响应、uvloop）
             ——v1 的 JSON 全解析税 2.3s/单 + 短 prompt 塌缩两案同修
S4 优先级    decider 按 RUN_INTENT 预算映射 priority（I01:-3…I11:0，vLLM 小值先跑已核源码）
             + 引擎 --scheduling-policy priority；账本异步（原 S4）按 §6⑦ 跳过
S5 终验      goodput 三遍 ×C=192 + 杀进程专项 + loadtest 22/22 + pytest 722 净值；
             生产落地：09 §0 栈形态 · start_vllm.sh+priority · 仪器/产物 logs/b5 入库
```

## 5 · 总门槛判定表（收官版：预注册 vs 实测）

| 步 | 预注册门槛 | 实测判定 |
|---|---|---|
| S0 | 覆盖率 ≥90% · 漂移 <5pt | ✅ 覆盖 99-103% ×3 遍 · 暖态漂移 <2.5pt（r1 C=8 冷启动偏差如实记）|
| S1+S2（按数据合并） | 池等待 <5% · C=96 全绿 | ✅ db_wait 29%→1.2-3.8% · C=96 I01 6159→3030ms（⚠️ S1 单独=负收益，§6②）|
| S3 门铃+多进程 | 轮询 −80% · 拔铃活 · 杀进程零丢单 | ✅ SSE 0.25s→2s 兜底=构造性 −87.5% · sse_tail 0.50→0.13-0.25s · listener 断连兜底在代码 · **杀 1/4 worker 24/24 零丢单** |
| S4 SLO 优先级（替换原账本异步——后者按 §6⑦ 跳过） | C=192 达标 | ✅ **C=192 三遍全过**（4842/4344/3906·吞吐 2154-2211）|
| S5 终验 | goodput ≥256 · loadtest ≥21/22 · 红线全绿 | ⚠️ **goodput=192**（256 判死于引擎：llm 91.3%，预注册归因出口）· loadtest **22/22** · pytest 722 净值 · 红线全绿 |

**最终分账对比（C=96 同档，调度层五刀前后）**：
e2e 6.82→2.72s · llm 4.43→2.08s（塌缩排队清除）· db_wait 1.99→0.10s · sse+queue 0.30→0.18s
——调度层份额从 ~35% 压到 <10%，膝点所有权移交引擎。

## 6 · ⛔ 推翻了什么（本案七翻案，每条都被判据自反抓出）

```
① P1 半翻：轮询税实测仅 4-10%（预测 >50% 的主力），池等待 29% 那半对 ⇒ 施工顺序当场对调
② S1 扩池单独做=负收益（C=64 从 pass 变 fail）：10 条连接原是天然限流阀，单进程下拆阀
   = IO 回调全砸一个 GIL，llm/db_tx 反涨 ⇒ 扩池×多进程必须连着落
③ ⛔⛔ E32 goodput after 臂翻案：亲和哈希窗对 decider 短 prompt 越界空切片 ⇒ 1713/1713
   全塌 backend0，"四卡臂实际是单卡臂"——E32"膝点与引擎无关"强版结论不成立；
   真分流后 C=96 I01 6159→3030ms。★分发计数判据当时没被读=判据行没生效
④ "llm 墙钟膨胀在 worker 侧"的中期归因翻案：多进程后仍膨胀 ⇒ 真身=③的单引擎排队
⑤ priority 无队列时是空操作：vLLM priority 只管 waiting→running 准入序，不管 running
   batch 内分食；引擎不排队（num_waiting=0）时优先级无从生效
⑥ mns=32"造队给优先级用"判毒：C=64 全线 db_tx 15s/单的灾难回归（机理未全解，
   疑为深队列下引擎 CPU 抢占编排进程；二分定罪后弃案）——priority 最终价值在暖机+
   默认 mns 下的 C=192 边缘（4842 贴线过 → 复跑 4344/3906 稳过）
⑦ 终极门槛 ≥256 未达（预注册出口）：C=256 时 llm 8.55s/91.3%——不是调度层欠债，
   是引擎容量墙；S4（账本异步）据此跳过：省 0.45s 的 db_tx 救不了 8.55s 的 llm
```

## 7 · 踩的坑（症状→根因→修法）

```
① 裸 asyncpg 读 extract(epoch...) 返回 Decimal 与 float 相减炸——runtime 的 numeric codec
   只在 Database 池里；分析脚本自带 float() 转
② uvicorn worker 无声死（两次，无 traceback 无 OOM）：客户端见 RemoteProtocolError；
   uvicorn 0.52 keep_subprocess_alive 会复活 ⇒ 压测客户端把瞬断记 client_error 终态入账
③ 停-起竞态：uvicorn 父进程收尾要几秒，立刻重启 bind 失败 ⇒ stop 等 :8000 真归还
④ SSE 门铃的作用域坑：run_id 只在 org 内唯一 ⇒ notify 载荷必须 org|run
⑤ 门铃泄漏坑：铃每圈挂新的、finally 只摘最后一个 ⇒ 每圈自挂自摘（挂→查序消竞态窗）
⑥ GPU 测试与常驻舰队互斥：E31 合同测试在四卡被占时 CUDA launch failed 假红 10 个
   ⇒ 全量回归必须空卡跑（或按卡圈隔）
```

## 8 · 边界与不做的事

```
不做   换掉 Postgres / 引入 Temporal·Hatchet 等外部地基（本轮先榨干自建栈；
      若 S5 后仍不达 256，"要不要换地基"作为独立裁定再议——那时分账表就是论据）
不做   改产品语义（审批流程、降级规则、SLO 门槛本身都不动）
不碰   引擎侧（E32 已收官；serving 配置冻结，本轮一切对比引擎口径不变）
归属   runtime 代码属主线：开工登记 MAINLINE，改动全部带回归测试、按路径提交
```
