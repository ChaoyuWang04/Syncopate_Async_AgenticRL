# Syncopate · 21 — ⛔ 作废数字登记表（2026-08-18）

> **这份是"哪些数字不能再引用"的唯一来源。**
> 引用任何 RL 训练相关的实测数字之前，**先在这里查一遍**。
>
> ⚠️ **为什么要单独一份**：一个作废的数字被将来引用，**比没有数字更危险** ——
> 它看起来精确、有出处、有上下文，而它量的是一个坏掉的系统。
> 本项目已经因此付过一次钱（「M7 已验收」被下游引用了两天）。
>
> ★ **纪律**：作废的数字**不删**（删了就没人知道当初错在哪），
> 但**每个作废数字必须就地标上 `⛔(21)`**。
> `scripts/check_pipeline_invariants.py --only quarantine` 会强制这一条。
>
> ⛔⛔ **2026-08-19 改了标记的位置：文档顶部横幅 → 数字行内。**
>
> 原设计是每份含作废数字的文档顶部挂一条横幅。问题是它长成了 **17 份一模一样的横幅**
> —— 而**假警报会训练人忽略这条判据，那比没有判据更糟**（守则③）。
> 更要命的是它**不精确**：横幅在第 3 行、数字在第 160 行，
> 读到数字的人根本不会把两者联系起来。
> ⇒ 改成就地标记：**警告出现在数字旁边**。主线 8 份横幅已摘，47 处数字已就地标。
> ⚠️ infra 的 3 份文档是 `交界`，判据对它们**只报告不判红**，交给他们自己标。
>
> ⚠️⚠️ **不要把这次改动误读成"作废解除了"** —— 截至 2026-08-19，
> 下面 §2 的 **22 条里 21 条仍是「待重测」**。
> 基石 bug 修好了，但**那些数字是在坏系统上量的，不会因为 bug 被修而变有效**。
> 这次改的是**警告的形式**，不是警告的内容。

---

## 0 · 三十秒读懂

```
七个根因把 2026-08-14 至 08-18 之间**所有 RL 训练的实测数字**污染了。
其中两个是基石级的：
  B1  三个 trainer rank 没有同步梯度  ⇒ 每次更新只用 1/3 的数据
  B2  trainer 的权重**从没推给 rollout engine** ⇒ rollout 全程用同一份权重

⇒ 「异步」「陈旧度」「ESS 崩塌」「lr 被夹住」这一整套叙事，**量的是这两个 bug**。

✅ 但**不是所有东西都废了** —— §3 列了仍然有效的，同样重要：
   过度作废会让我们把好不容易量到的静态事实也一起扔掉。
```

---

## 1 · 七个根因

| # | 根因 | 它污染了什么 | 出处 |
|---|---|---|---|
| **B1** | **三个 trainer rank 没有同步梯度**，各训各的 LoRA | 每次更新的**有效数据量 = 1/3**；存下来的 ckpt 只是 rank_0 那一份 | `../infra_exp/E21` |
| **B2** | **trainer 的权重从没推给 rollout engine** | `π_rollout` 全程钉在 π₀ ⇒ **所有「陈旧度 / ESS / IS」的数字量的是一个无界的 bug**，不是设计参数 | infra 2026-08-18 |
| **B3** | **配对比较的两端不是同一个起点模型**：基线跑在「裸基座+SFT adapter」，RL 起点是「合并后的 SFT 模型」 | 所有 SFT↔RL 的配对差值、逐题计数、逐模板涨跌 | `18 §11.2` |
| **B4** | **训练与评测的长度预算不一致**（3584/1536 vs 5120/2048） | 训练侧的截断率、`response_length`、`max_steps_cap` 命中；**评测量到的截断率不能套到训练** | `22 §P0-1` |
| **B5** | **训练与评测的采样参数不一致**（`top_p/top_k`） | 训练侧的格式类违规率；**评测测的不是训练那个策略** | `22 §P0-2` · `../infra_exp/E23` |
| **B6** | **合并模型丢了 36% 的 SFT 增量**（bf16 存不下小增量） | 「合并模型」与「基座+adapter」不等价 ⇒ B3 的成因 | `18 §3.3` |
| **B7** | **导出的 adapter 只是 rank_0 一份** | 所有基于 `models/Qwen3-4B-rl-v13-s110/lora_adapter` 的评测 | `18 §4` |

★ **B2 一次性解释了四个此前解释不了的观测**（详见 `../infra_exp/E22-lora-never-synced.md`；原信已删）：
ESS 是累计位移的函数 · 固定 k 下 `kl` 涨 36 倍 · 陈旧度阈值扫描不动 ESS · `log_ppl_diff` 230×。

---

## 2 · ⛔ 作废清单

> 下面这个代码块是**机器可读**的，检查器直接解析它。
> 格式：`标记串 | 它是什么 | 被哪些根因污染 | 怎么办`

⚠️ **标记串必须够独特** —— 2026-08-18 第一版用了 `18.8%` 和 `13.1%`，当场误伤两处：
`18.8%` 在 infra 文档里指的是**权重同步占步 18.8%**（一个**时间**测量，按 §3.6 **有效**），
`13.1%` 在 `learning-notes/05` 里是工具调用频次表。
**判据太宽会制造假警报，而假警报会训练人忽略这条判据** —— 那比没有判据更糟。
⇒ 加新条目时，先 `grep -rl` 确认它只命中你想要的那处。

```quarantine
0.0487 | M7-b 的权重位移 ‖ΔW_eff‖/‖W_base‖ | B1,B2,B7 | 待重测
0.0093 | M7 的权重位移（且是另一种口径） | B1,B2 | 待重测
0.74–0.88 | M7 的 ESS/N 区间 | B2 | 待重测
0.34–0.91 | M7-b 的 ESS/N 区间 | B2 | 待重测
576 / 7200 | M7 的陈旧轨迹占比 8.0% | B2 | 待重测
partial_ratio = 0 | 「没有轨迹跨越版本边界」 | B2 | ⚠️ 版本根本没变过，这个 0 无意义
+0.020 | M7-b 冻结 EVAL 的配对差值 | B1,B2,B3,B7 | 待重测
+0.011 | M7 冻结 EVAL 的配对差值 | B1,B2,B3 | 待重测
85 涨 | M7-b 逐题「显著变好」计数（及 70 跌 / 188 没动） | B1,B2,B3,B7 | 待重测
赢 146 | M7-b 逐题赢/平/输（且门槛是 1e-9） | B1,B2,B3,B7 | 待重测（并改用新门槛）
91 → 128 | unauthorized_write_cap 的 RL 前后变化 | B1,B2,B3,B7 | 待重测
91→128 | 同上（无空格写法） | B1,B2,B3,B7 | 待重测
0.6361 | 命中越权写 cap 的轨迹 raw_reward 均值 | B1,B2 | 待重测（它是 0.50 那个 ceiling 的唯一锚）
89% → 61% | 「该 defer 时 defer 了」的 RL 前后变化 | B1,B2,B3,B7 | 待重测
0.694 → 0.751 | 写桶 reward 的 RL 前后变化 | B1,B2,B3,B7 | 待重测
18.75% | 同上（另一种写法） | B2,B5 | 待重测
29% 的组内方差 | 一步多调用吃掉的组内方差占比 | B2,B5 | 待重测
11.4% → 14.6% | 训练 rollout 的截断率趋势（整体 13.1%） | B2,B4 | 待重测
0.145 | 被截断轨迹的平均 reward | B2,B4 | 待重测
0.2580 | 训练 rollout 的平均组内 std | B1,B2 | 待重测
10.4% | 零方差组占比 | B1,B2 | 待重测
72–78% | rollouter 空闲率 | B2 | 待重测
```

⚠️ **不在表里但同样脏的**：`_audit/v13_rl_s110.json` **整份**
（它是 B1/B3/B6/B7 四条一起作用的产物）。
⇒ **不要用它当任何比较的一端**，包括"历史参照"。

### 2.1 infra 侧要作废的（他们的文档，这里只登记）

```
E20 的全部数字（ESS / chi2_seq / chi2_token / log_ppl_diff / 位移 / grad_norm）   B1,B2
E17 §5.1 的两臂位移 · §4.2 的 A/B 时间构成（时间可留，精度侧作废）              B1,B2
B10 陈旧度代价曲线 · B19 sync_every 扫描                                        B1,B2 且是在 lr=1e-6 下测的
E08 §4.7 三模式同尺子（时间可留）· §4.8 · §4.9                                   B1,B2（时间类不受影响）
E20 §2「模型学会走更长的链路 ⇒ 越成功越崩」                                      B2（冻结的策略学不会任何东西）
```

---

## 3 · ✅ **仍然有效**的（这一节和 §2 一样重要）

> **过度作废的代价和引用错数字一样大** —— 会把好不容易量到的静态事实一起扔掉。
> 判据很简单：**这个数字的产生过程碰不碰"trainer→rollout 的权重同步"和"多 rank 梯度归约"。**

### 3.1 SFT 线：全部有效

`sft.py` 是**单进程单卡手写循环**，没有 `torch.distributed`（已 grep 全仓库确认）
⇒ **B1/B2 都碰不到它**。

```
✅ val_loss 0.7400→0.1110（v11）· 0.0219/0.0110（v13 e1/e2）
✅ 决策位熵 e1 0.1581 / e2 0.0615 · 近乎确定的位置占比 91.7%
✅ SFT 一遍的位移 0.4985% · 210 次更新 · 19 分钟 · 显存峰值 12.3 GB
✅ 有梯度格子 / 饱和格子 / 采样多样性等 SFT 选型指标
```

### 3.2 数据侧：全部有效

```
✅ v13 三桶 EVAL 343 / SFT 511 / RL 881（以 data/splits/v13 为准，不是 00-START §1 的旧数）
✅ D1–D11 多样性门禁 · L1/L2 泄露门禁 · 逐字节可复现性
✅ gold 动作数（GEO 中位 9 · 全库 9.3% 需要 ≥8）· 每 case 的 max_steps
✅ allowed_write_tools 与 gold 的 414/414 对齐
✅ 工具的 kind / effect / 副作用声明
```

### 3.3 静态代码事实：全部有效

```
✅ 训练 3584/1536 vs 评测 5120/2048（两个文件里的两个常量）
✅ 训练 top_p 1.0/top_k -1 vs 评测 0.95/20
✅ verl 报的 ESS 是 clamp(0, 2.0) 之后算的（rollout_corr_helper.py:751）
✅ 上游原话是「换聚合方式」不是「停机」
✅ cap 的 ceiling 刻度表 · system.txt 的内容
✅ AdamW 下位移 ≈ lr × 更新次数（数学，与数据无关）
```

### 3.4 产物比对：全部有效

```
✅ 「合并模型」与 SFT 逐位相同（4 层抽查，0 个元素不同）
✅ lora_adapter == rank_0（0.041528 vs 0.041530）
✅ bf16 保真残差 SFT 0.36 / RL 0.87
✅ 跨 rank 相对差 1.4136 ≈ √2（修复前）· ddp_fixed 三对 0/504（修复后）
✅ 评测分片合并 343/343 不漏不重 · SFT 桶 case 级泄漏 0
✅ logprob_coverage 全样本 = 1.0000
```

### 3.5 ★ 评测审计：**测量有效，解读无效**

`_audit/v13_sft_e1.json` 等是**真的评测**（vLLM + adapter，不涉及权重同步）
⇒ **作为"这个模型在冻结 EVAL 上的表现"是准的**。

⚠️ 但 `v13_rl_s110` 那份评的是「用 π₀ 生成的数据、按 1/3 batch 训出来的 rank_0 权重」
⇒ **它作为"RL 做了什么"的证据无效**。

★ 这是「数字错了」和「数字对、但它不是你以为的那件事」的区别 —— 后者更难防。

### 3.6 infra 的纯硬件/通信测量：全部有效

```
✅ E18 的 16 字节对齐悬崖 · A14/A16   ✅ E16 FP8 1.90–2.08×
✅ B11 拓扑放置 1.6%                  ✅ B20 dynamic_bsz 无收益
✅ E01/A5 的 kernel 级阶段归属        ✅ E00 的分算子带宽
⇒ 它们量的是硬件与通信，和策略正确性无关。
```

---

## 4 · 重测之后怎么回填

```
① 重测出新数字 → 在本文 §2 的对应行把「待重测」改成新值 + 日期
② 原文档里的旧数字**保留**，在旁边补一行「⛔ 旧值 X（作废，见 21）→ 新值 Y」
   ★ 不要直接替换 —— 「上机之前我们以为会怎样」本身就是价值的一半（docs-map 的约定）
③ 全部回填完之后，才允许把对应文档顶部的 ⛔ 横幅摘掉
```

⚠️ **重测的前提**：B1（已修）· B2（已修）· B3（要跑 G-1 重基线）· B4（已修）·
B6/B7（随 B1 消失）。**B5 还没定**（要 infra 决定要不要对齐采样）。
⇒ **在 B3 和 B5 落定之前重跑，只会产出下一批要作废的数字。**

---

## 5 · 强制判据

```bash
python scripts/check_pipeline_invariants.py --only quarantine
```

判据：**任何含有 §2 那些标记串的文档，顶部必须挂 ⛔ 横幅并指回本文。**

⚠️ 这条判据是「集合包含」型的，非黑即白、不需要阈值 ——
和 `ckpt_guards.assert_ranks_identical` 同一族。
⚠️ 加新的作废数字，**只改 §2 那个 ⚠️ **标记串必须够独特** —— 2026-08-18 第一版用了 `18.8%` 和 `13.1%`，当场误伤两处：
`18.8%` 在 infra 文档里指的是**权重同步占步 18.8%**（一个**时间**测量，按 §3.6 **有效**），
`13.1%` 在 `learning-notes/05` 里是工具调用频次表。
**判据太宽会制造假警报，而假警报会训练人忽略这条判据** —— 那比没有判据更糟。
⇒ 加新条目时，先 `grep -rl` 确认它只命中你想要的那处。

```quarantine``` 块**；检查器直接解析它，
不要另立一份名单（本项目为"同一件事两份实现"付过多次钱）。

---

## 5 · infra 实验报告搬过来的作废原文（2026-08-19）

> 这一节是**原文归档**，不是结论。它们已从 `docs/infra_exp/` 的报告正文里删除，
> 只在这里保留，用于回溯「我们当时是怎么想的」。
> **任何情况下都不要引用这里的数字。**

### 5.1 来自 `E20-rl-not-learning.md`

#### §3 关键分解：崩塌来自陈旧度，不是来自数值失配

**为什么作废**：整节的归因作废：干净基线上 log_ppl_diff 根本不增长（0.000391→0.000250）

**被谁推翻**：E20 §7.1

<details><summary>展开原文</summary>

```markdown
## 3 · 关键分解：崩塌来自陈旧度，不是来自数值失配

`log_ppl_diff`（rollout 与 trainer 的逐 token 对数似然差）的**第一个数据点是 0.0001**。
那一步 rollout 与 trainer **是同一个 param version** ⇒

```
0.0001  = vLLM 与 FSDP 的**纯数值失配地板**（不同 kernel、不同精度路径）
0.0231  = 训练到末尾时的实际差距
⇒ 230×，其中 229/230 来自「rollout 用的是旧版本权重」
```

⇒ **数值失配（TIS 那条线关心的东西）在这里不是主因**，主因是**陈旧度**。
⇒ 也就是说：**同步得越少，这个问题越严重** ——
而 B19 刚刚测出"`sync_every` 4→8 白捡 10%"。**两者直接冲突**，见 §5。

---

```

</details>

#### §3.7 E20-a 实测：token 级 IS 把估计量修好了

**为什么作废**：数字在坏基线上量的（ESS 0.449→1.000）；干净基线上是 0.909→0.852 vs 0.999，幅度整个下调

**被谁推翻**：E20 §7.6.1

<details><summary>展开原文</summary>

```markdown
## 3.7 ✅ E20-a 实测（2026-08-18 09:28）：**token 级 IS 把估计量修好了**

同尺子对照（`lr=3e-5`、60 步、`sync_every=4`，**只改 `--rollout-is`**）：

| 判据 | 序列级（现状） | **token 级** | 判定 |
|---|---|---|---|
| **ESS/N** 首 → 末 | 0.892 → **0.449** | **1.000 → 1.000** | ✅ **完全不塌** |
| IS 权重均值 | 0.954 → 0.575 | 1.000 → **0.997** | ✅ 贴着 1 |
| **`grad_norm`** | 0.00944 → **0.00225**（缩小 4.2×） | 0.00158 → **0.00369**（**增大 2.3×**） | ✅ **从"越训越不敢学"变成"信号在长"** |
| `log_ppl_diff` | 0.00039 → 0.01507 | −0.00000 → **0.02051** | 策略反而走得**更远**（梯度没被压） |
| **权重位移** | 0.1071% | 0.1101% | ⚠️ **几乎没变（+2.8%）** |

**⇒ 两个结论，必须一起读：**

1. ✅ **原因①（估计量崩塌）确认并解决**：预测「token 级好 3 个数量级」被证实
   —— ESS 从 0.449 变成 1.000，`grad_norm` 的趋势整个反转。
   **而且不牺牲任何吞吐**（同样 60 步、同样时间）。
2. ⚠️ **但位移几乎没动** ⇒ **原因②（更新次数太少）是独立的、还没被碰过的**。
   13–15 次更新，估计量再准也堆不出位移。

⇒ **下一批（E20-e/f/g）就是补更新量**：`ppo_mini_batch_size 6→2`（每 fit step 从 1 次更新变 3 次）
和 `lr 3e-5→1e-4`，**以 token 级 IS 为共同底座**。
★ 现在敢动这两个旋钮，正是因为①已经修好 —— **顺序对了**。

```

</details>

#### §3.8 异步到底买到了什么：它的全部价值就在「少同步」上

**为什么作废**：① 54.48s vs 55.28s 那个对照是「坏的 vs 好的」（fully_async 当时策略恒为 π0）；② 更根本：省下的钱不在少同步，在「少打断 rollout」—— gen 占降幅 96%，param_sync 只占 4%

**被谁推翻**：E08 §5.5

<details><summary>展开原文</summary>

```markdown
## 3.8 ★★ 异步到底买到了什么：**它的全部价值就在"少同步"上**

`e20_seqis_sync1`（fully_async 但每步都同步）跑出来的数，把这件事讲得很清楚：

| 配置 | 每 global step | 相对 colocate | 陈旧度 |
|---|---|---|---|
| **colocate 3 卡**（强制同步） | 55.28 s | 1.00× | **0** —— 生成用的就是当前权重 |
| **fully_async `sync_every=1`** | **54.48 s** | **1.01×** | ~1 个版本 |
| fully_async `sync_every=2` | 37.56 s | 1.47× | ↑ |
| fully_async `sync_every=4`（现用） | 32.43 s | **1.70×** | ↑↑ |
| fully_async `sync_every=8` | 29.37 s | 1.88× | ↑↑↑ |
| fully_async `sync_every=16` | 27.88 s | **1.98×** | ↑↑↑↑ |

⇒ ★★ **`sync_every=1` 与 colocate 只差 1.4% —— 异步等于白做。**
**异步唯一买到的东西就是吞吐，而它是靠"少同步"买的；少同步 = 陈旧度。**
（异步**没有**任何算法上的好处；它不会让模型学得更好，只会让每步更便宜。）

### 3.8.1 ⇒ 于是问题变成：**这个权衡还在吗？**

**在 token 级 IS 之前**：是一个真实的权衡 —— 同步越少吞吐越高，但 ESS 越塌。
**在 token 级 IS 之后**：`sync_every=4` 下 ESS 稳在 **1.000**
⇒ **有可能这个权衡已经被消掉了**（而不是需要去找平衡点）。

⇒ 已排 **E20-h（batch10）**：`token IS × sync_every ∈ {8, 16}`，判据写死：
```
ESS/N ≥ 0.95   ⇒ **权衡消失** —— 同时拿到 ~2 倍吞吐和健康训练
0.80–0.95      ⇒ 还剩一点，要在吞吐与陈旧度之间挑一个点
< 0.80         ⇒ 权衡仍在，token IS 只是把门槛往后推了
```

```

</details>

#### §4 候选解法 + §4.2 colocate 决定性诊断

**为什么作废**：候选解法已被实测取代；colocate 那个诊断要问的（陈旧度是不是主因）已由 E22 直接回答

**被谁推翻**：E20 §7 / E22

<details><summary>展开原文</summary>

```markdown
## 4 · 候选解法（按**对训练正确性的影响**排，不按实现难度）

| # | 做法 | 为什么可能对 | 代价 / 风险 | 判据 |
|---|---|---|---|---|
| **① 换 token 级 IS** | `rollout_is: sequence → token` | 逐 token 截断，**不在长度上指数放大**；verl 原生支持（`core_algos.py:2397` 明写 `"token"`） | 有偏（token 级 IS 不是严格的序列级修正），但**方差小得多** | ESS 不再随步数塌 + 位移能起来 + 任务分不掉 |
| **② 同步更频繁** | `sync_every 4 → 2 / 1` | 直接减小陈旧度 —— §3 说主因就是它 | **吞吐降 13–16%**（B19 的表反过来读） | 同上 |
| **③ 收紧陈旧度阈值** | `staleness_threshold 0.1 → 0.05` | 同②，但更"外科" | 吞吐降、rollout 卡更闲 | 同上 |
| ⛔ ~~提高 `rollout_is_threshold`~~ | 2.0 → 更大 | —— | **这是把温度计砸了**：权重本来就该被截断，放大阈值只是让崩塌不显示 | 不做 |

| **⓪ ★ 几何级拒绝采样** | `rollout_rs=seq_mean_k1` | **专治"长度陷阱"**：按长度归一后，长短序列拿到同一个信任分；与 `rollout_is` 正交，可叠加 | 需要选阈值；会**丢样本**（掩码而非加权） | ESS 不塌 **且** 长序列不被系统性剔除 |
| **④ 增加优化器更新次数** | `ppo_mini_batch_size 6→2` / `ppo_epochs 1→2` | §3.6：一个 epoch 只更新 109 次 | 会加大离策略程度 ⇒ **必须在①之后做** | 位移能起来 |
| **⑤ 退回 colocate** | `--mode colocate` | **colocate 下陈旧度 = 0**（生成用的就是当前权重）⇒ 直接消掉主因 | 吞吐 33 → 55 s/步（慢 1.66×） | ★ 也是**最干净的诊断实验**，见 §4.2 |

★ **①和②③是正交的**，可以叠加；但**先单独做①**，因为它不牺牲吞吐。

### 4.2 ★ 一个 30 分钟就能做的**决定性诊断**：colocate + lr=3e-5

**陈旧度是 fully_async 特有的**：

```
colocate       生成前刚同步过权重 ⇒ rollout 用的**就是当前权重** ⇒ **陈旧度 = 0**
               （剩下的只有 vLLM↔FSDP 的数值失配那 0.0001 地板）
one_step_off   恰好落后一个版本
fully_async    落后好几个版本（受 sync_every / staleness_threshold 控制）
```

⇒ **拿 colocate 在 lr=3e-5 下跑一次，看 ESS 还塌不塌**：
```
ESS 不塌  ⇒ 崩塌确实由陈旧度造成（§3 的推断被证实）⇒ 修 IS 口径 + 控陈旧度是对的方向
ESS 还塌  ⇒ 主因不是陈旧度，而是「策略自己走得快 ⇒ π_new/π_old 本来就大」
           ⇒ 那要回去看 clip 范围、mini-batch 划分、优势归一化
```
⚠️ 这个实验**比换 token 级 IS 更早给出方向**，而且它不改任何算法 —— 只换 `--mode`。
⇒ 已排 **E20-d**，优先级与 E20-a 并列。

```

</details>

#### §5 两个加速结论要打折 + §6 下一步

**为什么作废**：两个加速结论已在干净基线上重测（B19/B10）；下一步已被夜跑结果整段改写

**被谁推翻**：E08 §5-6 / handoff §5.1

<details><summary>展开原文</summary>

```markdown
## 5 · ⚠️⚠️ 这条发现让**昨晚两个加速结论要打折**

**B19（`sync_every` 4→8 提速 10%）和 B10（`staleness_threshold` 0.1→0.3 提速 8%）
的 ESS 代价，是在 `lr=1e-6` 下量的**（`launch_rl` 的默认值），而 B16 用的是 `3e-5`：

```
lr=1e-6   模型几乎不动 ⇒ rollout 与 trainer 天然贴近 ⇒ ESS 看起来很好（0.87–0.92）
lr=3e-5   模型真的在动 ⇒ ESS 从 0.9 塌到 ~0.35
```

⇒ **B19/B10 测到的"ESS 没掉"，是在一个"什么都没发生"的工况下测的** ——
它们**系统性地低估了陈旧度的代价**。
⇒ ⛔ **在 E20 结案之前，不许把 `sync_every=8` 或 `staleness=0.3` 写进默认配置。**
⇒ 两条都要**在能真正学习的 lr 下重测**（排成 **B21**）。

★ **教训（新的一条）**：**在"系统什么都没发生"的工况下测出来的代价曲线，是没有代价的曲线。**
测一个"节流阀"的代价，必须让被节流的东西真的在流动。

---

## 6 · 下一步（按新的优先级：正确性 > 速度）

1. 🔴🔴 **E20-a · token 级 IS**：`rollout_is=token`，`lr=3e-5`，60 步，四件判据一起看。
   **这是当前整个项目优先级最高的实验。**
2. 🔴 **E20-b · 陈旧度对照**：同 lr 下 `sync_every ∈ {1, 2, 4}`，看 ESS 崩塌速度与位移。
3. 🔴 **B21 · 重测 B19/B10 的代价**（在能学习的 lr 下）—— 它们现在的结论不可用。
4. 🟠 **E20-c · 数值失配地板**：0.0001 这个数值得单独钉住（它是 TIS/E15 那条线的起点），
   并且**换 FP8 rollout 之后要重测它**（E19 §3-③ 的前置判据）。
5. 🟠 若 ①②③ 都不能让位移起来 ⇒ 问题不在 IS，而在**优势/奖励信号本身**
   （比如 GRPO 组内方差不足 —— 动态分池的 `pool/saturated_share` 就是量这个的）。
   ⇒ 那时再回到数据侧。**先别跳到这一步，按顺序排除。**
---

```

</details>
### 5.2 来自 `E08-async-rl.md`

#### §4.7 B3 三模式同尺子

**为什么作废**：跨模式的**学习类**对比是「好的 vs 坏的」—— fully_async 当时 rollout 策略恒为 π0。时间类数字仍有效但解释要按新载荷重算

**被谁推翻**：E22 §4 / E08 §5

<details><summary>展开原文</summary>

```markdown
### 4.7 ★★ B3 · **三模式同尺子**（2026-08-17 20:01）—— 这句兑现物此前是假的

除 `--mode` 外全部相同（v13 数据、bucket 512、`dynamic_bsz=False`、rollout_n 8、
`sync-every 4`、同一底座）。取中位（首步含预热，见下）。

| 模式 | 卡 | **每 global step** | 相对 colocate3 | 生成那一项 |
|---|---|---|---|---|
| **colocate** | 3（共卡） | **55.28 s** | 1.00× | `gen` **26.82 s（48.5%）** ← 完全暴露 |
| **one_step_off** | 3+1 | **36.43 s** | **1.52×** | `generate_async` 28.01 s，与训练**重叠 76.9%** ⇒ `gen` 计 **0.00 s** |
| **fully_async** | 3+1 | **33.25 s** | **1.66×** | `gen` **3.40 s（10.2%）** |

原始 step 序列（首个是预热）：
```
colocate3      70.28 · 58.98 56.59 45.83 54.58 55.28 52.12
one_step_off   63.08 · 36.57 37.95 36.56 36.30 34.16 35.77 33.87
fully_async    （每行覆盖 4 步）106.42 · 134.39 132.98  ⇒ 26.6 · 33.6 33.2 每步
```

**⇒ 三条结论：**

1. **异步的收益落在预测区间内。** P-B1 写死的上界是长尾比 **1.37–2.75×**，
   实测 **1.52× / 1.66×** ⇒ **预测成立**（而且没有超出上界 —— 超出就说明口径错了）。
2. ★★ **「异步 = 把生成藏起来」只解释了一半。**
   `one_step_off` 把生成藏得**更彻底**（`gen` 计 0.00，`generate_async` 与训练重叠 76.9%），
   **却仍然比 `fully_async` 慢 10%**（36.43 vs 33.25）。
   ⇒ 差别在别处：`one_step_off` 每步都要 `update_weights + sync_rollout_weights`
   （各 8.42 s，占步 23.1%），而 `fully_async` 是 **4 步才同步一次**（摊薄后 2.63 s，占 7.9%）。
   **⇒ 真正的杠杆是「同步频率」，不是「藏没藏住生成」。**
3. **colocate 的 48.5% 花在生成上** —— 这是「异步该省的那块」的直接测量值，
   和 §4.5.1 的占空比（rollout 卡 82.5% 空闲）是同一件事的两个侧面。

⚠️ **成立范围**：`--steps 8/8/12`，短跑；`latency_scale=0.01`。
⚠️ `one_step_off` 那一跑里有**一次** `save_checkpoint`（5.51 s），落在最后一行，
未影响中位（那一行的 step 反而是最低的 33.87）。

```

</details>

#### §4.10 B19 sync_every 扫描

**为什么作废**：在坏基线上量的：当时陈旧度这个旋钮没接到任何东西。已在干净基线上重测（4→16 吞吐 +11.4%，估计量无代价）

**被谁推翻**：E08 §5

<details><summary>展开原文</summary>

```markdown
### 4.10 ★★ B19 · `sync_every` 扫描（2026-08-18 05:38）—— **B3 指认的那个杠杆，第一次量**

B3 发现「one_step_off 把生成藏得更彻底却仍慢 10%，差别在同步频率」⇒ 那就把这个旋钮扫开。

⚠️ **口径陷阱先说**：fully_async 的 `global_steps` 每个 fit step 跳 `sync_every`，
而 `--steps` 卡的是 `global_steps` ⇒ **固定 `--steps` 去扫 sync_every，四档做的训练更新次数完全不同**
（16 档只更新 1 次）。⇒ 本实验按 `steps = 4 × sync_every` 给，**四档都恰好更新 4 次**。

| `sync_every` | 每 global step | 同步摊薄 | **其余部分** | 相对 se=4 | 陈旧轨迹 | **ESS/N ≈** |
|---|---|---|---|---|---|---|
| 2 | 37.56 s | 5.23 | 32.33 | 0.86× | 24 | **0.921** |
| **4（现用）** | 32.43 s | 2.37 | 30.06 | 1.00× | 48 | **0.894** |
| **8** | **29.37 s** | 1.19 | 28.18 | **1.10×** | 96 | **0.874** |
| **16** | **27.88 s** | 0.60 | 27.28 | **1.16×** | 216 | **0.890** |

**⇒ 三条结论：**

1. ✅ **P1 命中**：单次 param_sync 的绝对时长四档都是 **9.1–9.9 s**（它是固定成本），
   省下来的全是**摊薄** —— 摊薄值 5.23 → 2.37 → 1.19 → 0.60，**精确地按 1/sync_every 走**。
2. ★★ **但省的不只是摊薄**：把同步摊薄扣掉之后，**「其余部分」也从 32.33 降到 27.28**（−15.6%）。
   ⇒ **同步的代价大于同步本身** —— 每次同步还要打断 rollout（abort→排空→重建 KV→恢复），
   同步越少，生成侧的流水越顺。**这一层此前没人算过。**
3. **代价（ESS/N 代理）**：0.921 → 0.894 → 0.874 → 0.890，**从 4 到 8 掉 2.2%**，16 那档又回来了
   （多半是噪声）。**全都远高于 0.3 的停止线。**

⇒ **`sync_every=8` 看起来是接近白捡的 10%**（甚至 16 档 16%）。
⚠️⚠️ **但同 B10 一样：不许据此直接改默认值。** 陈旧度的真实代价是**长程策略质量**，
12–64 步的短跑量不出来。⇒ **B10 与 B19 合并成一次长跑 + 配对评测**（B17）。

★ 顺带：**B10（`staleness_threshold` 0.1→0.3，+8.2%）和 B19（`sync_every` 4→8，+10%）
是两个不同的旋钮**，若可叠加则接近 +20% —— 但**叠加性本身要测**，别直接相乘。

```

</details>

#### §4.8 B10 陈旧度节流的代价曲线

**为什么作废**：同上，且是在 lr=1e-6（模型几乎不动）下量的。已重测：陈旧样本 6× 而估计量无变化

**被谁推翻**：E08 §6

<details><summary>展开原文</summary>

```markdown
### 4.8 ★ B10 · 陈旧度节流的代价曲线（2026-08-17 21:28）—— **成因②第一次有实验对着**

`staleness_threshold` 扫三档，其余全锁死（fully_async 3+1，12 步）：

| `staleness_threshold` | 每 global step | 相对 0.1 | IS 序列均值 | IS 标准差 | **ESS/N ≈** | 处理过的陈旧轨迹 |
|---|---|---|---|---|---|---|
| **0.1**（现用） | 32.86 s | 1.000× | 0.962 | 0.349 | **0.884** | 32 |
| **0.3** | **30.36 s** | **1.082×** | 1.010 | 0.352 | **0.892** | 112 |
| **0.5** | 30.33 s | 1.083× | 0.963 | 0.315 | **0.903** | 192 |

⇒ ★★ **放宽到 0.3 提速 8.2%，而 ESS 代理值一点没掉**（0.884 → 0.892，还略升，在噪声内）。
⇒ **0.3 → 0.5 再无收益**（30.36 vs 30.33）—— 旋钮在 0.3 附近就饱和了。
⇒ 「处理过的陈旧轨迹」32 → 112 → 192（6×）⇒ **旋钮确实在起作用**，不是没接上。

**⇒ 这是占空比成因②的第一个 after 候选**：`0.1` 是当初拍的数，
**实测它值 8% 的吞吐，而买到的东西量不出来**。

⚠️⚠️ **三条限定，缺一不可**：
1. ⛔ **2026-08-18 更正**：上表的 ESS 是我用 CV 算的**代理值**，而当时我写「这一跑里没有
   verl 自报的 ESS 字段」——**那句话是错的**，字段一直在：`rollout_corr/rollout_is_eff_sample_size`。
   按真值重读这几跑：`lr=1e-6` 下 ESS 全程 **0.85–0.90**，与代理值同结论。
   ⇒ **但更要紧的是 §E20 §5 那条**：这几跑的 lr 是默认的 **1e-6**，模型几乎不动
   ⇒ **陈旧度的代价被系统性低估**。本节的结论在能真正学习的 lr 下要重测（B21）。
2. **12 步的短跑**。陈旧度的真实代价是**长程的策略质量**，短跑量不出来
   —— 这正是 `ESS/N` 看起来没动的原因之一。
3. ⇒ **不许据此直接把默认值改成 0.3**。要改，先过 **B5** 的任务级尺子
   （和 E17 一样的要求）。⇒ 排进 **B17**：与 B16 合并成一次「长跑 + 配对评测」。

```

</details>

### 5.3 来自 `E12-weight-sync.md`

#### §4.5–§4.7 分步计时 / 稳态结案 / bucket 推翻 / 「与传输量无关」

**为什么作废**：整段建立在「稳态只推 132 MB LoRA」这个**从没量过**的前提上——实测每次推的是 **8,414 MiB 冻结基座**。修法① 之后 param_sync 稳态 **0.974 s**，而 sync_every 省下的钱**根本不在权重同步里**（gen 占降幅 96%）

**被谁推翻**：E22 §6.4.3 / E08 §5.5

<details><summary>展开原文</summary>

```markdown
## 4.5 ★★ E12-b 分步计时（2026-08-14，独占，`SYNCOPATE_SYNC_TIMING=1`）

台架：`verl_patches._patch_sync_step_timing`（可选补丁，挂 `setup_worker`——
`CheckpointEngineManager` 活在 **FullyAsyncTrainer 这个 Ray actor** 里，driver 侧打无效）。
判据行 `[verl-patch] 权重同步分步计时已启用` 已在日志确认。
运行：`logs/e12b_synctiming.log`，配置与 M7 完全一致，`--steps 12`。

### 第一次同步（param_version 0，推基座 + LoRA）

| 步骤 | 耗时 | 占比 |
|---|---|---|
| 1 `abort_replicas` | **0.008 s** | 0.0% |
| 3 `release_kv_cache_replicas` | **0.003 s** | 0.0% |
| 4 `build_process_group` | **45.967 s** | **42.6%** |
| 7 `resume_kv_cache_replicas` | **0.002 s** | 0.0% |
| 8 `resume_generation_replicas` | **0.002 s** | 0.0% |
| — 未计量（2 建 WorkerGroup + **5 传输** + 6 finalize） | 62.05 s | 57.4% |
| **param_sync 总计** | **108.034 s** | 100% |

⇒ **⛔ 推翻了 §4.2 之后写下的主假设**（见 §6-③）：
「KV cache 拆了重建 + partial rollout 中断恢复是大头」——
**这四步加起来只有 0.015 秒**，完全不是。

### ★★★ 第二次同步（param_version 1，稳态：只推 LoRA 132 MB）——**结案**

| 步骤 | 耗时 | 占比 |
|---|---|---|
| 1 `abort_replicas` | 0.008 s | 0.01% |
| 3 `release_kv_cache_replicas` | 0.004 s | 0.01% |
| 4 `build_process_group` | **0.023 s** | 0.04% |
| 7 `resume_kv_cache_replicas` | 0.002 s | 0.00% |
| 8 `resume_generation_replicas` | 0.001 s | 0.00% |
| **已计量小计** | **0.038 s** | **0.06%** |
| **未计量 = 第 5 步（传输 + 两侧 update_weights）** | **59.76 s** | **99.94%** |
| **param_sync 总计** | **59.797 s** | 100% |

★ **`build_process_group` 从 45.967 s 掉到 0.023 s（2000×）** ——
它是**一次性**开销（首次建 NCCL 组），`rebuild_group=False` 完全按设计工作。
⇒ 我在第一次同步后写下的「它占稳态 82%」的推测**当场被第二次数据推翻**（见 §6-④）。

### 4.5.1 首次 `build_process_group` 的 50 s 是什么（`logs/e12c_step5.log`）

再拆一层（探针打在 `NCCLCheckpointEngine` 上）：

```
engine.prepare(分配 2×2048MB buffer)      0.133 s   ← 0.3%
engine.init_process_group(rank=0)        50.083 s
engine.init_process_group(rank=1)        50.095 s
build_process_group 合计                 50.239 s
```

⇒ **首次开销 99.7% 是 `init_process_group`，即 NCCL 通信组的建立**，
和 buffer 分配无关（0.133 s，与空卡微基准 2.6 ms 同量级，说明"在被 vLLM 占满的卡上分配 4 GB 很贵"
这个猜测也**不成立**）。
⇒ 且它是**一次性**的（第二次起 0.023 s）⇒ **不在优化的关键路径上**，如实记录、不再追。

### 4.5.2 ★ 第 5 步拆成 trainer 侧 / rollout 侧（`logs/e12c_step5b.log`）

探针打在 `ActorRolloutRefWorker.update_weights`（trainer 侧）与
`CheckpointEngineWorker.update_weights`（rollout 侧）。
⚠️ 两者**并发执行**（`ray.get(trainer.update_weights(...) + rollout.update_weights(...))`），
**不能相加**。

**首次同步（param_version 0，推基座 8 GB + LoRA）**：

| 步骤 | 耗时 |
|---|---|
| `build_process_group`（其中 `init_process_group` 53.87） | 54.087 s |
| **trainer 侧：取参数 + send** | **67.086 s** |
| **rollout 侧：recv + 装进 vLLM** | **70.978 s** |
| 其余编排（abort / KV 拆建 / 恢复）合计 | 0.014 s |
| **param_sync 总计** | **125.103 s** |

⇒ **rollout 侧（71.0）≈ trainer 侧（67.1）+ 4 s**，而两者并发
⇒ **rollout 大部分时间在等着收，瓶颈在 trainer 侧。**
⇒ 首次推 8 GB 用 67 s ⇒ 有效带宽 ≈ **120 MB/s**，与 §4.1 两点反解出的 168 MB/s 同量级
（口径不同：那里是端到端反解，这里是单侧墙钟）。

### 4.5.3 ★★★ 稳态：trainer 侧的成本**与传输量完全无关**

```
首次同步（推 8 GB 基座 + LoRA）   trainer侧 取参数+send  67.086 s
第二次同步（只推 132 MB LoRA）    trainer侧 取参数+send  69.887 s
```

**数据量差 60×，耗时反而略涨。** ⇒ **成本不在 send，在"取参数"那一步。**

### 4.5.4 根因：`layered_summon` —— 为分片 FSDP 设计的路径，被用在了不分片的机器上

`ActorRolloutRefWorker.update_weights` → `get_per_tensor_param` → `collect_lora_params`。
我们**已经开着** `layered_summon=True`（`launch_rl.py`，跟 LoRA 配置一起写死、无注释），
所以走的是 `fsdp_utils.layered_summon_lora_params`：

```python
for prefix in prefix_list:
    for name, submodule in __prefix_submodules(fsdp_module, prefix):   # ← 36 层
        if fsdp_version(submodule) > 0:
            with FSDP.summon_full_params(submodule, writeback=False):
                sub = get_peft_model_state_dict(peft_model, state_dict=submodule.state_dict())
                ...拷 LoRA 到 CPU...
            get_torch_device().empty_cache()        # ← ★ 每层都调一次
```

**每步付 36 × (summon + 该层全量 `state_dict()` 物化 + `empty_cache()`)。**

★ **它是为「真正分片的 FSDP」设计的**——分片时一次性 gather 整个模型会爆显存，
所以宁可逐层。**但我们跑的是 `--fsdp-size 1`（DDP，不分片）**：
参数本来就在每张卡上完整存着，整体 summon 近乎免费，
而逐层路径把一个免费操作拆成 36 份收费操作。

⇒ **这是今天第三次遇到同一个形状**（另两次：E13 存整个模型只为取 3% 的 LoRA；
E11 对全部 token 算 logprob 只为用 4%）：
**「机制是对的，但它假设的运行条件和我们的不一样。」**

### 4.5.5 ⛔ A/B 结果：`layered_summon` **不是**原因，预测被推翻

`--layered-summon False`（`logs/e12d_nolayered.log`，只变这一个变量，
判据已核：日志里 `'layered_summon': False` ×2，基线是 True）：

| | 基线 True | 对照 False |
|---|---|---|
| 首次同步 trainer 侧 | 67.086 s | 58.775 s |
| **稳态 trainer 侧** | 55.76 / 66.24 / 58.40（均值 **60.13**） | **61.443** |

⇒ **对照值正好落在基线的波动区间内，无改善。**

> **原猜想**：`layered_summon` 逐层 summon（36×(summon + state_dict + empty_cache)）
> 是那 60 s 的主因；关掉它 trainer 侧应降到 **< 10 s**。
> **实测**：60.13 → 61.443，**没动**。
> **推翻后**：这反而是一次**高价值的排除**——
> `layered_summon=False` 走的是 `summon_full_params(整个模型)` 这条**完全不同**的取参数路径，
> **两条路径都是 ~60 s** ⇒ **成本不在「取参数」，在两条路径共有的部分。**
> 结合"与数据量无关"（8 GB 与 132 MB 同耗时），**只剩 `send_weights`**。
> **教训**：**A/B 的负结果能一次砍掉一整条分支**——
> 比"再读一遍代码找可疑处"有效得多，因为读码只能生成假设，不能排除假设。

### 4.5.6 ⚠️ 顺带炸出一个真实脆弱点：`gpu_util 0.75` 也是贴着墙的

`layered_summon=False` 那一跑在**第 4 次同步 OOM**（数据已够，A/B 结论不受影响）：

```
rollout 卡（31.37 GB）
  vLLM 进程                    24.65 GB
  CheckpointEngineWorker        4.71 GB   （自身 ~2.71 + 已分配的一个 2 GB bucket）
  ────────────────────────────────────
  已用                         29.36 GB
  剩余                          1.99 GB
  要再分配                      2.00 GB   ← 差 0.01 GB
```

⇒ **和文档里记的 `gpu_util 0.85` 那次 OOM 是同一个形状**（当时"差 0.13 GB"），
只是这次发生在 **0.75** 上 ⇒ **0.75 也不是安全边际，前几跑只是运气好擦过去了。**

**根因**：`prepare()` 在**每个 worker** 上分配 **send_buf + recv_buf 各 2048 MB = 4 GB**，
其中一份就住在被 vLLM 占了 24.65 GB 的 rollout 卡上。

⛔ **要更正的旧结论**：`gpu_util 0.75` 被记成"安全值"（记忆 + 分布式文档 §7.3）。
**正确的说法是：0.75 只是勉强够，实测会在第 4 次同步 OOM。
真正的解法不是压 `gpu_util`，是调小 bucket** ——
那 4 GB 里绝大部分根本用不上（实际只推 132 MB）。

★ **由此，下一个 A/B 一箭双雕**：调小 `--weight-sync-bucket-mb`
① 若耗时按比例下降 ⇒ 坐实"广播整个定长 buffer"的猜想；
② 无论如何都解除这个 OOM 脆弱点（2048→512 MB 可给 rollout 卡腾出 3 GB）。

⬜ **下一层探针已加**：单独给 `NCCLCheckpointEngine.send_weights` 计时，
把 60 s 切成「取参数」与「发」两半。⚠️ 新的可疑点：
`prepare()` 分配的是 **2048 MB 定长 buffer**，而 `collective.broadcast(self.bucket, ...)`
广播的是**整个 buffer**，与实际装了多少无关 —— 这与"耗时和数据量无关"的观测吻合。
若成立，`--weight-sync-bucket-mb` 调小应按比例降耗时，是下一个一行开关的 A/B。

### ⇒ E12 的答案（终）

```
稳态权重同步 59.8 s
  ├─ 编排（abort / KV cache 拆建 / 建组 / 恢复）  0.038 s   ← 0.06%，全部免费
  └─ 第 5 步：传输 + 两侧 update_weights        59.76 s   ← 99.94%
       └─ 其中真正的数据传输（两点反解）           ~0.8 s   ← 1.4%
```

**⇒ 约 59 秒花在「处理/装载 132 MB 的 LoRA」上，既不是编排，也不是传输。**
下一层（trainer 侧取参数+send vs rollout 侧 recv+装进 vLLM）的探针已加好，待下一跑。

## 4.6 🐟 2026-08-17 补：主线 v13-e1 跑里白捡的观测 —— **稳态 55.8 s 不是普适常数**

> 来源：主线 2026-08-17 16:20 那一跑（fully_async 3+1，v13 数据），**零成本旁观，未占用 GPU**。
> ⚠️⚠️ **口径污染，必须写在最前面**：那一跑被 `nsys profile --delay 900 --duration 180` 包着，
> 而且主线在 16:52 把它**弃掉重跑了**（`checkpoints/grpo/m7b_v13e1_nsys_aborted`）。
> 采样窗口（16:35:46–16:38:46）晚于本节用到的 6 条 timing 行，profiler 开销**应该**很小 ——
> 但「应该」不是判据。⇒ **同样的数要在 16:52 起的干净跑上复测**，复测前本节只作方向性证据。
> 尺子：`scripts/parse_fully_async_timing.py`（新建，已把「timing 行覆盖 4 个 global step」固化进工具）
> 数据：`logs/e12d_v13e1_timing.json`

这一跑显式传了 `update_weights_bucket_megabytes=512`（本报告的 55.8 s 是 **2048** 下量的）：

```
param_sync   13.34 → 10.32 → 9.22 → 8.39 → 8.12 → 8.47 → 7.96 s     稳态中位 8.43 s
对照本报告   稳态 55.8 s（bucket 2048）                               ⇒ 差 6.6×
```

⇒ **这与 §8-2 的候选修法「buffer 能否常驻复用而不是每次 alloc/free」指向同一件事**：
`bucket ÷4 ⇒ param_sync ÷6.6`，**耗时随 bucket 尺寸走，而不是随实际传输量（恒定 132 MB）走**
—— 正是「广播/处理的是整个定长 buffer」这个猜想预测的形状。

⚠️⚠️ **这不是同尺子 A/B，不许当结论用。** 同时变了至少五个变量：
数据版本（v11→v13）、底座（sft-v11-e1→v13-e1）、`dynamic_bsz`（True→False）、
`gpu_memory_utilization`（0.75）、`rollout.n=8`。
⇒ **它只把 B2 从「25 分钟去验证一个猜想」改成「25 分钟去补一个干净分母」**，B2 不能取消。

★ 同一批 timing 行还顺手更新了占空比的构成（每行覆盖 4 个 global step，已折算）：

| 项 | 本次实测（占步） | 本报告 §4.4 记录（M7） |
|---|---|---|
| update_actor | **54.0%** | 33.1% |
| old_log_prob | 16.7% | 25.7% |
| ref | 14.2% | 13.2% |
| **⇒ 三次前向合计** | **84.9%** | **72.0%** |
| gen | 9.0% | 6.3% |
| **param_sync** | **6.5%** | **18.8%** |
| step（每 global step） | 32.7 s | 74.1 s |

⇒ ★★ **两个方向相反的位移，都直接改队列的排序**：
① 三次前向 72% → **84.9%**（B12 / E17 更该打）；
② 权重同步 18.8% → **6.5%**（B1 的可回收空间缩水到原来的 1/3）。

⚠️ 同样受上面那五个变量污染，**方向可信、数值待同尺子复核**（B2 补分母、B3 补三模式）。

★ **教训（本报告要补的一条）**：§6 已经写了「同一个名字的指标在不同模式下不是同一件事」，
这次是它的**下一层** —— **同一个模式、同一个指标，换一个默认参数（bucket）就差 6.6 倍。**
⇒ 全局常量表里的每个数，除了记「哪个模式」，还要记**「当时那几个旋钮拧在哪」**。

## 4.7 ⛔ B2 实测：**bucket 不是原因** —— §4.6 的推论被推翻（2026-08-17 19:37）

同尺子 A/B（fully_async 3+1，12 步，**只改 bucket 这一个变量**，其余全锁死）：

| bucket | param_sync 各次 | **稳态中位** | 每 global step | 三次前向占步 |
|---|---|---|---|---|
| **512** | 12.77 / 10.21 / 8.63 / 9.04 | **9.04 s** | 32.57 s | 83.1% |
| **2048** | 13.70 / 10.85 / 8.79 / 9.73 | **9.73 s** | 32.88 s | 82.9% |

⇒ **差 7.7%，不是 6.6×。**

> **原猜想**（§4.6，由主线那一跑推出来的）：`bucket ÷4 ⇒ param_sync ÷6.6`，
> 即「广播/处理的是整个定长 buffer」。
> **实测**：只改 bucket，两档几乎一样。
> **推翻后**：**bucket 不解释 55.8 → 9 s 这个 6 倍。** §4.6 那段推论作废，
> 但**它推出来的那个「现象」仍然成立**：param_sync 今天就是 ~9 s，而本报告记录的是 55.8 s。
> **教训**：**同一次观测里的两件事要分开** ——
> 「param_sync 现在只有 9 秒」是**观测**（可信）；
> 「因为 bucket 小了」是**归因**（当时就标了「不是同尺子 A/B」，现在被证伪）。
> ⚠️ 而我**用这个归因改过队列排序**（B1 降级）。**排序的方向仍然对**
> （9 s 就是 9 s，B1 的可回收空间确实只剩 1/3），**但理由错了** ——
> ⇒ 纪律：**带着「未验证归因」去改优先级是可以的，但要在队列里标明它是待验的**，
> 否则验证结果回不来的时候，没人知道该回去改哪一条。

### 4.7.1 ⇒ 新问题（比原来那个更值钱）：**那 6 倍到底是谁干的**

已锁死不是：bucket（本节）、`layered_summon`（§4.5.5 的 A/B 两档都 ~60 s）。
仍在候选里的变量（M7 那次 vs 今天）：

```
数据版本      v11 → v13（序列长度分布不同 ⇒ 同步前要排空的在飞请求量不同）
底座          sft-v11-e1 → sft-v13-e1
dynamic_bsz   True → False
E13 的修复     ✅ **已查（零 GPU，git log -S）**：`if param.requires_grad` 落在
              **commit 743a6e0 · 08-16 12:50**，而 M7 跑在 **08-14 05:25–09:09**
              ⇒ **M7 那次跑的时候这个修复还不存在**，是一条**确凿的差异**。
              ⚠️ 但它**未必够解释 6 倍**：E13 自报每步省 4.34→0.083 s，
              4 步也就 ~17 s，而缺口是 ~46 s ⇒ **它像是其中一块，不像是全部**。
              而且它patch 的是 `fsdp2_sharded_save_to_cpu`（proximal anchor 快照），
              与 param_sync 的 `get_per_tensor_param` **是两条路径** —— 除非其中一趟
              搬运正好落在同步窗口里。**这一步只能靠实验分**
verl 版本      两次都记的 0.8.0（未复核）
```

★ **为什么值得查**：一个**没人刻意优化过的组件，悄悄快了 6 倍**。
要么是真收益（那要知道是谁给的，别哪天改回去）、要么 E12 的招牌数字 55.8 s 有问题
（那 TRACK-B 的兑现物之一要重写）。**两种情况都必须知道。**
⇒ 已排 **B15**：先查 E13 落地时间与 M7 的先后（**零 GPU，git log 就能定**），
再按需做一次 v11 + dynamic_bsz=True 的复现跑。

## 5 · 结论

1. **权重同步的时间 98.6% 不是传输**，而是同步前后那七个步骤的固定开销。
   ⇒ P-B2 **方向对了**（不在传输不在计算），但**具体机制猜错了一半**：
   不是 NCCL 建组、不是遍历全参，而是 **buffer 分配/释放 + `empty_cache()` + KV cache 拆建 + partial rollout 的中断/恢复**。
2. **瓶颈整体在训练侧的 logprob 计算**：三次前向 `update_actor` + `old_log_prob` + `ref`
   = 213.6 s = **72%**；而 rollout 生成只占 **6.3%** —— 异步确实把生成藏起来了，这是它该有的样子。
3. ⇒ **E11（稀疏 logprob）的价值被抬高**：那三项**全部**在算 logprob，且**全部**对整条打包序列算
   （88.3% 是 prompt）。E11 一次命中三项。
   ⚠️ **但别把 E11 的「24×」读成端到端 24×**：省的只是 lm_head 那一层（约占参数量 10%），
   36 层前向对 prompt 仍要算。粗估端到端收益 ≈ 10% × 72% ≈ **7%**，需 E01 确认。

## 5.1 ★★ 一个必须写死的区分：这笔钱**不是**「免传输策略」治的那笔

本报告的结论极易被误读成「免传输策略没用」。**两笔钱完全无关**：

| | 权重同步（本报告） | 训练侧分片通信（Track A 的免传输策略治的） |
|---|---|---|
| 是什么 | trainer → rollout 推新权重 | FSDP 每层 all-gather 冻结基座 |
| 多久一次 | 每 `sync_every`=4 步一次 | **每个 micro-batch 一次** |
| 多大 | LoRA **132 MB** | 约 **90 GB** |
| 实测结论 | **98.6% 不是传输**（本报告） | **传输就是要命的那一项**：3 卡 FULL_SHARD 1182 s/步 vs 单卡 198 s ⇒ 多给卡慢 6 倍；换 DDP 立刻 3.00× 线性 |

⇒ **E07 的 4bit 量化复制（配置 C）不受本报告影响**，它治的是右边那列。

⇒ **但本报告确实枪毙了一个想法**：「把 LoRA 权重再压缩让同步更快」——
传输只占 0.8 s，压到 0 也只省 1.4%。**优化方向要从「传得更快」改成「让停工—复工的仪式更便宜」。**
这两个方向的工作内容完全不同：不查就动手，很可能去调 bucket 大小 / 换传输后端 / 压权重
——**全都在动那 0.8 秒。**

## 6 · ⛔ 推翻了什么

> **原猜想 ①**：权重同步 **13.3 s**（占步 27–36%），来自 one_step_off。
> **实测**：fully_async 稳态 **55.8 s**。**⇒ 这个数不能跨模式引用。**
> **推翻后**：全局常量表要按模式分别记。
> **教训**：**「同一个名字的指标，在不同模式下不是同一件事。」** `update_weights`（one_step_off）
> 和 `param_sync`（fully_async）走的是不同代码路径、包含的步骤也不同。

> **原猜想 ②**（`launch_rl.py` 的注释）：decoupled 模式「每步多一次 actor 前向算 old_log_prob
> （**约 +6–10 s**）。**值得。**」
> **实测**：`old_log_prob` = **76.3 s，占 25.7%**。**低估 8–13 倍。**
> **推翻后**：「值得」这个判断是基于 6–10 s 做的，代价要如实标价：
> 76.3 s × 37 步 = **47 分钟，占全程 224 分钟的 21%**。
>
> ⛔ **但「换来的是 ESS 刹车」这个说法本身也是错的，我一度也这么写过（已更正）。**
> 读码查实（`experimental/separation/ray_trainer.py:503-530` + `config/algorithm.py:123-131`）：
> ```
> bypass  (2 policies)  old_log_prob := rollout_log_prob，走 compute_policy_loss_bypass_mode()
> decoupled(3 policies) 重算 old_log_prob 作为**proximal anchor π_old**，走标准 PPO loss + IS 权重修正
>                       源码注释：「π_old computed once per data batch,
>                                  serves as stable reference during mini-batch updates」
> ```
> ⇒ **`old_log_prob` 不是指标，是损失函数的一部分。** 它就是 AReaL 的 π_prox ——
> 「用 π_prox 而不是 π_behav 算 IS 比率，防止当前策略被陈旧的低质策略拽偏」。
> AReaL 的消融：decoupled 下 η≤4 安全，**朴素 PPO 超过 η=1 就崩**。
> ⇒ **这 76.3 s 买的是「异步在高陈旧度下还能训」这件事本身，ESS 指标只是副产品。**
>
> ⇒ ⛔ **因此「ESS 降频采样（每 N 步算一次 old_log_prob）」不可行**——
> 那不是「少测几次」，那是**在两个不同的目标函数之间来回横跳**
> （decoupled PPO ↔ bypass PPO）。**别做。**
>
> ⇒ ✅ **真正可查的优化方向**：`old_log_prob`（76.3 s）约是 `ref`（39.2 s）的 **1.95×**，
> 而两者**都是同一批数据上的纯前向**。差这一倍是从哪来的？
> （old_log_prob 顺带算 entropy，但 `use_fused_kernels=True` 下 entropy 应该几乎免费。）
> **若两者本该相当，则每步有约 37 s 是没有解释的** → 挂到 E01 一起查。
> ⇒ 另外 **E11（稀疏 logprob）直接命中它**：old_log_prob 是纯 logprob 前向，正是 E11 优化的形状。
> **教训**：**注释里的估算数字会被后人当实测引用。** 估算就要标「估算」，
> 并在拿到实测后回填——这条已经在 E11 犯过一次（配置上限当实际值）。

> **原猜想 ③（读码之后、分步计时之前）**：55 s 的固定开销大头是
> **KV cache 拆了重建 + partial rollout 的中断/恢复 + `empty_cache()`**。
> **实测**：这四步加起来 **0.015–0.038 s**，占比 **0.06%**。`empty_cache` 微基准 12.2 ms。
> **推翻后**：编排**全部免费**，钱在第 5 步的**权重处理**上。
> **教训**：**「读码列出来的步骤多」不等于「那些步骤贵」。** 我从调用链上数出 8 步，
> 就默认成本分散在这 8 步里——实际是 7 步几乎为 0、1 步占 99.94%。
> ⇒ **列出候选之后要逐个称重，不能按"看起来重"排序。**

> **原猜想 ④（第一次同步数据出来之后，当场写下的）**：
> `build_process_group` 46 s ⇒「若每次都这样，它占稳态同步的 ~82%」。
> **实测**：第二次同步 **0.023 s**，掉了 2000 倍。
> **推翻后**：它是**一次性**建组开销，`rebuild_group=False` 按设计工作。
> **教训**：**n=1 的推断连一次都撑不过。** 这条猜想从写下到被推翻只隔了约 6 分钟——
> 便宜是因为它写在报告里而不是写进了代码。**先记录假设、再等第二个数据点，成本极低。**

## 7 · 踩的坑

| 症状 | 根因 | 修法 |
|---|---|---|
| 日志里 `timing_s/timing_s/param_sync` 键名重复 | verl 拼前缀时重复加了一层 | 解析时按 `timing_s/timing_s/param_sync` 匹配 |
| 一开始以为 fully_async 每 step 74 s 比 one_step_off 32.6 s 慢一倍多 | **基线不可比**：one_step_off 那个数是 `bypass_mode=True` 跑的（不含 old_log_prob 的 76.3 s） | **不下这个结论**；需要 E08-b 的同机分母 |
| ⛔ **给第 5 步加探针，整跑崩了**：`AttributeError: 'RayWorkerGroup' object has no attribute 'update_weights'` | `update_weights` 上有 verl 的 `@register(...)`，它把 `{dispatch_mode, execute_mode, blocking}` 挂在函数的 **`MAGIC_ATTR = "attrs_3141562937"`** 上（`decorator.py:440`），**`RayWorkerGroup` 靠扫描这个属性自动生成组级方法**。我用裸的 `async def` 换掉方法，把元数据丢了 ⇒ 组级方法直接不存在 | 包装时 `functools.wraps` **并把 `MAGIC_ATTR` 原样复制过去**；拿不到该属性就**跳过探针**而不是硬装。★ 教训：**包装带装饰器的方法，必须把装饰器挂的属性一起带过去**——"看起来只是加一层计时"是最容易忽略这点的场景 |

## 8 · 下一步 / 衍生问题

0. 🆕 **B2 的目标已改写（2026-08-17，见 §4.6）**：bucket 512 下主线实测 param_sync 稳态
   **8.43 s**，而本报告的 55.8 s 是 bucket 2048 下量的。⇒ B2 不再是「验证一个猜想」，
   而是**在同尺子下补一个干净分母**（其余变量全锁死，只改 bucket）。
   ⚠️ 在 B2 出结果之前，**别再引用「权重同步占步 18.8%」** —— 它绑死在 bucket 2048 上。
1. **E12-b · 给 8 个步骤分别加计时**（🔴 需一次独占 GPU 跑）——
   确认 55.0 s 的固定开销里，`empty_cache()` / KV cache 拆建 / partial rollout 恢复 各占多少。
2. **候选修法（等 ① 定位后再动，别猜着改）**：
   - `finalize()` 里的 `torch.cuda.empty_cache()` 是否必要？buffer 能否**常驻复用**而不是每次 alloc/free？
   - KV cache 是否必须整体释放？（权重变了旧 KV 确实失效，但**释放显存**和**作废缓存**是两件事）
   - 调大 `--sync-every`（现在 4）——**但这会加大 staleness**，是取舍不是优化。
3. **重新评估 decoupled 的性价比**（§6-②）：ESS 刹车能否降频。
4. **E08-b · colocate 同机基线**——现在有两个模式的数了，就差同机分母。
```

</details>

### 5.4 来自 `E17-triple-forward.md`

#### §5.1 补上一把尺子：关掉 KL 之后模型真的漂得更快吗

**为什么作废**：① 位移是**输入**不是产出（AdamW 下 ≈ lr×次数）② 数字在坏基线上量的 ③ 更新次数分母差 4 倍。吞吐侧的 A/B 比值仍然有效

**被谁推翻**：E20 §3.12 / E20 §7

<details><summary>展开原文</summary>

```markdown
## 5.1 ★★★ 补上一把尺子：**关掉 KL 之后，模型真的漂得更快吗**

> 起因：Chaoyu 指出 —— 删掉 KL 是**脱离算法设计**，而且我们的尺子（任务完成度）**太粗**，
> 「真的训飞了」和「尺子测不出来」在它眼里长得一模一样。**这个批评是对的。**
> ⇒ 于是直接量**位移本身**，而不是等任务分。尺子：`scripts/rl_ckpt_drift.py`（🆕），
> 口径抄 `weight_shift.py`：`ΔW_eff = (α/r)·B@A`，比的是 `‖ΔW_eff‖/‖W_base‖`，**只对被适配的层算**。

两臂各 60 步（各自跑到自己的收尾保存）：

| 臂 | 位移 `‖ΔW_eff‖/‖W_base‖` | 训练更新次数 | **每次更新的位移** | 熵（首→末） |
|---|---|---|---|---|
| **A · KL 开** | 0.1018% | **52** | **0.00196%** | 0.0519 → 0.0556 |
| **B · KL 关** | 0.1185% | **60** | **0.00198%** | 0.0591 → 0.0652 |

⇒ ★★ **每次更新的位移速率差 +0.9% —— 在噪声里。**
⛔ **口径更正（2026-08-18）**：更新次数原写 13/15，那是 `param_version` 数；
真值是 dump 文件数 **52/60**（主线 17 §7.1）。**A/B 的结论不受影响**（两臂同一个错分母，比值不变），
但两个绝对值各除以 4，已在上表改正。
⇒ **在 `kl_loss_coef=0.001` 这个设定下，KL 项实际上没有在约束任何东西。**
⇒ 熵两臂都是**微升**（0.052→0.056 / 0.059→0.065），**没有出现"关掉约束就塌"的迹象**。

### 5.1.1 这把尺子把问题**改写**了

原来的问题是「**ref 能不能不要**」（省 12.7%，但听起来很危险）。
量完位移之后，真正的问题变成：

> **`kl_loss_coef=0.001` 这个值，是不是本来就小到不起作用？**

如果是，那么现在的局面是 **付 14% 的算力，维持一个不起作用的约束** ——
那既不该保留（浪费），也不该简单删掉（**因为将来可能需要一个真起作用的约束**）。
⇒ **三条候选，判据都清楚**：

```
① 加大系数到真起作用（比如 0.01/0.05），再量位移 —— 若位移明显被拉住，说明约束是真的
   ⇒ 那时 ref 那 14% 就是**买到了东西**，讨论的应该是怎么让它更便宜（E19 的 FP8 / 抽样）
② 保持 0.001 但让 ref 变便宜（FP8 / 降频 / 抽样）      —— 折中，风险最低
③ 直接去掉                                              —— 只有在①证明"加大也没用"时才合理
```

⚠️⚠️ **成立范围（必须一起读）**：
1. **只有 13–15 次更新**。约束是**累积**的 —— 短程测不出的东西，长程可能很要紧。
2. 两臂的更新次数不同（13 vs 15），所以上面比的是**每次更新的位移**，不是总位移。
3. 位移 0.1% 仍比"正常 LoRA 微调 0.5–5%"低一个量级 —— **整个训练都还在很小的位移区间里**，
   这本身就会让"约束起不起作用"变得难测。
4. **还没测**：对基座的实际 KL（在冻结 probe 集上算）、EVAL 的 cap 构成有没有偏。

⇒ ⛔ **因此 E17 的结论从「关掉 KL 省 12.7%」改成「先回答系数该是多少」** ——
**"省 12.7%" 仍然成立，但它不再是这个实验的结论，只是一个待定价的选项。**

```

</details>
