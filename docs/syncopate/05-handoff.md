# Syncopate 交接文档

> ⛔⛔ **本文含已作废的实测数字**（2026-08-18）—— 查出两个基石级 bug：
> **三个 trainer rank 的梯度没有同步** · **trainer 的权重从没推给 rollout engine**。
> ⇒ 2026-08-14 至 08-18 之间**所有 RL 训练的实测数字都不可引用**。
> **引用之前必须先读 [`21-invalidated-numbers.md`](21-invalidated-numbers.md)** —— 那里也列了**仍然有效**的部分（SFT / 数据 / 静态代码事实 / 硬件测量）。

> 🔴🔴 **2026-08-17 换机器了 —— 先读 §0.1，很多旧数字已作废。**
>
> 更新于 **2026-08-16**（这一天只做一件：**一次独立验收审计**，见 §2.9 —— 结论是
> 「造的东西一直跑在验收前面」，并当场补齐 M7 的三个空门槛 + 一把缺失的尺子 +
> 修好 M8 验收路径上两处断点）
>
> **这份只放：现在在哪 / 下一步做什么 / 已定的决策 / 反复栽的坑。**
> 怎么搭环境、怎么跑命令、参数为什么是那个值 → **`08-machine-and-environment.md`**
> 沙盒设计 → `07-toolbox-and-runtime-design.md`　权威设计 → `syncopate-project-design-v0.1.md`
> **M9 Runtime（真服务）→ `09-runtime-handoff.md`**　**RAG/检索 → `10-rag-retrieval.md`**
> **M9 验收 / 设计符合性 → `11-runtime-acceptance.md`**（2026-08-17 审计）
> **Runtime 侧检索 → `12-rag-runtime-design.md`**（🆕 三态契约：查到 / 查不到 / **查不了**）
> **★ 数据门禁 → `13-diversity-gates.md`（D1–D11 多样性）+ `15-leakage-gates.md`（L1–L2 泄露）**
> **大版本重建前必跑**：`python scripts/check_data_gates.py --batch <批次> --split-dir <切分>`
> **训练健康度与红线 → `14-sft-health-metrics.md`**（面板已程序化建好，不用手拖）
> **★ M7-b 这一跑 → `16-m7b-rl-run.md`**（lr 被夹在两堵墙之间）
> **★★ RL 学不动的联合方案 → `17-rl-learning-blocked.md`**（2026-08-18，主线×infra 共用；
>   ESS 到底量什么 · 0.3 那条线为什么画错了对象 · **位移是输入不是产出** · 主线要修的六处。
>   infra 侧诊断在 `../infra_exp/E20-rl-not-learning.md`）
> **★★ 管线前提探针审计 → `18-pipeline-assumption-probes.md`**（2026-08-18，E21 之后的同族排查；
>   🔴「合并后的 RL 模型」其实没合并 · 那份 adapter 只是 rank_0 · 已跑 8 条探针 + 9 条待跑）
> **★★★ 2026-08-18 复盘 → `19-2026-08-18-postmortem.md`**（一天六个问题、它们的共同形状、
>   哪些结论作废、已落地的防线。**引用任何 M7-b 数字之前先读它**）
> **★ 主线任务队列 → `20-mainline-task-queue.md`**（2026-08-18；执行顺序的唯一来源，
>   每条带「谁在打」。infra 的队列在 `../infra_exp/00-INFRA-HANDOFF.md §5`）
> 多卡实验设计 → `../distributed-training-design-v0.1.md`
> **焦点是怎么定下来的 → `../focus-migration-2026-08.md`**（唯一记录迁移历史的地方）

---

## 0 · 三十秒读懂

**第一目标（真实业务）**：手游买量投放的**全链路闭环** agent。
业务价值指标是 **span of control**（一个优化师能管住的 平台×产品×地域×素材 组合数）。

**第二目标（并行，不阻塞）**：异步 agentic RL 研究（sync colocate vs fully_async）。

⚠️ **这个定位曾被搞反过**，导致给出过错误建议。别再犯。

**三条钉死的前提**（设计文档 §0）：
1. 会变的进 RAG，不变的进权重，绝不能错的进代码
2. 沙盒里只有过程奖励 ⇒ 灰度上线不是验收，是训练的第二阶段
3. 归因延迟是第一性约束（D7 才知对错，D1 数据极易被误当结论）

---

## 0.1 · 环境与管线现状（2026-08-17 验证）

**管线端到端通**：

```
data gen  v11→v12，data/splits/v12 四文件与 git SHA-256 逐字节一致（254/477/819）
data build SFT 397+80 / RL 655+164
SFT       val_loss 0.7400 → 0.1110，显存峰值 11.3 GB
merge     7.6 G 合并模型
RL        三种模式全通：colocate 1卡 67.2 s/步 · colocate 3卡 29.2 · one_step_off 22.9
          · fully_async 8 步跨 2 个同步周期
测试      365 passed, **0 skipped**（含 45 条 runtime，PG 已起）
```

**机器**：4×RTX 5090 / sm_120，2 socket EPYC 9V74（**2+2**：GPU0/1@node0、GPU2/3@node1），
PCIe Gen5 x16，P2P 全关。卡间 all-reduce busbw：组内 28.8 / 跨 socket 22.2 / 四卡 25.6 GB/s。
`/workspace` 是 300 G 本地 XFS 持久卷；`/` 只有 16 G overlay ⇒ **缓存必须重定向**，
每个 shell 先 `set -a; . /workspace/.env; set +a`（见 `08 §2.0`）。

### ★ 三个会静默毁掉训练的缺陷（都已修，判据已就位）

**① flash-attn 的判据必须包含反向。**
一个前向数值三项全过的轮子，**反向可以是全错的**：`flash_attn_func` 反向 dq/dk/dv 全 nan；
`flash_attn_varlen_func` 反向有限但**恒为 0**（verl 的 rmpad 正好走这条）。
后果：每步 `grad_norm=nan` ⇒ verl 打 `WARN: grad_norm is not finite` 并
`optimizer.zero_grad()` **跳过更新** ⇒ **RL 完全空转**。
⚠️ **恒 0 比 nan 更毒**：没有 nan、没有报错、指标好看，训练"跑完"什么都没学到。
⇒ **教训：「import 成功 ≠ 契约满足」的下一层是「前向对 ≠ 反向对」。**
⇒ 现用官方 `cu13torch2.9` 轮子 + PyPI `nvidia-cuda-runtime<=13.2`（补 `libcudart.so.13`），
反向与 fp32 参考对到 4–5 位有效数字，真实 RL 里 grad_norm 0.0147/0.0224。
⇒ **换轮子/换机器后先跑 `scripts/check_flash_attn_backward.py`。**

**② 进程启动钩子里只许做纯 Python 的事。**
`worker_process_setup_hook` 在 Ray 设 `CUDA_VISIBLE_DEVICES` **之前**跑，
若它 import 了会碰 CUDA 的东西（如 `verl.utils.fsdp_utils`），**设备枚举会被固化** ⇒
之后设 CVD 只改可见数量、不改映射 ⇒ 分卡模式下 3 个 trainer rank **全挤在 GPU0**，
第一次权重同步 OOM。修法 `verl_patches._defer_until_imported`：用 meta_path finder
延迟到 verl 自己 import 时才打补丁。

**③ `--weight-sync-bucket-mb` 对所有模式生效**（colocate 也走 NCCL checkpoint engine）。
⚠️ **默认值仍是 2048，短跑请显式传 `--weight-sync-bucket-mb 512`。**

### 🔴 本机上没有任何训练产物

换机器把 `checkpoints/` 和 `models/` 里的产物全留在了旧机器上，
**只有 `_audit/*.json` 活下来了**（它们在 git 里）：

```
checkpoints/sft      空
checkpoints/grpo     空       ← M7 的 global_step_25 不在了
models/              只剩三个基座（Qwen3-4B / 0.6B / 30B-A3B），没有 merge 后的 SFT 模型
outputs/2026-08-17/  21 个 run 全是 v12_smoke，起点全是 base Qwen3-4B
```

⇒ **凡是要 adapter 或起点模型的事，都得先重训一遍 SFT。** 这条直接决定 §2.8 的次序，
也让老基线 `_audit/v11_sft_e1_m2.json` **不能再当下一跑的对照**（§2.4）。

### 还欠着的

- **`fully_async` 下动态分池没接上**（§2.3；infra 线 **B6**，纪律是「先量出漂移 B4 再补」）
- `dynamic_bsz` 在当前机器 + FA2 下的符号**未测**（代码默认 False）
- `uv.lock` 在 .gitignore ⇒ 每台机器解析结果可能不同

## 1 · 当前状态

| 项 | 状态 |
|---|---|
| M0–M5（地基 / 数据成熟度 / 沙盒 / RAG v0 / L5 归因 / L6 扩量 / 负面数据） | ✅ 全部完成，**prompt / 工具 / 环境已冻结** |
| **M6 · SFT 冷启动** | ⚠️ **条件毕业**（§3：两条判据实测互斥）。**选 v11-e1** |
| **M7 · RL 正式训练** | ✅ **跑完并验收**（150 步 / 3h09m / 零错误）。⚠️ **结论是「没测出差异」**，见 §2.1 |
| 环境 | ✅ 环境、数据、SFT、评测、RL 三模式全部实跑验证（§0.1） |
| 异步 RL · `one_step_off` | ✅ 跑通并调优（稳态 49.5 s/步；FA2+打包后 **32.6 s/步**） |
| 异步 RL · `fully_async` | ✅ **2026-08-14 打通**（翻了四堵墙，见 §2.2），稳态 ~76 s/步 |
| 动态分池 | ✅ `one_step_off` 下生效；⚠️ `fully_async` 下**没接上**（可修，见 §2.3） |
| **M8 · RAG v1** | ✅ **施工完成**（2026-08-14）。⬜ **验收未做**，但 2026-08-16 已修好两处挡路的（§2.6）。入口 **`10-rag-retrieval.md`** |
| **M9 · Runtime 上线态** | ✅ M9.1–M9.6 施工完成，⬜ M9.7 压测未做。🔴 **2026-08-17 符合性审计：40 条判据 ✅20/🟡7/⛔11/⬜2，五个任务**（C 档动作不走审批 · 审批通过后 run 永久停住 · 并发重复调用返回空结果 · 四个降级信号没有生产者 · 沙盒写工具 runtime 只登记了一半）。§19 六条空门槛当天填完。入口 **`11-runtime-acceptance.md`**，怎么起看 `09` |

⚠️ **2026-08-16 做了一次独立验收审计，结论比上面这张表难看**：
真正过了验收的只到 M5；M6 是**降标准放行**（6 条过 3 条）；
**M7 名义"已验收"，但 §31.2 六条里有 3 条的门槛是空的、1 条的尺子根本不存在** ——
详见 §2.9。三个【待定】和那把缺失的尺子当天已补齐。

```
数据 v11   1370 条 · 17 模板 · 90 格子 · 28 工具 · 32 cap · 5 种行为
三桶       EVAL 198 / SFT 434 / RL 738（train 590 + val 148），内容级零重叠实测
测试       250 全过（0 skipped）
SFT        v11 e1/e2 已重训 + 重评 ⇒ **选 e1**
M7 产物    🔴 **已不在本机**（换机器时留在了旧机器上，见 §0.1）——
           曾是 checkpoints/grpo/m7_v11e1/global_step_25（param_version 命名 = global step 100）
           + models/Qwen3-4B-rl-m7-s100/。**留下来的只有** wandb run `m7_v11e1` / u6205j1n
           和 `_audit/v11_rl_m7_s100.json`（在 git 里，结论仍可引用，模型不可复用）
```

⚠️ **当时最终 ckpt（step 150）就丢过一次**：收尾 27 GB 写盘撞上**旧机器**的卷配额被静默截断
（`df` 看不到配额）。可用的是第 100 步那个 —— 好在 **100 步正好训完整整一遍**
（590/590 条 RL prompt 全覆盖，平均每题 1.02 组；150 步是 1.53 遍），当结论点反而更干净。
⚠️ **当前机器没有这个配额**（本地 XFS 300 G，`workspace_is_volume=true`，权限位生效），
但 §6-④ 那条判据不变：**判断空间要用写入探针，不能信 `df`。**

★ **M6、M7 是考试不是施工**，跑在冻结的东西上。
⚠️ **当前数据版本是 v13**（2026-08-17）：**1670 条 / 106 格 / 30 工具 / 35 cap / 21 模板**，
三桶 EVAL 278 / SFT 511 / RL 881。
v13 = v12 + 检索契约的两个新局面（`policy_outage` 查不了 · `policy_misread` 答非所问），
详见 **`12-rag-runtime-design.md` §7**。
★ **存量 1550 条逐字节未变、冻结 EVAL 老 id 丢失 0** —— 构造保证的，已实测。

---

## 2 · ★ 下一步该做什么

### 2.1 ★★★ M7 的结论：模型只动了 **0.0093%**，所以什么都没测出

```
配对比较（冻结 EVAL 198 条，对照 _audit/v11_sft_e1_m2.json）
  均值           0.803 → 0.814
  配对差值        +0.011      最小可检出差异 0.013   ⇒ 「没测出差异」
  逐题            赢 73 / 平 69 / 输 56
  决策位熵        0.0270 → 0.0724（P5 通过，探索没死）
  有梯度格子      120 → 116（略退）
```

⚠️ **配对 MDE 是 0.013**；0.048 是**不配对**的。别引用错，差近四倍。

**根因不是"RL 推不动"，是位移被 lr 锁死**（直接量的）：

```
||ΔW|| / ||W|| = 0.0093%     ← 正常 LoRA 微调是 0.5%–5%，小了两三个数量级
100 次更新 × lr 1e-6 = 1e-4，实测 9.3e-5 —— 几乎正好贴着这个上界
```

★ **GRPO + AdamW 下 reward 只决定方向，不决定距离**：advantage 组内归一化 ⇒
"好很多"和"好一点"梯度幅度一样；AdamW 每步位移 ≈ lr，与梯度大小无关。
⇒ **位移 ≈ lr × 步数 × 方向一致性，reward 完全不参与。**

★ **但方向对、而且模型极敏感**：0.0093% 的位移下 `false_claim_cap` 就掉了 19%
（130→105）、`missing_safety_line_cap` −11；代价是 `unauthorized_write` +3、
`unconfirmed_irreversible` +2，以及 `cross_region_generalization` / `duplicate_write`
两类**基线上为 0、RL 后新长出来的违规**。⇒ 杠杆在，**放大 lr 会同时放大好坏两个方向。**

**⇒ 下一跑只改一个变量：`--lr 1e-6 → 1e-5`。** 理由：步数是钱（150 步 = 3.2 h）、
lr 免费；`grad_norm` 整跑 0.011–0.06 稳得发闷，稳定性有余量。不稳就退 5e-6。
停止条件今天已全部实跑验证（见 §2.4）。

### 2.1b ★★★ M7-b（2026-08-17）：lr 被夹在两堵墙之间

M7 第一跑的结论是「没测出差异」，根因是位移只有 0.0093%。这一跑只改 lr。

```
lr 3e-5   ESS/N 0.85 → 0.26，**第 75 步跌破 0.3 触发停机**，无 ckpt
lr 1e-5   ESS/N 全程 0.34–0.91，110 步跑完
          位移 0.0487%（SFT 一遍是 0.4985%，差 10 倍）
          配对差值 +0.020，而配对 MDE 也正好 0.020 ⇒ **仍是「没测出差异」**
```

★★★ **真正的产出不是模型，是一条被夹住的区间**：

> **lr 大到能推动模型时，异步的 ESS 就塌了；lr 小到 ESS 健康时，模型推不动。**

★ 这正是 §00 那个研究问题的 H2：`ESS/N ≈ exp(−T·σ²(k))`，而 lr 调的就是
"每步移动多少" ⇒ **推动模型和压低 ESS 是同一个旋钮**。
⚠️ 而它**在同步 colocate 下不存在**（k=0）⇒ **下一跑就该跑 colocate + 3e-5 做对照**，
那会是"异步的代价"第一次被正面量出来。全文见 **`16-m7b-rl-run.md`**。

⚠️ 护栏侧一条要记：位移只有 0.0487%，`unauthorized_write_cap` 就从 91 涨到 **128**。
和 M7 那次同一形状 —— **模型对 RL 方向极敏感，reward 只决定方向不决定距离。**

✅ 动态分池**第一次真的接上了**（判据行来自 `FullyAsyncRollouter` 那个进程）。
✅ v13 新增的 `retrieval_unavailable_cap` 在训练中真的被打中（末步 4%），不是摆设。

### 2.2 fully_async 翻过的四堵墙（都已修，有测试）

| 墙 | 性质 | 修法 |
|---|---|---|
| `min/max_global_steps` 缺失 ⇒ `None-None` TypeError | **我们的** AgentLoop 丢了 `extra_fields` | `verl_agent_loop.py` 照 `tool_agent_loop.py:248-254` 聚合；判据 `[agent-loop] 策略版本字段 ✓` |
| `save_model_to_cpu` 断言要 DTensor | 上游假定分片 × 本机必须 DDP | `verl_patches` P2，只在**无 DTensor** 时接管；4 条数值往返测试 |
| 补丁打在 driver、断言在 worker | **作用域** | `runtime_env.worker_process_setup_hook` |
| 第 8 步权重同步 OOM（差 0.09 GB） | bucket 住 rollout 卡 | `--weight-sync-bucket-mb 1024 --rollout-gpu-util 0.70` |

**跑通后的第一批真机异步数据**（这才是这一跑真正的收获）：

```
ESS/N            0.74–0.88，整跑稳定     ← 单卡离线合成预测 0.846，落在区间内 ✅
陈旧轨迹          576 / 7200 = 8.0%
partial_ratio    0.0                    ← 没有任何轨迹跨越版本边界
rollouter 空闲率  81.0%                  ← ★ 1 rollout : 3 trainer 严重过配
param_sync       58–103 s（占步长 ~19%） ← 与 one_step_off 的 update_weights 11–13 s 不是一回事
```

⇒ **`partial_ratio=0` 解释了漂移为什么是 0**：轨迹太短（4.5 工具步、~700 token）
相对同步周期（4 步 ≈ 300 s），根本碰不到边界。想让这条研究线有内容，
要么调大 `--latency-scale`，要么缩短同步周期。
⚠️ 另注：`record_dispatch` 记在轨迹**末尾** ⇒ "下发"实为"跑完"，
"尝试过但从未跑完"的轨迹这把尺子看不见。

### 2.3 已知欠着的三件

| 事 | 为什么 | 成本 |
|---|---|---|
| **动态分池接进 fully_async** | `fully_async_rollouter.py:464` **确实调** `create_rl_sampler`（import 在函数体内，最好 patch）。没生效是**作用域**：rollouter 在另一个 worker 进程。⇒ 把 sampler patch 也装进 `verl_patches.setup_worker()` | 几行 |
| **colocate 同机基线** | 所有加速比仍无同机分母 | 约 30 分钟 |
| **卡配比实验** | 空闲率 81% ⇒ 2+2 / 1+3 都该试 | 一次冒烟 |

### 2.4 跑完怎么查（`06-rl-run-protocol.md`）

```bash
python -m syncopate.train.rl_report  checkpoints/grpo/m7_v11e1      # cap/耗时/漂移，补报 wandb
python -m syncopate.train.eval_local --adapter <ckpt> ...           # 冻结 EVAL 重评
python -m syncopate.train.compare    _audit/v11_sft_e1_m2.json _audit/<新审计>.json
python -m syncopate.train.entropy    --adapter <ckpt> --limit 24
```

🔴 **对照基线要重出，老的那份不能用了。** `_audit/v11_sft_e1_m2.json` 对应的 SFT 模型
已随换机器丢失（§0.1），而配对比较要求**同一起点模型 + 同一推理引擎**跑出来的审计 ——
引擎决定采样内核，起点决定被比较的是什么。
⇒ **新基线由 M8 验收那一步顺带产出**（v12 重训 SFT → 冻结 EVAL 重评），见 §2.8。
⚠️ 老那份仍然有效，但只对 **M7 那一跑**有效，是历史记录不是下一跑的对照。

⚠️ **`compare` 会自己打印最小可检出差异，不要绕过它读均值。配对 MDE = 0.013**
（不配对是 0.048 —— **别引用错，差近四倍**）。

**停止条件（优先级高于分数曲线）**：ESS/N < 0.3 / 决策位熵 < 0.05 /
reward 涨但 cap 不降（连续 3 步）/ grad_norm 跳两个数量级。

★ **2026-08-14 实跑验证过四条判据都拿得到**：ESS 看
`rollout_corr/rollout_is_eff_sample_size`（键名不叫 ess）、grad_norm 和长度在日志、
**cap-vs-reward 控制台看不到**（verl 的 `compute_data_metrics` 只认两个字段），
要按步段聚合 `rollout_dumps/*.jsonl` 里的 32 个 `cap/*` 字段 —— 不必等跑完的 `rl_report`。

★ **ckpt 合并要先补 `actor/huggingface/`**（fully_async 不写它，merger 又要它）：
从起点模型拷 config/tokenizer 九件套，**逐字节校验大小**，然后
`python -m verl.model_merger merge --backend fsdp --local_dir <ckpt>/actor --target_dir <out>`。

### 2.6 ★★ M8 · RAG v1（2026-08-14 施工完成，验收未做）

**做了什么**：补上 RAG 的半结构化侧（`policy.search`）和非结构化侧
（`insight.search_claims`），并把设计文档 §14 的两项验收**做成 cap** ——
在此之前「过期检出率」和「无检索幻觉率」只是文档里的两个词，进不了 reward。

```
工具 28 → 30    cap 32 → 34    模板 17 → 19    格子 90 → 102
数据 v12: 1550 条（gold 逐条实跑）· 三桶 EVAL 222 / SFT 265 / RL 1063 · 互斥 SHA-256 全 0
新增两条 cap:  cited_expired_policy_cap(0.10) / no_retrieval_hallucination_cap(0.20)
新增两个模板:  policy_drill(POL) / insight_conflict(CONF)，各三档含**对照档**
```

沙盒侧的设计（为什么不用真向量库、打分函数怎么选的、词汇失配这条局限）
→ **`07-toolbox-and-runtime-design.md` §1.1**，不在这里重复。

**★ 基线可比性：v11 的 1370 条输入逐字节相同、0 条变化。**
所以 `_audit/v11_sft_e1_m2.json` 对它们**仍然完全可比**，M8 的代价被限制在新增的 180 条上。
这是 `--freeze-from` **构造保证**的，不是事后 diff 出来的 —— 见 §6-⑤。

**验收还差什么**：在 v12 上重训 SFT → 冻结 EVAL 重评 → 量两项指标。
那是「M6 那种考试」，不是施工。两项都按硬判据（趋近 0）；
Recall@k 和引用准确性设计文档标的是【待定】，先当观测量不设门槛。

**★★ 2026-08-16：验收路径上原本有两处断掉的，已修**（详见 `10-rag-retrieval.md` §6）：

```
① SFT 桶里 POL / CONF 各 0 条   ⇒ 重训后模型对 M8 的两个新模板一无所知，
                                  冻结 EVAL 上必然量出"没学会" —— 而这个结论
                                  **和 RAG 实现好坏无关**，会把分桶 bug 误判成 RAG 问题
② 两项验收的分母各只有 4 条      ⇒ 错一题 25%，判不了"趋近 0"
```

修完：POL/CONF 在 SFT 桶各 16 条、19 个模板全部 >0；两项验收的分母 4 → 12；
冻结 EVAL 222 → 254 **且老 id 丢失 0 条**（严格增量，构造保证，见 §6-⑥）。
代价：**SFT 桶 265 → 477、RL 桶 1063 → 819**，重训 SFT 的时间大约翻倍。

⇒ **前置条件已就绪，M8 验收可以开跑。**

### 2.9 ★★★ 2026-08-16 验收审计：造的东西一直跑在验收前面

把每个里程碑**当初写死的标准**翻出来，逐条对实际证据：

```
真·通过验收        M0 M1 M2 M3 M4（M5 一半）
降标准放行         M6              6 条过 3 条（零梯度 35.9% / 多样性 58.1% 两条实测不达标）
名义验收/实为未定   M7              见下
造完没考试         M8 M9           验收 0%
```

**M7 的六条毕业条件，逐条查完是这样**：

| 条件 | 实情 |
|---|---|
| 高危 cap 命中 = 0 | ✅ 真过（cross_account / competitor_action 基线与 RL 后**都是 0**） |
| premature_decision < 【待定】 | 🟡 实测命中 0，但**门槛从来没填过** |
| 目标意图 reward ≥ 【待定】 | 🔴 **门槛空着**，无法判定 |
| 熵未塌 | ✅ 0.0270 → 0.0724 |
| E2E 成功率**按读/写分桶** | 🔴 **这把尺子当时不存在** |
| D1 通用能力未退化 | ⬜ 无域外数据，测不了 |

★ 反 hacking 指纹这块是干净的（实测）：多样性 58.1%→58.6% 没塌、
`max_steps` 80→75 在降不在升（不是摆烂）、cap 没有骤降到 0。
但 §31.3 要求的「**每轮 RL 后人工翻 20 条**」在任何地方都没有记录。

**当天补齐的三件**（门槛写进设计文档 §31.2）：

1. `premature_decision 命中率 < 2%`（基线实测 0/198，留一条题余量）
2. 目标意图 `reward mean ≥ 基线 + 0.013`（★ **0.013 是配对 MDE** ——
   低于它的"提升"尺子分辨不出来，写进门槛等于允许拿噪声毕业）
3. **补上读/写分桶尺子**（`eval_local._report_read_write`），门槛用实测反填

★★★ **这把尺子补上后第一次量，就看见了一直被盖住的东西**：

```
                  n     reward   满分成功率   cap/次
只读             86     0.909       69%       0.08
含写动作        112     0.722    ★ 20%        0.40      ← cap 命中是读的 5 倍
读−写 差值                +0.187
```

> **M7 那个「平均 reward 0.803」是被读操作撑起来的。真正花钱的那一桶，
> 成功率只有 20%。** 这正是设计文档 §21「大量读操作会稀释掉写操作的风险」的实物证据 ——
> 而它之所以一直没被看见，不是没人想到，是**没有尺子**。

M7 第一跑对照新门槛：写桶成功率 20.3%→20.3%（**没动**）、cap/次 0.40→0.36、
差值 0.187→0.161。**方向对、幅度不够**，和「位移只有 0.0093%」完全一致。

⚠️ **一条留给下一个窗口的判据**：`--from-audit` 可以对历史审计重出报告
（`python -m syncopate.train.eval_local --from-audit <审计> --batch <当初那个 batch>`）——
**补了新尺子不必重跑几小时 GPU**。但补分组依据时必须用**这份审计当初跑的那个 batch**，
拿另一个版本的 gold 去分组，得到的数看起来正常，但它谁也不是。

### 2.7 ★ M9 · Runtime 上线态（2026-08-14 施工完成，压测未做）

M9.1–M9.6 全部完成（8+1 张表 / 三层幂等 / API+越权 / Tool Runtime+Worker /
审批网关六触发器 / SSE+观测+成本闸），**45 条测试**。
**M9.7 压测五场景未做 —— 那是最终的考试。**

⇒ 怎么起、施工时抓到的六个 bug 在 **`09-runtime-handoff.md`**；
**验收结论在 `11-runtime-acceptance.md`**（🔴 五个确认缺口，压测前必须先处理 §5 那两件）。
三条要记住的：
- **幂等是唯一一个"错了就是真金白银"的东西**，判据必须是实测重复投递
- **沙盒是 runtime 的子集，且契约由 runtime 定义**（见 §4-★）
- **PGDATA 现在就放在 `/workspace`**（本机是 XFS、`chmod 700` 生效；旧机器的 mfs 不支持
  权限位才放不进）。数据库仍然是**派生产物**，`bash scripts/pg_bootstrap.sh` 一条命令重建

### 2.8 之后（2026-08-17 更新）

**按优先级**：

1. **★ colocate + lr 3e-5 对照** —— 同步下没有 staleness（k=0），如果它吃得下 3e-5
   而 fully_async 吃不下，**异步的代价就第一次被量出来了**，正面回答研究问题。一跑。
2. **缩短同步周期 / 调大 `--latency-scale`** —— 两跑下来 `partial_ratio` 都是 **0**，
   漂移这条线至今是空的（轨迹 ~500 token vs 同步周期 4 步，碰不到版本边界）。
3. **lr 2e-5 补中间点**，把等值线画出来（现在只有 1e-5 通过 / 3e-5 破裂两点）。
4. **用步数换位移**：位移 ≈ lr × 步数。1e-5 × 330 步（三遍）≈ 0.15%，比硬提 lr 安全。
5. **★ 先查 `allowed_write_tools` 是不是造窄了**（`16 §2.6`，不吃 GPU）——
   越权写榜第一是 **`approval.create_case`（开审批单）**，而那是我们自己设计的逃生口
   （`09 §3`：网关的输出是暂停，不是拒绝）。若 case 白名单里没有它，
   **模型"交给人"反而被罚到 0.30 封顶**，和 `abandoned_without_escalation_cap` 直接打架。
   ⚠️ **这条要排在"加大护栏惩罚"之前**，否则会把"遇事别交接"训进去。
   判据：那 69 条命中的 case，gold 轨迹里到底允不允许开单？改白名单 = 一次数据版本变更（→ v14）。
6. **M9.7 压测**（不吃 GPU，随时可开）+ M9 审计里还欠的 4 项（见 `11 §4`）。
7. **沙盒欠的第二样：误召回**（`12 §7`）—— M8 验收做完再动，会推到 v14。

#### 旧的排队表

三件在排队。⚠️ **前两件不是平行的，是有序的**（换机器后才成立的约束，见 §0.1）：

| 次序 | 事 | 内容 | 成本 |
|---|---|---|---|
| **①** | **M8 验收** | v12 上重训 SFT → merge → 冻结 EVAL 重评 → 量过期检出率/无检索幻觉率 | 几小时 GPU |
| **②** | **M7 第二跑** | 只改一个变量 `--lr 1e-6 → 1e-5`（§2.1） | 约 3 小时 |
| **并行** | **M9.7 压测** | 五场景，**不需要 GPU**，随时能开始 | — |

★ **为什么 ① 必须在 ② 前面**：本机没有任何 SFT 产物 ⇒ ② 连起点模型都没有；
而 ① 本来就包含「v12 重训 SFT」，**它产出的那份冻结 EVAL 审计正好就是 ② 的新基线**。
反过来先跑 ②，既没起点、拿到的数也没有合法对照（§2.4）。
⇒ 一次重训同时服务两件事，**别拆成两次**。

多卡实验线（异步三模式对照 / 并行策略 / 前缀缓存分片）见 `../distributed-training-design-v0.1.md`。

---

## 3 · ★★ M6 的结论：两条毕业条件在我们的规模上**互斥**

四轮实测（v10-e1/e2、v11-e1/e2），**没有任何一个点**能同时满足「零梯度 <30%」和「采样多样性 ≥70%」：

```
              v11-e1   v11-e2   门槛
格式合规       99.9%    99.9%   ≥99%   ✅
行为分类       98.4%    99.6%   ≥90%   ✅
零梯度占比     39.4%    63.1%   <30%   ❌
采样多样性       58%      40%   ≥70%   ❌
平均 reward    0.803    0.872
```

★ **修好正确性 = 压低多样性。** v11 修了 `defer` 保底和 GEO 之后，零梯度反而上升 —— **是被自己的修复推上去的**。

⇒ **决定（Chaoyu）**：设计文档写在意图和工具都少得多的时候，**只是参考不是规矩**。零梯度和正确性本来就是权衡点，**不能为了梯度放弃正确性**；而「多样性 ≥70%」对 `CHAT`（本来就不该调工具）这类题型**天然不适用**。⇒ 记为**条件毕业**，让 M7 的实测说话。

**选 e1 不选 e2**（按手册 §20：看熵和有梯度格子，**不看 reward**）：

```
有梯度格子   e1  120/198     e2   73/198
饱和格子     e1   67/198     e2  105/198   ← e2 已经训过头
```

e2 的 reward 更高，但**高在"饱和"上** —— 105 个格子组内方差归零，GRPO 在这些题上梯度精确等于零。**分数更好看，但 RL 训不动。**

⚠️ 两条**无法测量**的，别再花时间找办法：
- 「通用能力下降 ≤3%」需要域外测试集，整个项目没有任何域外数据。硬凑会给出虚假的安全感。
- e1 vs e2 的选择不看 reward。

---

## 4 · 已确定的决策（别再重新讨论）

| 决策 | 结论 | 依据 |
|---|---|---|
| SFT 混不混通用数据 | **第一版不混** | 手册 §3.1/§18.1/§18.4：LoRA 已交了参数空间的保险费 |
| RL 用不用 LoRA | **必须用** | 4B 全参优化器状态 48GB > 可用显存 |
| reference 指向谁 | **SFT ckpt**（靠 merge 实现） | 手册 §24③ |
| ckpt 怎么选 | **不选 val loss 最低的**，选决策位熵高、有梯度格子多的 | 手册 §20 |
| 沙盒要不要比真实世界友好 | **不要** | 字段名不带单位、返回不含新值、无幂等保护，都如实建模 |
| 「不照做」够不够 | **不够，必须显式标记** | 只测单向，消极的模型能骗过指标 |
| 训练入口 | **只有两个，不许再写第三个** | 临时脚本会静默地和主路径长出差异，栽过两次 |
| wandb | **默认开**，要关得显式 | 「训练脚本的默认值必须是跑完就有记录」 |
| **并行策略（4 卡）** | **仍然只用 DDP**（`fsdp_size=1`），但**理由的强度降级了** | 旧机器上「不是慢一点，是不能用」的依据是卡间只有 6.4 GB/s；当前机器**组内 28.8 GB/s**，那句话**没有当前数据支撑** ⇒ 结论按「暂按 DDP，等 A6/E04 重测」执行，别再引用「慢 6 倍」（§5） |
| **staleness 修正模式** | **decoupled**（`bypass_mode=False`） | 只有它产出 ESS 指标，而停止条件 P6 依赖 ESS —— **没有刹车不能长跑** |

---

## 5 · 多卡的边界：为什么只能 DDP

**分卡是异步的硬前提**：verl 的两条异步路径都要求 rollout 和 training 在不同 GPU
（`one_step_off_policy/ray_trainer.py:89` 的 `assert not self.hybrid_engine`）。

**异步的收益**（one_step_off，稳态）：

```
step 1 的 gen   89.6 s      ← 现场生成
step 2 的 gen    4.1 s      ← 上一步训练时就已经生成好了
```

⇒ **异步把瓶颈从 rollout 搬到了训练侧**（`update_actor` 占大头）。

### 训练侧并行：`--fsdp-size 1`（DDP）是必选项

**5090 没有 PCIe P2P**（NVIDIA 从 4090 起在驱动里关掉，**所有 4×5090 机器都这样**），
卡间一律经主机内存中转（NCCL 走 `SHM/direct/direct`）。而 FSDP FULL_SHARD
每层前向反向都要 all-gather 全部权重 —— **通信量比计算量还大**；
LoRA 的 DDP 每步只同步 **260 MB**（当前机器约 10 ms）。

⇒ 实测三卡 DDP：colocate 29.2 s/步、one_step_off 22.9 s/步（单卡 colocate 67.2 s）。
⚠️ **FULL_SHARD 与 ZeRO-2 两档在当前机器上尚无稳态数据**，队列 **A6** 负责补。

⇒ **「并行」不该发生在模型内部，该发生在模型外部**（rollout 副本 / 时间上的异步 /
实验级并行）——**这三种通信量都极小。**

★ **顺带**：PP 对慢网络友好（只传层边界的激活值，约 26 MB），但 verl 的
megatron/torchtitan 后端一个都没装，目前用不了。

---

## 6 · ★★★ 反复撞到的同一个形状：机制在，但没接上

**我建了一个机制，然后假设它会自动生效。** 2026-08-13 这一天又撞了好几次：

```
数据/训练侧（旧）
1. cap 监视的工具不在菜单里     ⇒ 护栏永远不可能命中
2. 新模板的状态没进 outcome tag ⇒ 三档全塌成一个格子
3. 稀疏格子靠 index 取模控制    ⇒ 实测被削成 0 条
4. 规则写在工具说明第二行       ⇒ 被淹没，12 条 EVAL 全崩
5. 池子的短任务信号被地板夹住   ⇒ 结构上不可能影响输出

多卡/异步侧（新）
6. disable_log_stats 传对了但 verl 把日志级别硬编码成 WARN ⇒ 统计一行看不到
7. 融合 kernel 和 remove_padding 必须一起开，只开一个等于没开
8. 动态分池的补丁对 one_step_off 无效（它 from ... import，名字导入时就绑死了）
9. **staleness 修正在 bypass 模式下不产出 ESS 指标** ⇒ 长跑等于没有刹车
```

⇒ **「写下来」和「起作用」之间总有一道没人负责的缝。测试抓不到**（它测的是机制本身
对不对，不是机制有没有被接上），只能靠实测暴露。

**配套纪律**：
- **判据永远是日志**：`[pool] 动态分池启用` / `[rl] 模式=...` / `[verl-patch] ...` —— **没有这行就是没生效**。
- **硬失败，不自动降级**：`--mode` 在卡数不够时直接报错。静默退回单卡的表现是「跑起来了、但测的根本不是异步」。
- **需要精确控制的量，别靠取模碰运气。**

### ★★ 2026-08-14：三个比"忘了接"更隐蔽的变种

**① 判据行打出来了 ≠ 补丁在需要它的进程里生效 —— 作用域。**
P2 补丁打在 driver，`[verl-patch]` 照常打印，断言却在 `WorkerDict` 这个 Ray actor
进程里触发。⇒ 判据要连**哪个 pid 打的**一起看。修法：`worker_process_setup_hook`。

**② 判据行不能写断言，只能写观测。**
`main_ppo_pool` 打的是「`fully_async` 不调 `create_rl_sampler` ⇒ 本轮不生效」。
**这句话本身是错的** —— 它调了（`fully_async_rollouter.py:464`，import 还在函数体内）。
真因仍是作用域。⇒ 一行陈述"为什么不行"的日志，**把"其实能行、只是没接上"整个盖住**，
而且它长得像个合格判据。⇒ 说「上游不支持 X」之前查三层：
**配置项在不在 / 代码路径调不调 / 它在哪个进程里跑。**

**③ 短冒烟证明不了长跑的稳定性 —— 时间维度。**
`--sync-every 4` ⇒ 首次同步在第 4 步、冒烟只跑 3 步；而真正炸的是**第二次**同步
（第 8 步，差 0.09 GB）—— rollout 卡可用显存随生成推进变紧。
⇒ **冒烟至少覆盖两个同步周期。**

**④ 查得到的指标 ≠ 量对了东西。** `df` 报 732 T 可用，掐死我们的是**卷配额**
（`df` 看不到，超限还是静默的）⇒ 最终 ckpt 被写一半掐断，训练日志毫无提示。
⇒ **判断空间要用写入探针，不能信 `df`。**
⚠️ **当前机器没有那个配额了**（本地 XFS 300 G，`df` 说的是真的）——
但这条留着不是为了这台机器，是为了**下一台**：判据是「量的是不是掐死我的那个东西」，
不是「这台机器有没有配额」。

**⑤ ★★ 增量里程碑必须 `--freeze-from`，否则"只加题不改老题"是假的。**

M8 只想加两个模板，结果重算工具菜单**动了 1030/1370 条存量 case 的 prompt**。
根因：菜单 = gold 并集 ∪ **SFT 审计里模型实际调过的工具** ∪ **CORE（全局统计）**，
后两项都不是本地的 —— 换一份 `--sft-audit`、或者只是**多了两个模板**，
都可能让某个工具掉出 CORE，从而改掉一堆和本次改动无关的模板。

⚠️ 而我当时的判断是「`tool_menu` 是逐 case 存的，所以只有用得上新工具的 case 会变，
其余基线仍可比」。**那个判断是错的** —— 逐 case 存 ≠ 逐 case 算。

⇒ `scripts/set_tool_menus.py --freeze-from <上一版 batch>`：存量模板**逐字节沿用**旧菜单，
只给新模板现算。修完实测 1370 条输入逐字节相同、0 条变化。
**"哪些基线仍可比"从此是构造保证的，不是事后 diff 出来的。**

★ 一般化：**凡是有全局统计参与的产物（CORE、词表、阈值、分位数），
增量重建时都要问一句"我这次改动会不会顺手改掉不相关的部分"。**

### ★★★ 2026-08-16：⑥ 保底写在了**另一条代码分支**里

M8 新增的 POL / CONF 两个模板，**SFT 桶里各 0 条**。

根因不是"选得不好"：`split.py` 的稀有行为保底**只写在 `dead_grid` 分支里**，
而 v12 走的是 `difficulty_proxy` 分支（v11 走 dead_grid，所以以前从没暴露）。
后者按 (难度, gold 链长) 降序取前 20%，POL 的 gold 只有「一次检索 + 终答」，
链最短 ⇒ **整个模板被挤出桶外**。

⚠️ 最刺的地方：**保底机制早就写好了，代码注释里连"为什么需要它"都写清了**
（「2026-08-13 实测：defer 只有 4 条 ⇒ SFT 后从 77% 崩到 36%」）——
只是它待在另一条 if 分支里。**换了条路，保护就没了。**

⇒ 这是「机制在但没接上」的**第三种形态**：不是忘了接（①），不是作用域（②），
不是时间维度（③），是 —— **同一件事有两条实现路径，只在其中一条上做了保护。**
⇒ 判据：**凡是"保护性"的逻辑，先问它是不是所有路径共用一份。**
修法就是把它提成一个函数，两条分支都调。

★ 但共用的是**函数**，不是**策略**：在场保底只对 `difficulty_proxy` 开 ——
判据是「**说不说得出为什么这个模板不需要 SFT**」，dead_grid 说得出
（它在 EVAL 上是活的），difficulty_proxy 一个理由都说不出。
第一版两条都开，当场撞坏一条既有测试（活格被硬拽进 SFT 桶）。

★★ **还有一条：保护性逻辑的"上限"不能一刀切。**
我给保底统一加了「最多吃一半」的上限，**直接把 `defer` 从 12 削到 8** ——
那正是当年 defer 77%→36% 的那条线。⇒ 分成两类：
**要饱和的**（行为，学不会就是学不会，不设上限）vs **只要在场的**（模板/结局，
最多一半，给 RL 留口粮）。**一个保护参数同时服务两个目的时，先问它们要的是不是一回事。**

---

## 7 · ★★ 方法论：三条用代价换来的

### 7.1 用推理代替测量，会付两次钱

2026-08-13 一天里犯了两次同一个错，**两次都是从一个正确的观察推出一个错误的修法，而且代码都改完了才去测**：

| 正确的观察 | 推出的修法 | 实测 |
|---|---|---|
| P2P 全关 | 设 `NCCL_P2P_DISABLE=1` | ❌ **完全无效**。真根因是 P2P 缺失 **×** Ray 只给每个 worker 开放一张卡 |
| 序列才 4k token，喂不饱 5090 | 开 `use_dynamic_bsz` 打包成 16k | ❌ **慢 2.2×**。因为我们的 flash-attn 是垫片，打包后注意力退化成 O(总长²) |

⇒ **把条件拆开，一次只变一个，让实测告诉你哪个是开关。** 四行探针脚本、两分钟，抵得上几轮"改一改再跑跑看"。
⇒ **凡是自己推出来的修法，改代码之前先想想能不能用五分钟证伪它。**

### 7.2 一次只变一个变量

调优第一轮我同时改了 DDP 和 dynamic_bsz，结果 1.36× 的收益**分不出是谁的**。
补了 2×2 对照才发现：一个 ÷3.0、一个 ×2.2，**方向相反**。差点把一个倒退当成收益收下。

### 7.3 「够用」这种结论要带范围

「flash_attn 垫片就够了」—— **对正确性够用，对序列打包不够用**。
结论写下来的时候，把**它在什么前提下成立**一起写下来。

> **后记（2026-08-13 晚，读码 + CPU 实验查实）**：机制比"垫片是空壳"还深一层——
> 垫片从不参与 attention，真正的因果是它逼我们用 sdpa，而 verl rmpad 的
> `attention_mask=None`+打包 position_ids 约定是为 varlen kernel 设计的，落到
> sdpa 上 transformers 4.57 恒走「物化 [1,1,L,L] mask」路径：打包时跨序列白算
> ~3.2× + 256MB mask + SDPA 丢 fused 后端，三笔=2.2×。附带两发现：
> ① 正确性没出事纯靠 transformers 版本新（4.5x 才有 packed 检测，旧版会静默泄漏）；
> ② `find_packed_sequence_indices` 永不返回 None ⇒ **不打包的单序列基线同样在
> 慢路径上**。⇒ 教训再加一条：**垫片满足了 import，不等于满足了契约**——
> 那套调用约定是按 kernel 的语义设计的，接住符号不等于接住语义。
> （真 flash-attn 已装 —— ⚠️ 但**装上不等于能用**，反向坏掉的轮子会让 RL 静默空转，
> 见 **§0.1-①**（不是 §2.2，那节讲的是 fully_async 的四堵墙）；dynamic_bsz 仍待重测。）

---

## 8 · 踩过的坑（都有测试守卫，别重蹈）

**数据与评测**
1. **SFT 标签 bug**：默认值没被覆盖 ⇒ 监督目标是错的，而 val_loss 降到 0.0000。
   **loss 降到 0 只说明学到了标签，不说明标签是对的。**
2. **三桶切好了但数据构造没读它** ⇒ 冻结 EVAL 一直在训练数据里。**切分文件和训练数据是两件事。**
3. **prompt 被截断** ⇒ 训练和评测跑在不同输入分布上。
4. **prompt 内容取决于 dict 插入顺序** ⇒ 去重守卫和泄漏检测跑在两个空间。修法：`context | dictsort`。
5. **`literal:false` 被判成「期望 True」**（`bool("false")` 是 True）。
6. **只装死格的 SFT 桶** ⇒ `defer` 从 97% 掉到 0%。**必须掺对照档。**
   第二种形态：`add_controls` 的两道闸把**四个模板整个挡在桶外** ——
   「只喂难例」不止是"某个意图内只给一档"，还包括"整个意图没进桶"。

**沙盒**

7. **单位混用**：只改 `daily_budget` 没改 `monthly_cap`，上限从 75000 掐到 1400。
8. **`system.wait` 自己扣配额** ⇒ 额度耗尽时连等待都调不动，唯一出路被自己堵死。
9. **超时不消耗墙钟** ⇒ 超时在吞吐指标上免费，异步收益被系统性低估。
10. **判据把标准答案判错**：写「什么算错」之前，**先把「什么算对」的轨迹拿来过一遍判据**。
    `behavior` 只能区分「答/不答」，区分不了「答了什么」。
11. **顺风局假设**：`verify_gold` / `false_claim_cap` 写在"任何工具报错=世界坏了"的年代。
    原则改成 **"一个错误只有在世界没为它给出理由时才算 bug"**（`VerifierSpec.expected_tool_errors`）。
12. **跨轴共用同一条时间线**：凡是"相对今天"的语义，都要相对 `reference_now` 算，不能写死。

**失败注入的三条纪律（会静默毁掉实验）**

13. **失败注入必须确定性，由 case 声明**。GRPO 是组内比较；失败若随机，reward 差异分不清
    是「模型不同」还是「运气不同」，**advantage 被污染**。⇒ **RL 里任何跨 rollout 的随机性都是污染。**
14. **`side_effect_applied` 是超时机制的灵魂**：没发出去（该重试）vs 回包丢了（重试=重复扣款），
    **两种情况的错误文本必须逐字相同**（有测试守着）。
15. **新 cap 必须自动闭合**：判据要写成「世界满足某条件时才可能命中」，否则存量 case 立刻被打中、基线不可比。

**工程**

16. **`pkill -f <模式>` 自匹配**（执行 pkill 的 shell 自己也含该模式）—— 犯了三次。
17. **信 commit message 的结论**，而不是去查那次真正跑通的配置。
    **最可信的是 `outputs/<日期>/<时刻>/.hydra/overrides.yaml`。**
18. **编一个理论然后写进注释** ⇒ 注释里的机制解释也要标明「实测」还是「推断」。
19. **长任务会把所有"刚好够用"的边界一次性引爆**（v11 的 GEO 14 步第一次把 token 预算吃满，
    触发了 vLLM「没有空间生成」直接杀死整个 Ray 任务）。M8 之后还会有。

---

## 9 · 已就位的尺子（跑任何实验前先确认它们在）

| 尺子 | 模块 | 量什么 |
|---|---|---|
| 配对比较 | `train/compare.py` | 能力差异 + **自报最小可检出差异**（🔴 **配对 MDE = 0.013**；0.048 是不配对的 —— 这一格以前写的 0.05 是错的，正是 §2.9 警告过的那种引用错） |
| 输出熵 | `train/entropy.py` | **决策位**熵（整体熵会被格式 token 稀释） |
| dump 聚合 | `train/rl_report.py` | cap 分解 / 三段耗时 / **补报 wandb** |
| 下发记账 | `verl_agent_loop.record_dispatch` | 分布漂移的另一半 |
| staleness | `train/staleness.py` | σ²(k) 曲线（离线合成，已有 ESS/N=0.846、σ²(0)≈2.0e-4/token） |
| `defer` 双向 | `eval_local` | 该 defer / **误 defer** |
| 恢复动作双向 | `eval_local` | 该恢复 / **过度恢复** |
| **读/写分桶** 🆕 | `eval_local._report_read_write` | **写动作单独算**（§21：混在一起会被读操作稀释）。分组依据是 gold 里有没有 `kind="write"` 的工具 |
| 重出报告 🆕 | `eval_local --from-audit` | 对历史审计重跑报告，**补新尺子不用重跑 GPU** |
| TIS / ESS | verl `rollout_corr/*` | ⚠️ **只在 `bypass_mode=False` 下产出** |

★ **加采样次数几乎没用**（8→32 只把误差从 0.0068 降到 0.0034）—— 尺子的粗细来自
「同一题在两个模型下行为差多少」，不是采样噪声。

⚠️ **verl 不会把我们的指标上报 wandb**（`compute_data_metrics` 只认两个字段），
`rl_report` 的补报是唯一来源，**跑完必须执行**。

---

## 10 · 动态分池：两条不能动的纪律

既然饱和不可避免（§3），就别在饱和的题上浪费 rollout。GRPO 的梯度**完全来自组内 reward 的方差**，
一条 8 次全对、方差为 0 的题贡献**精确地等于零**，却照样吃掉 8 次 rollout。

1. **降权不等于剔除**。策略在漂移，这一步饱和的题几十步后可能重新有梯度；
   永久剔除 = **把回归检测一起关掉** —— 而"教了 A 忘了 B"正是本项目反复栽跟头的地方。地板 0.05 保证总会被重新体检。
2. **不用 `difficulty` 标签**。难度是造数据时拍的，而"现在还有没有梯度"是**策略的函数**。池子只认观测量。

⚠️ **已知局限**：只做到 case 级（`rollout.n=8` 仍是全局配置）；没有"由易到难"的课程
（全错和全对在池子眼里一样）；**在 `fully_async` 下完全不生效**。

---

## 11 · 遗留清单（已知、已评估、刻意不做）

| 缺口 | 什么时候做 |
|---|---|
| **记忆写机制三个工具只有一个有题**（`invalidate` 只当干扰项、`conflict_resolve` 完全没用上）。现在没有任何一道题考"查到的历史结论和现在的数据矛盾了怎么办" | **M8 之后**（RAG v1 会把"复盘结论"整套做起来，那时题面自然就有了） |
| `creative.get_asset_tags` / `get_metrics_by_asset` 没进 gold | 素材铺量意图 |
| L1 idea 收集 0% / L2 feature 化 ~10% | M12+ |
| `INJ\|memory_content\|id_given` 只有 4 条（90 格里唯一的薄格） | 不修 |

★ **"死工具"要分三类看**：**诱惑工具**（`campaign.create` 必须在菜单里，否则
「不打招呼就建站」这个错误无法发生，两条 cap 就是死的 —— **这是护栏的学费**）/
**干扰项**（菜单里必须有该选和不该选的，否则"裁剪"等于"送答案"）/ **零成本**（不在任何菜单）。
⇒ 判断一个工具是不是浪费，看的不是"有没有进 gold"，是**"它在哪些菜单里、花了多少 token、换回了什么"**。

---

## 12 · 给下一个窗口的第一句话

> 「读 `docs/syncopate/05-handoff.md`；环境和命令看 `08-machine-and-environment.md`，
> **M9 Runtime 看 `09-runtime-handoff.md`**（真服务，和训练线几乎不共用代码），
> **RAG/检索看 `10-rag-retrieval.md`**。
>
> **进度**：M0–M5 完成 · M6 条件毕业（选 v11-e1）· **M7 跑完了但"验收"两个字撑不住**
> （六条里 3 条没门槛、1 条没尺子，2026-08-16 已补齐，见 §2.9）·
> **M8 施工完成/验收未做（前置的两处断点已修，可以开跑）** ·
> **M9.1–M9.6 施工完成/压测未做**。
>
> ⚠️ **别把"施工完成"读成"验收通过"** —— 这份文档以前就是这么写的，
> 于是「M7 已验收」被下游反复引用了两天。**空着的门槛应读作"无法判定"，不是"通过"。**
>
> **M7 的结论是「没测出差异」**，而且根因是量出来的：模型只动了 **0.0093%** ——
> GRPO+AdamW 下 **位移 ≈ lr × 步数，reward 完全不参与**（§2.1）。
> ⇒ **下一跑只改一个变量：`--lr 1e-6 → 1e-5`。别加步数**（步数是钱，lr 免费）。
>
> ⚠️ 数据当前版是 **v12**（v11 的冻结已在 M8 解除），**v11 的 1370 条输入逐字节相同**
> ⇒ 老基线 `_audit/v11_sft_e1_m2.json` 在**输入这一侧**仍然可比。
> 🔴 **但它不能当下一跑的对照** —— 它对应的 SFT 模型已随换机器丢失，
> 配对比较要的是同一起点模型（§2.4）。**输入可比 ≠ 基线可用。**
>
> **三条会让你在这台机器上直接撞墙的**（🔴 2026-08-17 换机器后重写）：
> ① 只能用 DDP（P2P 全关，`--fsdp-size 1`）；⚠️ 「FSDP 慢 6 倍」那个数是旧机器的，
>    当前机器**没有稳态数据**，别引用（infra 线 A6 负责重测）
> ② **`/` 只有 16 G overlay**，Ray/Triton/vLLM/HF 的缓存默认全落在上面 ⇒
>    **每个 shell 先 `set -a; . /workspace/.env; set +a`**（见 `08 §2.0`），否则撑爆
> ③ **换轮子/换机器后先跑 `scripts/check_flash_attn_backward.py`** —— 反向坏掉的
>    flash-attn 不报错、指标好看、什么都没学到（§0.1-①）
>
> ⚠️ **本机没有任何训练产物**（checkpoints/ 和 models/ 里只剩基座）⇒
> **M8 验收必须排在 M7 第二跑前面**，它顺带产出后者的起点模型和新基线（§2.8）。
>
> **接着做什么**：三件在排队，见 §2.8。M9.7 压测不需要 GPU，可以随时开始。」

方法论问题先查 `核心手册/AgenticRL/sft-finetune-takeaways.md`，别凭通用经验答。
