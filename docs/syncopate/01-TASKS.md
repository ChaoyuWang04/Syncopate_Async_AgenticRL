# Syncopate · 01 · TASKS — 主线任务进度表

> **这份只做一件事：记录「谁在做什么、做到哪一步」。**
>
> ❌ 不记决策过程、不记调查记录、不记踩过的坑 —— 那些在
> [`22-decision-log.md`](22-decision-log.md)。
> ❌ 不记实测数字的来龙去脉 —— 那些在各自的权威文档里（见「结论去哪找」）。
>
> ⚠️ 2026-08-19 精简：本文曾增量更新到 **1269 行**，两个重复的 R-1 章节、四张状态表 ——
> 一份要天天读的进度表被决策记录淹了。⇒ **保持单一用途。**
>
> infra 线的队列在 `../infra_exp/00-INFRA-HANDOFF.md §5`。

---

## 0 · 当前目标与关键路径

**目标**：出一个**能上线灰测**的东西（Chaoyu 2026-08-19，见 `22 §D-1`）。

```
A · 出一个 checkpoint          训练侧    ⏸ **暂停** —— infra 还在修基建（22 §D-3）
                                        已有候选 r1_seqis，留着
B · 把它跑给用户用              runtime   🔴 **关键路径** —— 这条路目前不存在
```

**B 路 2026-08-19 收工时的状态**（起点是"这条路不存在"）：

```
✅ 横切收口       ActionGate —— 模型碰不到权限/幂等/重试/成本闸/审批/审计/事件/步数上限
✅ 平台真实形状   BUC 限流 · 改动频次 613/1487632 · 分页 · 显式字段 · 异步任务 · 乐观并发
✅ 工具实现       **30 / 30**（起点 2/30；账本 tool_parity.py，缺口 0）
✅ Agent Loop     模型驱动 + transcript 恢复（假模型驱动，判据已钉）
✅ 两侧对齐       B-5a 签名账本 · B-5b 行为对照台（**共抓到 6 个真分歧**）
✅ 灰测闸门       放量维度 = automation_tier 阶梯 · fail-closed · 线上尺子 = 人工修正率
🔴 模型服务端点   B-4 —— **要 GPU**
🟡 压测           降级路径已测（场景②首次有被测对象）；**真压 + 单流 TPOT 基线要 GPU**
```

⚠️ runtime 测试 **61 → 195**，全量 **365 → 651**。

⚠️ **runtime / agent loop 不能是训练侧套一层薄膜** —— 设计思路可复用，
实现必须独立、按可上线标准写（`22 §D-2`）。

---

## 1 · B 路 · runtime（🔴 关键路径，全部 0 GPU）

| # | 条目 | 谁在打 | 状态 |
|---|---|---|---|
| **B-0** | 生产级 agent loop 的设计边界（`09 §4.5`） | 主线 | ✅ **已落地** |
| **B-3a** | ★ **横切收口**：`ActionGate` —— 模型碰不到权限/幂等/重试/成本闸/审批/审计/事件/步数上限 | 主线 | ✅ **已落地** |
| **B-1a** | ★ 平台的两条硬机制：BUC 积分制限流 · 改动频次上限（`613/1487632`） | 主线 | ✅ **已落地** |
| **B-1b** | 平台其余形状：分页 cursor · 异步任务（submit→id→轮询）· 显式字段 · `validate_only` | 主线 | ✅ **已落地** |
| **B-1c** | ⛔ 单位改成"分" + 字段名带单位（`07 §P0-2`）—— **不是 runtime 的活** | 主线 | ⏸ **挪到 v14**：`new_budget` 有 100 处且散在**训练侧沙盒与 gold 数据**里，改名会作废 v13 |
| **B-2** | 补齐工具的 runtime 实现 —— **账本 30 / 30，缺口 0** | 主线 | ✅ **已落地**（五批） |
| **B-3b** | 模型驱动的循环 + transcript 恢复（假模型驱动，判据已钉） | 主线 | ✅ **已落地**（接真模型 = B-4） |
| **B-4** | 模型服务端点（现有唯一跑模型的代码是**离线批量评测**） | 主线 | 🔴 **队首**，等 GPU |
| **B-5a** | ★ **工具对齐账本**：缺口必须被登记；签名两侧一致 | 主线 | ✅ **已落地** |
| **B-5b** | ★ **行为对齐对照台**（表驱动）：**共抓到 6 个真分歧** | 主线 | ✅ **已落地** —— 5 个工具真跑对照，其余 25 个**逐条登记了为什么没跑** |
| **B-6** | M9.7 压测 + `11 §4` 的缺口 | 主线 | 🟡 降级路径已测 · `11 §4` **只剩 `cap_hits` 一条**（任务一/二/三/五全关）；真压要 GPU |
| **B-7** | 灰测口径 —— 放量维度改成 **`automation_tier` 阶梯**（不是流量比例）· fail-closed 开关 · 线上尺子 = **人工修正率** | 主线 | ✅ **已落地**（`09 §4.6`），⬜ 阶梯节奏与首批用户**要 Chaoyu 定** |

⚠️ **B-5 是这条路上最容易被跳过、后果最大的一条**：训练侧的 reward 是按沙盒工具语义写的，
两侧不一致 ⇒ **训练时的最优策略在线上就不是最优**。目前没有任何东西在看它。

---

## 2 · A 路 · 训练侧（⏸ 暂停，等 infra）

| # | 条目 | 吃 GPU | 状态 |
|---|---|---|---|
| **A-0** | 候选 `r1_seqis`：总分 +0.101(t=9.3) · 该 defer 97%（没掉）· 误 defer 4.1%→0.1% | — | ✅ 在手 |
| **A-1** | G-3 冒烟：`max_model_len` 5120→7168 动 vLLM KV cache | 7 min | ⏸ |
| **A-2** | SFT 重训 → 选点 → 定起点（sub-epoch 默认已开） | 19 min | ⏸ |
| **A-3** | 一个 epoch（137 步）@ lr 3e-5 + `sequence`，**挂 `rl_guard.sh`** | 数小时 | ⏸ |
| **A-4** | 按行为读数选 ckpt（**不看总分、更不看训练分**） | — | ⏸ |
| **A-5** | 零梯度格子处置（GEO 12 卡死 · 96 饱和）⚠️ 必须在 A-2 之后 | ❌ | ⏸ |
| **A-6** | ⚠️ **M6 是「条件毕业」** —— 两条判据（零梯度<30% · 多样性≥70%）实测互斥，但那是在 **4 个整 epoch 点**上得的；曲线这一段很陡（一个 epoch 内有梯度掉 31%）⇒ **很可能只是没采到中间**。A-2 的 sub-epoch 存点跑完就能回答 | ❌ | ⏸ |

**起跑时仍然成立的三条纪律**：

```
① 不用 lr 1e-4 出上线候选     实测 defer 直接归零，且**不可逆**（组内 std=0）
② 不用训练分选 ckpt           训练分说 lr1e-4 更好，任务分说它显著更差（t=−3.1）
③ 起 RL 必挂守卫              bash scripts/rl_guard.sh <日志> <ckpt目录> --kill
```

---

## 3 · 不 gating 任何东西（谁都可以随手做）

| # | 条目 | 成本 | 状态 |
|---|---|---|---|
| **C-1** | ~~两条 runtime 测试顺序依赖~~ | — | ✅ **已关闭** —— 根因是**队列全局**（不是测试写法）；`claim_run` 加 `org_id` 限定，结构上拿不到别人的活 |
| **C-2** | GRPO 组重复抽样：欠 `pool.py` 的运行时判据（审计侧已在） | 30 min | 🔴 待开 |
| **C-3** | 文档口径更正一批（`00 §1` 三桶数字 · `16 §6` · `17 §3.3` · memory 的「两堵墙」） | 1 h | 🔴 待开 |
| **C-4** | σ² 反解自检 | 2–3 h | ⛔ **挂起** —— 等 A 路重跑 |

---

## 4 · 已完成（按完成顺序，细节见 `22`）

| # | 条目 | 日期 |
|---|---|---|
| P0-1 | 训练/评测 rollout 预算统一成一份值 | 08-18 |
| P0-2 | 一步多调用：拦在发生点 + ceiling 重标定 | 08-18 |
| P0-3 | 越权写 cap 按工具真实代价分两档 | 08-18 |
| P0-4 | 作废数字登记表（`21`）+ 全库 ⛔ 横幅判据 | 08-18 |
| P1-1 | 停机条件整族重写 + `rl_guard.sh` 进仓库 | 08-18 |
| S1 | 采样参数对齐：评测跟训练 | 08-18 |
| P1-2 | `compare` 三计数（门槛 = 每题 2·采样标准误） | 08-18 |
| P1-3 | 截断成因拆三种 + `max_steps_cap` 判据收窄 | 08-18 |
| P2-2 | SFT sub-epoch 存点 + `select_sft_ckpt.py` | 08-18 |
| P2-5 | 检查器分开「查出违反」与「无法判定」（退出码 0/1/2） | 08-18 |
| P2-6 | 数据版本契约（`DATA_VERSION` + 不一致硬失败） | 08-18 |
| P2-7 | 管线收敛：契约参数不许各抄一份 + `contract` 判据组 | 08-19 |
| R-1a | `--rollout-is` 默认改回 `sequence` | 08-19 |
| R-1b | 行为塌陷**跑中**守卫（`defer_watch.py` + `rl_guard` D 族） | 08-19 |
| R-2 | 行为读数 + verdict 进 `compare` | 08-19 |

---

## 5 · 已关闭 / 降级

| # | 条目 | 处置 |
|---|---|---|
| **R-1** | ~~reward 在教「不拒绝」~~ | ⛔ **归因作废** —— 是 prompt **100% 截断**的产物；预算修好后 defer 97%→**100%**、REJ **+0.203**（`22 §E`） |
| **R-3** | 停机线口径拍板 | ✅ **关闭** —— 这条线永远不会触发（`22 §D-4`）。⚠️ 但「不会触发」≠「ESS 没用」 |
| **G-1** | 重基线评测 | ✅ infra 代跑完 → `_audit/v13_sft_e1_merged.json`（合并损失 −0.025） |
| **G-2** | Q2 探针（LoRA vs 合并权重） | ✅ infra 已做 |

---

## 5.1 · 🆕 2026-08-19 文档收尾

```
20  1269 → 142 行（决策全搬进 22，只留进度）
22  新建 —— 决策与调查记录的唯一存放处（§D 四条方向性决定 · §E R-1 归因作废 · §F KL 与位移）
⛔  作废标记从「17 份文档顶部横幅」下沉成「**47 处数字行内标记**」
    ⇒ 假警报没了，而警告出现在**数字旁边**而不是 200 行之外
```

⚠️⚠️ **不要误读成"作废解除了"**：`21 §2` 的 **22 条里 21 条仍是「待重测」**。
基石 bug 修好了，但**那些数字是在坏系统上量的，不会因为 bug 被修而变有效**。
⇒ 改的是**警告的形式**，不是警告的内容。

---

## 6 · 结论去哪找

```
21  哪些数字不能引用（引用任何 RL 数字前先查）
22  我们当时为什么这么决定、查到了什么   ← 本文搬出去的全部内容
06  跑 RL 的协议与停机条件
08  机器/环境/入口，含**固定管线的两条纪律**（§4.0）
14  SFT 健康指标与选点判据
11  runtime 验收口径
```

---

## 7 · 维护约定

- **每条做完就地改「状态」**；结论写进权威文档，**不要在本文堆细节**。
- **新开条目必须有「谁在打」**——空白格会被两边都当成对方在做。
- `交界` 的条目**动之前先说一声**，两条线共用一个 git 仓库。
- ★ **目标变了要整张表重排**，不是只往上加新条目（`22 §D-1`）。
