# Syncopate · 01 · TASKS — 主线任务进度表

> **这份只做一件事：记录「谁在做什么、做到哪一步」。**
>
> ❌ 不记决策过程、不记调查记录、不记踩过的坑 —— 那些在
> [`22-decision-log.md`](22-decision-log.md)。
> ❌ 不记实测数字的来龙去脉 —— 那些在各自的权威文档里（见「结论去哪找」）。
>
> ⚠️ 2026-08-19 精简：本文曾增量更新到 **1269 行**，两个重复的 R-1 章节、四张状态表 ——
> 一份要天天读的进度表被决策记录淹了。⇒ **保持单一用途。**
>
> infra 线的队列在 `../infra_exp/01-TASKS.md`。

---

## 0 · 当前目标与关键路径

**目标**：出一个**能上线灰测**的东西（Chaoyu 2026-08-19，`22 §D-1`）。
**当前两条最高优先级**（Chaoyu 2026-08-20，`22 §I`）：

```
U · 统一会话能力训练（M+O+CoT）  训练侧   🔴 唯一队首（08-28 融合裁定，施工图 24）：
                                        P0 ✅ → P1 OPD 在办 → P2 v14 → P3 多轮 RL → P4 抛光
F · chatbox 前端壳              产品侧   ✅ 全套落地且在用（08-20）
A · 出一个 checkpoint           训练侧   ✅ 候选在手（cand_v13r2_e1 RL-100，+0.186，待晋级确认）
B · 把它跑给用户用               runtime  ✅ 端到端通 + B-4/B-5 压测（goodput 192·22/22，11 §5）
```

⚠️ runtime 测试 **61 → 226**，全量 **365 → 694**。
⚠️ **runtime / agent loop 不能是训练侧套一层薄膜** —— 设计思路可复用，
实现必须独立、按可上线标准写（`22 §D-2`）；F 路前端同理。

---

## F · chatbox 前端壳（🔴 队首①，Chaoyu 08-20，`22 §I-1`）

| # | 条目 | 谁在打 | 状态 |
|---|---|---|---|
| **F-1** | **conversation 门面（后端）**：`conversations` 表 + `runs.conversation_id` + `POST /conversations/{cid}/messages`（一条消息 = 一个 run） | 主线 | ✅ **已落地（08-20）**：9 条测试（越权/必填/幂等/回放序）；真模型会话全链实测过；⚠️ 顺手修掉测试污染——真人租户 `org_demo` 与测试租户结构隔离（C-1 同族） |
| **F-2** | **assistant-ui 前端**（`frontend/`，Vite+React+TS+Tailwind） | 主线 | ✅ **已落地（08-20）**：`@assistant-ui/react` 0.15.15 · `useExternalStoreRuntime`（消息状态归我方：历史回放/审批中断恢复/多路 SSE 归约）· 手解 SSE 带 Last-Event-ID 重连（Node 冒烟过）· behavior 差异渲染 + 审批卡。构建零报错，dist 468K（JS gzip 135K）。⚠️ dist/node_modules 不进 git，重建命令在 `09 §0` |
| **F-3** | 部署接线 | 主线 | ✅ **已落地（08-20）**：无空闲外部端口 ⇒ dist 由 API 条件挂载 `/app`（不存在则跳过，测试不受影响），与 API 同源共用 8265 边界；`09 §0` 已更新 |
| **F-4** | 验收：同一会话里 消息→步骤流→审批→结论→再来一条 全链真跑 | 主线 + Chaoyu | ✅ **Chaoyu 已在真浏览器用起来**（08-20）：读/闲聊/多轮/契约 C 均验过。⚠️ 遗留小 bug：步骤行显示「档位 **?**」——档位改推导后前端不再声明，该位置应显示推导结果或去掉 |
| **F-5** | 🆕 **多轮壳层**（Chaoyu 08-20：闲聊上下文是环境决定的，不该等训练） | 主线 | ✅ **已落地**：部署侧上限 14336（与训练契约刻意分叉，理由见 `decider.py`）+ 会话历史进 prompt（最近 6 轮、每轮结论 400 tok 封顶、超了标注）。探针 `probe_multiturn.py`：格式保持 5/5 · 「我是谁」答出姓名 · 「那它的转化呢」正确指代 CMP_1。⚠️ 元对话（"我问了几个问题"）含糊 = 分布外，归 M 路 |
| **F-6** | 🆕 **意图与档位都不该由人选**（Chaoyu 08-20） | 主线 | ✅ **已落地**：① 工具菜单改**全量 30 个**（模型自选，`probe_full_menu` 三项全绿；以前不给是 7625>7168 预算挡着，不是设计选择）② 档位由**动作推导**（`tier_policy`，能升不能降；模型无降级接口）⇒ 前端两个下拉框删除，审批卡改显示"为什么判成这一档"。⚠️ 顺带关闭 `09 §4.6.4`（automation_tier 必填缺口，换解法关闭） |

⚠️ clarify 的用户回复在 v14 之前按"新任务"处理（上下文进 prompt = 训练分布问题，`22 §I-2`）。

## U · 统一会话能力训练（🔴 队首，Chaoyu 08-28 融合裁定：**M + O + CoT 三合一**，`24`）

> 原 M 路（多轮数据）与 O 路（OPD 说人话）**合并**，C-7（CoT）随 v14 bundle 并入——
> 一份多轮数据、三条监督通道按 token 段分家（NL 段←OPD · 工具段←SFT+RL · think 段←
> 大教师冷启+RL）。**完整施工图与逐阶段量化门槛 = [`24-unified-conversation-training.md`](24-unified-conversation-training.md)**，
> 本表只记阶段进度。已完成的 O 路资产全部继承：O-1 探针（OPD 对症）✅ · O-2a 契约改造 ✅ ·
> O-2 双教师设计 ✅（并入 24 §2）· O-4 三尺（并入 24 各阶段门槛）。
> M-1 的口径问题在 24 §2/P2 内回答；M-2/M-3 = 24 的 P2。

| 阶段 | 内容 | 谁在打 | 状态 |
|---|---|---|---|
| **P0** | 尺子与地基 | 主线 + infra | ✅ **全五项过（08-28）**：盲评自一致 100% · before 全立（说人话 0.60 vs 1.14；承接 L1 40/L2 88/L3 0/L4 0）· 分段器双修 · spike PASS · 教师 70% 压线 |
| **P1** | OPD 闲聊+承接（教师=底座，只训 NL 段）；五门槛全过 | 主线 + infra | ✅ **完成（08-28）**：任务分 **+0.022 显著涨** · 说人话 **1.37 超底座** · 首步 50/50 · L1 80% · 健康全绿；⚠️ L2 36/acted_on_bad_data+18 两红旗转 P2 入口红线（24 §4-P1） |
| **P2** | v14 重建 bundle（多轮+CoT 冷启+C-5 白名单+C-6 val 并入，一次带全）+ SFT；晋级门槛 24 §4-P2 **七条**（含 P1 红旗回归线） | 主线 + infra | 🔴 在办（08-29：数据 752 行 ✅ · SFT v14_r1 ✅ · **验收链在跑**=scripts/u_p2_accept.sh，产物 `_audit/v14_sft_e{1,2}.json`，接手人从这里继续） |
| **P3** | 多轮 RL（会话级 GRPO 起步）；晋级=L2 ≥90% · L3 ≥75% · L4 ≥60% · 任务分 ≥−MDE · think 行为闸 | 主线 + infra | ⬜ |
| **P4** | OPD 抛光（RL 后再蒸表达）→ **最终验收五条**（24 §4 末） | 主线 | ⬜ |

---

## 1 · B 路 · runtime（✅ 端到端已通；余项让位给 F/M/O）

| # | 条目 | 谁在打 | 状态 |
|---|---|---|---|
| **B-0** | 生产级 agent loop 的设计边界（`09 §4.5`） | 主线 | ✅ **已落地** |
| **B-3a** | ★ **横切收口**：`ActionGate` —— 模型碰不到权限/幂等/重试/成本闸/审批/审计/事件/步数上限 | 主线 | ✅ **已落地** |
| **B-1a** | ★ 平台的两条硬机制：BUC 积分制限流 · 改动频次上限（`613/1487632`） | 主线 | ✅ **已落地** |
| **B-1b** | 平台其余形状：分页 cursor · 异步任务（submit→id→轮询）· 显式字段 · `validate_only` | 主线 | ✅ **已落地** |
| **B-1c** | ⛔ 单位改成"分" + 字段名带单位（`07 §P0-2`）—— **不是 runtime 的活** | 主线 | ⏸ **挪到 v14**：`new_budget` 有 100 处且散在**训练侧沙盒与 gold 数据**里，改名会作废 v13 |
| **B-2** | 补齐工具的 runtime 实现 —— **账本 30 / 30，缺口 0** | 主线 | ✅ **已落地**（五批） |
| **B-3b** | 模型驱动的循环 + transcript 恢复（假模型驱动，判据已钉） | 主线 | ✅ **已落地**（接真模型 = B-4） |
| **B-4** | 模型服务端点 | 主线 | ✅ **已落地（08-20）**：vLLM 端点（`:8100`）+ `VllmDecider`（渲染/解析**复用训练侧同一份**：templates + apply_chat_template(tools) + parse_step；菜单初版按 intent 裁剪，**08-20 已改全量 30 个模型自选**，见 F-6）+ `agent_loop` 接进 worker（`SYNCOPATE_DECIDER_URL` 显式开启，判据行必打）。**真模型驱动实证**：I09 自主走 查指标→记忆→安全线→风控→预算政策 再提写、被 C 档闸正确拦停。⚠️ 已知缺口显式记在 decider.py：answer_fields 是最小集、context 未按 org 建 |
| **B-5a** | ★ **工具对齐账本**：缺口必须被登记；签名两侧一致 | 主线 | ✅ **已落地** |
| **B-5b** | ★ **行为对齐对照台**（表驱动）：**共抓到 6 个真分歧** | 主线 | ✅ **已落地** —— 5 个工具真跑对照，其余 25 个**逐条登记了为什么没跑** |
| **B-6** | M9.7 压测 + `11 §4` 的缺口 | 主线 | ✅ **压测已跑（08-20，真模型）**：§19 全表 **24/25 达标**（P95 全部大幅达标 · 8 并发劣化 1.04× · 幂等/SSE/恢复/场景②⑤ 全过；唯一红项=P50/P95 判据自身失义，改判提案在 `11 §5`）。当场抓到并修掉 3 个真 bug（calendar SQL 类型 · 工具崩溃带走 run · emit seq 竞态）。`11 §4` 仍剩 `cap_hits` 一条 |
| **B-7** | 灰测口径 —— 放量维度改成 **`automation_tier` 阶梯**（不是流量比例）· fail-closed 开关 · 线上尺子 = **人工修正率** | 主线 | ✅ **已落地**（`09 §4.6`），⬜ 阶梯节奏与首批用户**要 Chaoyu 定** |

⚠️ **B-5 是这条路上最容易被跳过、后果最大的一条**：训练侧的 reward 是按沙盒工具语义写的，
两侧不一致 ⇒ **训练时的最优策略在线上就不是最优**。目前没有任何东西在看它。

---

## 2 · A 路 · 训练侧（✅ 第一条完整链已跑通）

| # | 条目 | 吃 GPU | 状态 |
|---|---|---|---|
| **A-0** | ★ **新候选 `cand_v13r2_e1/adapter_global_step_25`（RL-100 步点）**：配对 **+0.186（t≈16，MDE 0.023）** · 该 defer 100% · 误 defer 0% · REJ 0.97 · `abandoned_esc` 比基线还降 2 · caps 全线干净。~~r1_seqis~~（旧系统世代，退役归档） | — | ✅ 在手，**待 Chaoyu 确认晋级** |
| **A-2** | SFT 重训 → 选点 → 定起点 | 19 min | ✅ **完成（08-19）**：`sft-v13r2` 训练健康（位移 e1 0.49%）；五点选点（一次性验证，日常只评 e1/e2）→ **epoch1**（熵 0.196 · 有梯度 **271** · 零梯度 **21.0%**——重建数据让格子 222→271）；f0.125 被格式判据拦下（parse_err 0.50）。合并 → `models/Qwen3-4B-sft-v13r2-e1`（全局残差 0.307；⚠️ 单层探针曾误拦——恰好选中 252 层最差层，判据已改全局加权，门槛 0.5 不动）；**任务级合并损失 −0.007 < MDE 0.021 测不出**（老系统 −0.025 显著）。配对基线 = `_audit/v13_sft_v13r2_e1_merged.json`（2048 尺子 0.711） |
| **A-3** | RL candidate 首跑 | — | ✅ **完成（08-19 晚）**：`cand_v13r2_e1` 400 步零中断（PG+mb8 · KL off · lr 3e-5），晋级闸三条全过；池 ~80 步饱和（覆盖 100%）⇒ 完成判据达成不必续。守卫两次误杀均为判据病（熵已除名，`22 §G-9`）；瘦身 sort bug 丢了 s400 优化器状态（已修，`22 §G-10`） |
| **A-4** | 按行为读数选 ckpt | — | ✅ **完成**：六点曲线 base 0.356→SFT 0.711→RL-100 **0.897**→200 0.902→300 0.898→400 0.856（**峰值 200、过训回落**）。RL-100 与 RL-200 统计并列（+0.186 vs +0.191 < MDE），**按 cap 定胜负选 RL-100**（abandoned_esc −2 vs +8；400 点 missing_safety_line 暴涨 +56 = 过训指纹）。⚠️ v14 输入：`missing_safety_line/abandoned_esc` 随步数单调涨 = reward 盲区；GEO 全程 0.25 不动 = 死格实锤（triage 已导出） |
| **A-5** | 零梯度格子处置 | ❌ | 🔄 **数据已更新**（08-19 夜跑产出）：卡死 12→**76**（FAIL 25/BUD 13/SCALE 12/GEO 10，RL 收敛解法时自造的占多数）· GEO 死格实锤（六点全程 0.245 不动）⇒ triage 名单 `_audit/triage/cand_v13r2_e1/` 是 v14 数据/curriculum 的直接输入 |
| **A-6** | M6「条件毕业」复核 | ❌ | ✅ **全答**：零梯度侧翻案（e1 21.0%<30%，缺终答数据造的假互斥）；多样性侧实测 = RL 会主动坍缩它（工具序列 390→83→59 种、决策位熵 0.196→0.007–0.033）⇒ 两判据不互斥，但**多样性要在起点量，不能在 RL 后量** |

**起跑时仍然成立的三条纪律**：

```
① 不用 lr 1e-4 出上线候选     实测 defer 直接归零，且**不可逆**（组内 std=0）
② 不用训练分选 ckpt           训练分说 lr1e-4 更好，任务分说它显著更差（t=−3.1）
③ 起 RL 必挂守卫              bash scripts/rl_guard.sh <日志> <ckpt目录> --kill
```

---

## 3 · 不 gating 任何东西（谁都可以随手做）

| # | 条目 | 成本 | 状态 |
|---|---|---|---|
| **C-1** | ~~两条 runtime 测试顺序依赖~~ | — | ✅ **已关闭** —— 根因是**队列全局**（不是测试写法）；`claim_run` 加 `org_id` 限定，结构上拿不到别人的活 |
| **C-2** | GRPO 组重复抽样：欠 `pool.py` 的运行时判据（审计侧已在） | 30 min | ✅ **已落地**（08-19）—— 升级成**结构性保证**：采样器排除上一批 ⇒ 边界错位也不可能重复；批内重复直接 raise；检查器对新跑（dump 带 `case_id`）零容忍。⚠️ 根因是**边界错位不是有放回**（`18 §6` 更正） |
| **C-3** | 文档口径更正一批 | 1 h | ✅ **已关闭**（08-19 核过）—— `00 §1` 三桶已随精简消失、`16` 已归档、`17 §3.3` 已随重构消化、memory「两堵墙」已标作废；docfacts 判据在持续扫 |
| **C-4** | σ² 反解自检 | 2–3 h | ⛔ **挂起** —— 等 A 路重跑 |
| **C-5** | 🟠 **越权写白名单与「交给人」打架**（原 `17 §6.5`，归档时捞出）：`approval.create_case` 排 `unauthorized_write_cap` 榜首，而 runtime 语义是「暂停不是拒绝」⇒ 模型交给人反而被罚 0.30 封顶。**必须排在任何「加大护栏惩罚」之前**；改白名单 = v14 数据版本变更 | 0 GPU 先出判据 | 🔴 **并入 M-3 bundle**（判据可先行） |
| **C-7** | ~~带思考的 SFT 数据要不要立项~~ | — | ✅ **已立项**（Chaoyu 08-20，CoT 训练支持，登记 MAINLINE-INFRA）⇒ 数据侧并入 **M-3 的 v14 bundle** |
| **C-6** | 🟠 **RL val 桶 165 条（20%）从未被消费**（08-19 审计发现）：`test_freq=-1`、`val_before_train=False` ⇒ verl 内部验证从不跑（评测走冻结 EVAL，这是设计）⇒ val.parquet 谁都不读。采样器只够得着 train 的 659 条，而 `pool_readout --total-cases 824` 的分母一直是桶总数——覆盖率被系统性低估。**建议下次数据重建时 val 并入 train**（824 全喂池子）。⚠️ 别在 infra 队列还在用 v13 文件时动 | 重建时顺手 | 🔴 **并入 M-3 bundle** |

---

## 4 · 已完成（按完成顺序，细节见 `22`）

| # | 条目 | 日期 |
|---|---|---|
| P0-1 | 训练/评测 rollout 预算统一成一份值 | 08-18 |
| P0-2 | 一步多调用：拦在发生点 + ceiling 重标定 | 08-18 |
| P0-3 | 越权写 cap 按工具真实代价分两档 | 08-18 |
| P0-4 | 作废数字登记表（`21`）+ 全库 ⛔ 横幅判据 | 08-18 |
| P1-1 | 停机条件整族重写 + `rl_guard.sh` 进仓库 | 08-18 |
| S1 | 采样参数对齐：评测跟训练 | 08-18 |
| P1-2 | `compare` 三计数（门槛 = 每题 2·采样标准误） | 08-18 |
| P1-3 | 截断成因拆三种 + `max_steps_cap` 判据收窄 | 08-18 |
| P2-2 | SFT sub-epoch 存点 + `select_sft_ckpt.py` | 08-18 |
| P2-5 | 检查器分开「查出违反」与「无法判定」（退出码 0/1/2） | 08-18 |
| P2-6 | 数据版本契约（`DATA_VERSION` + 不一致硬失败） | 08-18 |
| P2-7 | 管线收敛：契约参数不许各抄一份 + `contract` 判据组 | 08-19 |
| R-1a | `--rollout-is` 默认改回 `sequence` | 08-19 |
| R-1b | 行为塌陷**跑中**守卫（`defer_watch.py` + `rl_guard` D 族） | 08-19 |
| R-2 | 行为读数 + verdict 进 `compare` | 08-19 |
| P3-1 | 🔴→✅ **v13 SFT 数据 131/503 条缺终答**：轮数上限没跟 `case.max_steps`（`18 §12`）；构建器修 + 回放硬断言 + 数据重建（成员冻结）+ 检查器 `data` 组守着 | 08-19 |
| P3-2 | SFT 长度上限并入契约：删 `--max-length`，从 `rollout_budget` 推，超长硬报错（`08 §4.1`） | 08-19 |
| P3-3 | `clip_ratio` 常驻判据接进 `rl_guard`（P 族·输入完整性，非零即停机） | 08-19 |
| P3-4 | Q4 失败注入确定性守卫（`failures_fp` 进 dump + 检查器断言）· dump 带 `case_id`（解卡 infra 的 P4 复查与 defer 双向拆分） | 08-19 |
| P3-5 | Q5/Q6 探针 `probe_sft_rl_consistency.py` · Q7 进检查器 `data` 组（RL prompt 同源 + 零截断，实测最长 4654/5120，**余量仅 466**） | 08-19 |
| P3-6 | D 族连零门槛用 5120 干净跑复核：健康侧上界 11，25 维持（余量 ≥2.3×） | 08-19 |
| P4-1 | ★ **全链影子复现验证通过**（`scripts/run_pipeline_shadow_rebuild.sh`，可重复跑）：外部数据→v11→v12→v13→切分→双 parquet 与现役**逐字节/逐内容一致**（batch 6681 文件逐字节 · 三桶 SHA · parquet 1327 行逐 token）+ D/L 门禁全过。顺手抓到并修掉 xlsx 内嵌时间戳（openpyxl 默认写"现在"，「逐字节可复现」在那一环静默断掉） | 08-19 |
| P4-2 | 磁盘回收：5 个旧 ckpt 各提 253MB adapter 后删（`scripts/reclaim_ckpt_space.sh`，三道闸），135G 释放 ⇒ 199G 可用 | 08-19 |
| B-6a | ★ **runtime 首次真跑**（此前 195 条测试全在进程内，服务从没被起过）：worker 进程入口 + 冒烟驱动 `scripts/runtime_smoke.py`（固定 query 五条：审批批准/拒绝/未声明档/灰测拦 B 档/D 档拒，5/5 过，含 24 条 burst）。**当场抓到一个结构 bug**：cancelled 各退出路径不发终态事件 ⇒ SSE 客户端挂死；修法=终态事件并进 `finish_run` 同一事务（结构保证），新增 `run.cancelled` 事件种类 | 08-20 |

---

## 5 · 已关闭 / 降级

| # | 条目 | 处置 |
|---|---|---|
| **R-1** | ~~reward 在教「不拒绝」~~ | ⛔ **归因作废** —— 是 prompt **100% 截断**的产物；预算修好后 defer 97%→**100%**、REJ **+0.203**（`22 §E`） |
| **R-3** | 停机线口径拍板 | ✅ **关闭** —— 这条线永远不会触发（`22 §D-4`）。⚠️ 但「不会触发」≠「ESS 没用」 |
| **G-1** | 重基线评测 | ✅ infra 代跑完 → `_audit/v13_sft_e1_merged.json`（合并损失 −0.025） |
| **G-2** | Q2 探针（LoRA vs 合并权重） | ✅ infra 已做 |

---

## 5.1 · 🆕 2026-08-19 文档收尾

```
20  1269 → 142 行（决策全搬进 22，只留进度）
22  新建 —— 决策与调查记录的唯一存放处（§D 四条方向性决定 · §E R-1 归因作废 · §F KL 与位移）
⛔  作废标记从「17 份文档顶部横幅」下沉成「**47 处数字行内标记**」
    ⇒ 假警报没了，而警告出现在**数字旁边**而不是 200 行之外
```

⚠️⚠️ **不要误读成"作废解除了"**：`21 §2` 的 **22 条里 21 条仍是「待重测」**。
基石 bug 修好了，但**那些数字是在坏系统上量的，不会因为 bug 被修而变有效**。
⇒ 改的是**警告的形式**，不是警告的内容。

---

## 6 · 结论去哪找

```
21  哪些数字不能引用（引用任何 RL 数字前先查）
22  我们当时为什么这么决定、查到了什么（§H = 归档文档的存活结论）
06  训练协议与健康度：停机/完成判据 + 看哪些指标（H 部分 = 原 14）
08  机器/环境/入口，含**固定管线的两条纪律**（§4.0）
13  数据门禁（D 族多样性 + L 族泄露，原 15 已并入）
10  RAG 唯一权威（R 部分 = 原 12）
11  runtime 验收口径
```

---

## 7 · 维护约定

- **每条做完就地改「状态」**；结论写进权威文档，**不要在本文堆细节**。
- **新开条目必须有「谁在打」**——空白格会被两边都当成对方在做。
- `交界` 的条目**动之前先说一声**，两条线共用一个 git 仓库。
- ★ **目标变了要整张表重排**，不是只往上加新条目（`22 §D-1`）。
- ★ **更新即改写，不是追加**（守则⑪，Chaoyu 2026-08-19）：动笔前先通读；
  已完成的行要定期沉到 §4 或删掉，本文曾胖到 1269 行——**表只进不出就会再来一次**。
