# Syncopate · TASKS

> 只放未完成任务、依赖和完成条件。计划、决定、读数进入对应 REPORT；完成后从队列移除。
> 当前状态看 [START](00-START.md)，执行与学习预算看 [训练](04-TRAINING.md)，资源看 [Compute](05-COMPUTE.md)。

## T1 · 工程接线与全链学习运行

训练正确性和测速的未完项统一跟踪于 [infra 队列](../infra_exp/01-TASKS.md) 的 B03～B07；本页不复制局部验收或把它算成新全链通过。

| 任务 | 状态 / 依赖 | 完成条件 |
|---|---|---|
| T1-2 冻结配对评测与阶段观察 | 尚未开始；评测/Runtime 对缺失 `finish_reason` 尚未硬拒绝，本地准备可与 B03 剩余项并行 | 先统一明确 `length` 与缺失结束元数据的口径；再用同一题集、预算和采样比较 base/SFT/RL/OPD，分开工具/终答/思考通道；记录变好、变差、没动；不清洗数据或追业务分数 |
| T1-3 完整学习运行入口 | 待实现 `research/observe`，运行依赖 B07 | 由唯一 runbook 登记并执行 SFT 1 epoch、RL 100 次、OPD 50 次真实更新的初始观察预算；先核实数据循环和费用，attempts/skips 不充当更新；不冒充 candidate |
| T1-4 固定源码全链运行与汇总 | 等 infra B07 组合验收与 T1-2/3 | SFT → merge → Exam → RL → adapter → eval → OPD → eval 连续读取本轮产物，全部健康闸通过；如实报告阶段变化或未证明改善 |

## T2 · 服务工程验收

**前置**：训练资源与 B11/B12 推理测试可复用；公开服务、发布和主动故障注入另需批准。

- 当前 Celery/Redis/PostgreSQL 栈的真实负载、连接预算、流关闭和前端构建检查。
- 在隔离测试环境验证排队、限流、审批、幂等、恢复和回滚，不操作真实业务。
- 模型版本、adapter、请求与审计可追溯；不沿用旧硬件或开发库污染的 SLO。

**完成条件**：[Serving 清单](07-SERVING.md) 每项有证据或明确“不适用”；没有公开发布声明。

## 队列规则

- 数据冻结；模型质量告警留在评测结果，不新开造数/清洗任务。
- Candidate 和真实上线不在当前执行队列；将来用户明确要求时再登记。
- 同一事项只有一个执行方，另一条线仅链接依赖；不把决定和日志塞进 TASKS。
