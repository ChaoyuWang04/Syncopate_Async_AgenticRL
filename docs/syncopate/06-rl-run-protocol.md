# Syncopate · 06 — RL 跑之前的预期与解读方案

> ⛔⛔ **本文含已作废的实测数字**（2026-08-18）—— 查出两个基石级 bug：
> **三个 trainer rank 的梯度没有同步** · **trainer 的权重从没推给 rollout engine**。
> ⇒ 2026-08-14 至 08-18 之间**所有 RL 训练的实测数字都不可引用**。
> **引用之前必须先读 [`21-invalidated-numbers.md`](21-invalidated-numbers.md)** —— 那里也列了**仍然有效**的部分（SFT / 数据 / 静态代码事实 / 硬件测量）。

> 写于 2026-08-11，第一次正式 RL 之前。
> **纪律：预期写在跑之前。** 跑完再解释曲线，人一定会为看到的任何形状编一个理由。

---

## 0 · 起点与设定（⚠️ **这是 2026-08-11 第一次 RL 的设定，不是当前的**）

> 保留它是因为 §1 的预测是对着这一栏写的，**换掉它 §1.4 的兑现就没有对照物了**。
> **当前的起点 / 数据 / 机器一律以 `05-handoff.md` §0.1 与 §2.8 为准**——
> 尤其：本机 **4×5090**（不再是单卡）、数据版本 **v12**、
> 🔴 **`checkpoints/sft/` 是空的，下一跑的起点要重训**。

| 项 | 值 | 依据 |
|---|---|---|
| 起点 ckpt | `checkpoints/sft/v3_ctrl/**epoch1**` | 决策位熵 0.484（e2 只有 0.336）、有梯度 44 格（e2 只有 36）。手册 §20：训得越狠熵越低，GRPO 探索不动 |
| 训练池 | `data/rl/v3` 467 train / 67 val | 与冻结 EVAL 零重叠（SHA-256 实测） |
| 评测集 | 冻结 EVAL 64 条 | case_id 从 M0 起未变 |
| 长尾 | `latency_scale=0.01` | 480 秒压成 4.8 秒。**这一档跑不出异步的收益**，是刻意的：先验算法，再验调度 |
| TIS | `sequence`，阈值 2.0 | verl 默认；研究主线要对比的对象 |

---

## 1 · ★ 可证伪的预测（跑之前写死）

### 1.1 能力

| # | 预测 | 判定 |
|---|---|---|
| P1 | 涨的主要是 **CRE(0.30) / MISS(0.55) / BUD(0.73)**——低分且有梯度的那批 | 配对比较按模板分组 |
| P2 | **饱和的 22 格基本不动**（HIGH/LONG/FRESH/CLAR/REJ） | 组内 std ≈ 0 的那批 |
| P3 | 总体提升 **小于 SFT 那次的 +0.352**，量级在 0.05–0.15 | RL 是在 SFT 的地板上抬天花板，不是再抬一次地板 |

⚠️ **P3 小于最小可检出差异时，结论是「没测出」不是「没提升」。**
🔴 这里原写「配对 MDE ≈ 0.05」是**标错了**：0.05 那个量级是**不配对**的。
⚠️ **MDE 随评测集大小变，别记死一个数**：冻结 EVAL 254 条时配对 MDE = 0.013；
**v13（343 条）实测是 0.020–0.021**。⇒ **一律读 `compare` 自己打印的那个数。**
门槛和结论都必须引配对那个，见 `05-handoff.md` §2.4。

### 1.2 过程（比分数更早报警）

| # | 预测 | 崩了的样子 |
|---|---|---|
| P4 | `cap/*` 命中率**同步下降**，尤其 `unauthorized_write`、`missing_memory_check` | **reward 涨但 cap 不降 = reward hacking**，立即停 |
| P5 | 决策位熵从 0.484 缓慢下降 | **跌破 base 的 0.09** = 探索死了，停 |
| P6 | ESS/N 维持在 0.8 以上 | **跌破 0.3** = sequence-level TIS 失效，该换 token-level |
| P7 | `response_length` 不单调增长 | 暴涨 = 典型的长度 hacking |

### 1.3 效率

| # | 预测 |
|---|---|
| P8 | `head_of_line_ratio`（最慢/平均）≈ 1.3–1.6，和 bs=2 冒烟一致 |
| P9 | 分布漂移 `total_variation` **恒等于 0**——sync 有 barrier，构造上不可能漂移 |

**P9 是对照组**：只有先证明 sync 下是 0，async 下测到的非 0 才有意义。

---

## 1.4 ★★ 兑现：M7（2026-08-14，fully_async / 150 步 / 起点 v11-e1）

**四段式记账，推翻的预测不删。**

### P1「涨的主要是 CRE / MISS / BUD」—— **部分推翻**

- **原猜想**：低分且有梯度的那批涨得最多。
- **实测**：BUD **+0.055** ✅、FAIL +0.046、RAG +0.030；但 **CRE −0.026** ❌、
  FRESH −0.062、MISS 仅 +0.006。GEO 纹丝不动（0.246→0.248）。
- **推翻后**：「低分 + 有梯度」不足以预测涨幅。FRESH 掉得最多，而它**基线并不低**（0.882）——
  说明 RL 会**动到本来做得好的题**，方向不是单调向上的。
- **教训**：预测涨幅要按「组内方差」分组，不是按「均分高低」分组。

### P9「分布漂移恒等于 0」—— **成立，但理由和写的时候想的不一样**

- **原猜想**：sync 有 barrier ⇒ 构造上不可能漂移；async 下应该测到非 0。
- **实测**：**async 下也是 0.000**，下发 7200 = 训练 7200，一条不差。
- **推翻后**：真因是 **`partial_ratio = 0`** —— 没有任何一条轨迹跨越策略版本边界。
  轨迹太短（4.5 工具步、~700 token）相对同步周期（4 步 ≈ 300 s），根本碰不到边界；
  且 `--partial-rollout True` 是**续跑不是丢弃**。⇒ 不是"没测到漂移"，是**这一跑里没有漂移可测**。
- **教训**：① 想让这条线有内容，要调大 `--latency-scale` 或缩短同步周期，
  **让轨迹真的跨越版本边界**；② `record_dispatch` 记在轨迹**末尾** ⇒ 「下发」实为「跑完」，
  「尝试过但从未跑完」的轨迹这把尺子**看不见** —— 要测那个得在 rollout **开始时**也记一笔。

### 停止条件全部通过，且判据都实跑验证过

`ESS 0.74–0.88`（键名是 `rollout_corr/rollout_is_eff_sample_size`，**不叫 ess**）/
决策位熵 0.027→**0.072**（升了，探索没死）/ grad_norm 0.011–0.06 /
长度 508–738 无增长 / **cap 随 reward 同向下降**（step 1–25 → 101–125：
reward 0.718→0.763 同时 cap/条 0.305→0.223、截断 0.107→0.048）。

⚠️ **cap-vs-reward 在控制台看不到**（verl 的 `compute_data_metrics` 只认两个字段），
中途要监控就按步段聚合 `rollout_dumps/*.jsonl` 里的 32 个 `cap/*` 字段。

---

## 2 · 停止条件（2026-08-18 整节重写）

> ⛔ **旧版本长这样，留着当教训**（不删）：
> ```
> 立即停：ESS/N < 0.3 / 决策位熵 < 0.05 / reward 涨但 cap 不降（连续 3 步）/ grad_norm 跳两个数量级
> ```
> **四条问的都是同一件事：「模型学得怎么样？」**
> 而 2026-08-18 查出的两个基石级 bug ——
> **三个 trainer rank 的梯度没有同步** · **trainer 的权重从没推给 rollout engine** ——
> **静默跑完了整整两轮训练（M7 + M7-b），四条线一条都没响。**
>
> ★★★ **根因不是阈值定错了，是缺了一整族判据：没有任何一条在问「训练系统还活着吗」。**
> 打个比方：仪表盘上全是车速、油耗、转速，却没有一个灯告诉你
> **方向盘和车轮之间的连杆断了** —— 车照跑、表照转、数字都合理。

### 2.A · 系统还活着吗（🆕 全新一族，此前一条都没有）

**这四条全是「两个东西应当相同」型**：非黑即白、不需要阈值、不会因基线漂移而失效
（同 `ckpt_guards.assert_ranks_identical` / `check_pipeline_invariants`）。
**每一条都对应一个真实发生过、且完全静默的失效。**

| # | 判据 | 从哪读 | 不满足说明 |
|---|---|---|---|
| **A1** | 每次权重同步之后，`rollout_corr/kl` 必须**回落到首步那个数量级** | 训练日志 | **权重没推给 rollout engine**。⚠️ 主线实测那一跑：从第 3 个 param_version 起再没回落过，末尾 36× |
| **A2** | 第 1 步各 rank 的 `lora_B` **梯度逐位相同** | `SYNCOPATE_DDP_PROBE=1` | **梯度没跨 rank 归约**（E21）。★ 判据成立的前提：LoRA 的 B 零初始化 ⇒ 起点必然一致 |
| **A3** | 训练与评测的**契约五元组**相等：`(max_prompt, max_response, max_turns, top_p, top_k)` | `rollout_budget.py` + `overrides.yaml` | **评测测的不是训练那个策略**。已由 `check_pipeline_invariants --only budget` 静态守住 |
| **A4** | **绝对有效条数** `N × ESS/N ≥ 24` 条序列 | `rollout_corr/rollout_is_eff_sample_size × batch` | 梯度已经是噪声 |

⚠️ **A1 和 A2 只能在运行时抓**（它们是"每次同步之后该发生什么"，产物上看不出来）；
A3 是静态判据。⇒ **两套工具都要有，见 §2.D。**

### 2.B · 估计量还有效吗（原 ESS 那条，**改了口径**）

```
🟡 ESS/N < 0.3                        ⇒ **不停机**，换聚合口径：--rollout-is token
🔴 fraction_low + fraction_high > 40%  ⇒ 停机（IS 修正退化成常数缩放 = 研究问题 H3 的正面症状）
```

**为什么 ESS 从「停机线」降级成「换配置」的信号 —— 三条**：

1. **它是个比例，隐含了大 batch 的假设。** 上游语境 batch ~1024 条 ⇒ 0.3 = 有效 307 条；
   我们只有 48 条 ⇒ 0.3 = 有效 **14 条**。**同一个数字，含义差 20 倍。**
   ⇒ 该看的是**绝对条数**（A4），不是比例。
2. ★ **上游原话是「consider switching to geometric aggregation」，我们写成了「立即停」**
   （`00 §7` 里我们自己抄的）。而 infra 的 E20-a 已证实：换成 token 级，
   ESS 直接回到 1.000，**一分吞吐都没付**。
   ⇒ 「什么时候该从 sequence 换成 token」**正是这个项目的研究问题（H3）**——
   我们把研究问题的答案，当成了训练的刹车。
3. **verl 报的 ESS 是 `clamp(0, 2.0)` 之后算的**（`rollout_corr_helper.py:751-757`）
   ⇒ 高侧尾巴被砍掉、方差被人为压小 ⇒ **报出来的 ESS 系统性偏高**，
   而且 `fraction_high` 越大越乐观。

### 2.C · 模型学得怎么样（原有三条，不动）

```
🔴 决策位熵 < 0.05                探索死了
🔴 grad_norm 突然跳两个数量级      查坏样本或 lr（nan/inf 立刻停 —— flash-attn 反向坏掉就是这个症状）
🔴 reward 涨但 cap 不降（连续 3 步） reward hacking 的指纹
正常停：跑满预定步数
```

⚠️ **不要因为 reward 曲线好看就继续。** 设计文档 §31.3 的回退条件优先。

### 2.D · 判据由谁来执行

```
check_pipeline_invariants   **静态 / 离线**   跑之前和跑之后，查文档 / 产物 / 源码     A3
scripts/rl_guard.sh         **运行时**        跑的过程中盯指标，触发就停             A1 A4 + 2.B + 2.C
SYNCOPATE_DDP_PROBE=1       **首步一次**      第 1 步比各 rank 梯度                  A2
```

⚠️⚠️ **此前守卫不在仓库里** —— `logs/rl_guard.log` 存在，而产生它的是那次跑时临时写的一段 shell，
**跑完就没了**，而且它只盯 ESS / 熵 / 磁盘（全是 2.C 那一族）。
**这正是两个基石 bug 能静默跑完两轮的另一半原因。** ⇒ 守卫必须进仓库。

### 2.E ⚠️ 两个阈值是拍的

`A4 的 24 条` 与 `2.B 的 40%` **都是按当前 batch（6×8=48）反填的工程值，不是推导出来的**。
⇒ **第一次重跑之后用实测反填**，并在这里改掉这句话。
（本项目为"空门槛 / 拍的阈值"付过多次钱，见 `.claude/memory/blank-thresholds-are-not-passes.md`。）

## 3 · 跑完怎么查

```bash
python -m syncopate.train.rl_report   checkpoints/grpo/<run>          # cap/耗时/漂移，补报 wandb
python -m syncopate.train.eval_local  --adapter <ckpt> ...            # 冻结 EVAL 重评
python -m syncopate.train.compare     <本跑起点模型的审计> _audit/RL_<run>.json
python -m syncopate.train.entropy     --adapter <ckpt> --limit 24     # 决策位熵
```

🔴 **基线不要照抄路径。** `_audit/M1_ctrl_epoch1.json`（v3 时代）和
`_audit/v11_sft_e1_m2.json`（M7 时代）对应的模型**都已不在本机**，
它们是历史记录，不是下一跑的对照。配对比较要求**同一起点模型 + 同一推理引擎**
⇒ 起点是谁，就用谁的冻结 EVAL 审计当基线，见 `05-handoff.md` §2.4。

`compare` 会自己打印最小可检出差异，**不要绕过它读均值**。
⚠️ **配对 MDE 随评测集变**（254 条时 0.013 · v13 的 343 条实测 0.020）——
**读 `compare` 打印的那个，别引用记忆里的数**；且配对与不配对差近四倍，别混。

---

## 4 · ★★★ 分卡是异步的硬前提（🔴 **限制已解除，方法论仍然有效**）

verl 的两条异步路径都要求 **rollout 和 training 在不同的 GPU 上**：

- `one_step_off_policy/ray_trainer.py:89` — `assert not self.hybrid_engine`（显式禁止 colocate）
- `fully_async_policy` — `trainer_pool` 和 `config.rollout.n_gpus_per_node` 是两个独立资源池

**写这节的时候本机只有一张 5090，所以「吞吐收益」和「分布漂移」量不了。**
现在是 **4×5090**，两条路径都已实跑打通（`one_step_off` / `fully_async`，
见 `05-handoff.md` §2.2）⇒ **这条限制不再成立。**

⚠️ **但真异步跑通并没有让 §4.1 作废，反而给了它一个实证**：M7 那一跑
`partial_ratio = 0`、分布漂移 `total_variation = 0` —— **不是没测到漂移，是那一跑里
没有漂移可测**（轨迹 ~700 token 相对同步周期 4 步 ≈ 300 s，根本碰不到版本边界）。
⇒ **真异步能不能量到 σ²(k)，取决于调度恰好产生多大的 k**，而离线合成里 k 是我们给的。

### 4.1 离线合成：k 精确可控，与真异步互补（不是替代）

研究假设的核心量是

$$\mathrm{ESS}/N \approx \exp(-T\,\sigma^2(k))$$

要量 $\sigma^2(k)$，**并不需要真的异步**——异步只是产生 staleness 的一种方式。
直接做法：把第 $t-k$ 步 policy 生成的轨迹留着，用第 $t$ 步的 policy 重算 logprob，
得到 ratio 分布 → ESS。$k$ 由我们精确控制，而不是由调度随机决定。

| | 真异步 | 离线合成 |
|---|---|---|
| $k$ 的取值 | 随机、由调度决定（**M7 实测 k 根本没长出来**） | **精确可控**，能扫 k=0,1,2,4,8 |
| 硬件 | ≥2 GPU（✅ 本机 4 张，已打通） | 单卡即可 |
| 能测 σ²(k) 曲线 | 能，但含混杂**且要碰运气** | **能，且干净** |
| 能测吞吐/空转 | ✅ | ❌ |
| 能测分布漂移 | ✅ | ❌ |

⇒ **两边都要**：H1/H2 的核心曲线用离线合成扫（干净、可控、便宜），
   吞吐与漂移用真异步补 —— 但**真异步要先让轨迹真的跨越版本边界**
   （调大 `--latency-scale` 或缩短同步周期），否则量到的只会是又一个 0。

我们已经有 σ²(k=0) 的第一个实测点：ESS/N = 0.846，T ≈ 825 token
⇒ $\sigma^2(0) \approx 2.0\times10^{-4}$/token。

---

## 5 · 已就位的尺子

⇒ **权威清单在 `05-handoff.md` §9，这里不再抄一份**（抄了就会长成两份，
然后两份都不准 —— 2026-08-16 补的**读/写分桶**和 `--from-audit` 就只进了那一份）。
本节只留跑 RL 时最常用的四把：`compare`（自报 MDE）/ `entropy`（决策位熵）/
`rl_report`（cap 分解 + 补报 wandb）/ verl `rollout_corr/*`（ESS，⚠️ 只在
`bypass_mode=False` 下产出）。

⚠️ **verl 不会把我们的指标上报 wandb**（`compute_data_metrics` 只认两个字段），
所以 `rl_report` 的补报是唯一来源，跑完必须执行，否则曲线上只有 verl 自带的那些。

---

## 附录 A · v8 基线的预期（写于 2026-08-11，跑之前）

沙盒大改之后，旧的 base 数据（0.502 / EVAL 64 条）**全部作废**：
预算单位从元变成分、写动作必须带幂等键、工具从 22 个变 24 个、
prompt 渲染顺序变了、多了十类失败注入。EVAL 从 64 涨到 104 条。

| # | 预测 | 判据 |
|---|---|---|
| P1 | 总体 **0.25–0.40**，明显低于旧的 0.502 | 平均 reward |
| P2 | **F 类 < 0.20**，明显低于非 F 类 | 按 case_id 前缀分组 |
| P3 | **写动作大面积失败** —— base 不知道要传 `client_request_id` | tool_errors 暴涨 |
| P4 | **单位错**：用户说「840 元」，base 大概率填 840 而不是 84000 | policy 子分塌陷 |
| P5 | **全灭格子大幅增加**，集中在 `FAIL_*` | 零梯度构成 |
| P6 | 注入类有非零的 `prompt_injection_cap` 命中（封顶 0.0） | cap 分解 |

★ **P3 是最需要注意的一条**：base 会因为一个纯格式问题（漏一个参数）
在**所有写动作**上失败。这不是能力问题，是「约定未知」——
按 M0 的分类它属于**廉价死格**，SFT 一个 epoch 就能解决。

⚠️ 读结果时必须把这一层剥掉再看，否则会把「不知道要填这个字段」
误读成「不会做这个任务」，进而把 SFT 桶选错。
这正是 M0 那条教训的复现场景：**`p=0` 有两种成因，混在一起会把 SFT 引向错误方向。**

### 不测旧 SFT 的理由

`checkpoints/sft/v3_ctrl` 是在**另一个世界**里训的（22 工具 / 元 / 无幂等键 /
无失败注入 / 不同的 prompt 渲染）。拿它测 v8，分数低是因为世界变了，
不是因为模型不行 —— 数字没有意义，40 分钟 GPU 白花。

正确顺序：**测 base → 按新审计重切 SFT 桶 → 在 v8 上重训 → 再评**。
