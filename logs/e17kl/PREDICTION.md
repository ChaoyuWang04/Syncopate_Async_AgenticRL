# E17 KL 两臂 · 预测（2026-08-19，跑之前写死）

## 主判据
A（KL 开）与 B（KL 关）**直接配对**，看 `defer` 双向率 + 三计数，**不看均值**。

## P1 · 吞吐
B 比 A 每步快 **12–13%**，且 B 的日志里 `timing_s/ref` 出现 **0 次**。
依据：b12 实测 12.7%（那次在坏基线上，但两臂同受影响 ⇒ 比值有效）。
⇒ 若 `timing_s/ref` 仍出现，就是没关掉，本次作废。

## P2 · 任务分（主判据）
**无显著差异**（MDE ≈ 0.02）。依据：KL 项当前只贡献损失的 0.011%。
⇒ **若 B 的 defer 掉了**，那才是最有价值的结果 —— 它证伪
「损失里占比小 ⇒ 可以省」这条在生态里非常普遍的直觉。

## P3 · defer（★ 同时回答第二个问题）
**A 臂的 defer 会从 83% 回到 90% 以上。**
依据：A 与 r1_tokenis **只差 prompt 预算**（3584 → 5120）；3584 下 100% 被左截断，
「调查先于任何结论（含拒绝、反问）」存活 0/659、「数据不够时用 defer」存活 46%。
⇒ 若仍是 83% 左右 ⇒ 截断不是原因，reward 那条归因成立。
⇒ 若回到 ~97% ⇒ 夜跑「reward 在教模型不拒绝」要整段重写。

## P4 · 机械判据
`prompt_length/clip_ratio` 必须是 **0.0000**（此前一直是 1.0000）。不是 0 就作废。
