# infra 线的计时原始数据

> ⚠️ **2026-08-18 有效性说明 —— 引用之前必读。**

这里全是 `*_timing.json`，即**计时/吞吐**的原始测量。

```
✅ 计时数字本身有效   它们量的是"花了多久"，不经过梯度同步或权重内容这条路
🔴 **解释可能作废**   E22 证实：这些跑里每次权重同步推的是 **8.4 GB 冻结基座**，
                     而当时以为只推 132 MB LoRA
                     ⇒ 凡是「权重同步为什么这么慢」这一类**分析**，前提是错的
                     ⇒ 修法① 之后实测：param_sync **6.25 s → 0.974 s**（E22 §6.4.3）
🔴 **精度侧全部作废** b10_* / b19_* 的"陈旧度代价"：那些跑里 rollout 的策略恒为 π₀，
                     **陈旧度这个旋钮没接到任何东西**
```

⇒ 作废/有效的完整登记见 `docs/infra_exp/00-INFRA-HANDOFF §5` 的重测队列，
以及主线的 `docs/syncopate/21-invalidated-numbers.md`。
