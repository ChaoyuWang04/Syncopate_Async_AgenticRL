# Syncopate / verl-async-agentic-rl 项目指令

本文件是 Codex/GPT 进入本仓库时自动读取的根级指令。它只保存长期有效的入口、边界和核心守则；会变化的进度、数字和施工记录仍以各权威文档与落盘证据为准，不在这里复制第二份。

## 1. 新任务开始时必须先读

在分析、规划或动手前，按顺序完成以下阅读：

1. 完整读取 `.claude/memory/MEMORY.md`，再读取与当前任务有关的记忆条目。不能只看索引摘要，也不能因为目录名是 `.claude` 就忽略；这些是本项目沿用的项目记忆。
2. 完整读取 `docs/syncopate/00-START.md` 和 `docs/infra_exp/00-START.md`，确认两条线的当前状态、导航和下一步。
3. 训练与推理 infra 当前执行队列只认 `docs/infra_exp/01-TASKS.md`；原业务停止边界看 `docs/syncopate/01-TASKS.md`。不要从旧聊天、记忆摘要或归档推断现在做到哪一步。
4. 主线系统边界先读 `docs/syncopate/02-SYSTEM.md`；做对应工作时再读唯一专题：
   `03-DATA.md`、`04-TRAINING.md`、`05-COMPUTE.md`、`06-RUNTIME.md` 或
   `07-SERVING.md`。Infra 任务先读 `docs/infra_exp/02-SYSTEM.md`，再按任务读
   `03-TRAINING.md`、`04-SERVING.md`、`05-COMPUTE-AND-KERNELS.md` 或
   `06-EXPERIMENTS.md`。旧主线原因在 `docs/archive/syncopate/pre-consolidation-v16/`，
   历史归档不能当当前默认。
5. Modal、硬件、新栈或保留的 v16 复现任务还必须依次读：
   - `.claude/memory/modal-migration-state.md`
   - `docs/syncopate/05-COMPUTE.md`（主要资源与卡型调查边界）
   - 数据任务读 `docs/syncopate/03-DATA.md`，训练任务读 `docs/syncopate/04-TRAINING.md`
   - `modal_app/README.md`
   - 与本次任务对应的 `_audit/` 落盘证据
6. `docs/archive/` 只表示历史，不表示当前状态；`reference/` 是参考或上游快照，也不能自动当成本仓库现行实现。

阅读后核对当前队列与证据，向用户说明理解和计划。用户已授权的工作继续执行；不要重复索取同一授权。

## 2. 事实优先级与项目边界

- 用户/Chaoyu 当前明确裁定优先；若它与旧文档冲突，指出冲突并按最新裁定处理。
- 当前权威入口、队列和实际落盘证据高于记忆摘要；记忆负责导航和保留经验，不能替代最新实测。
- “代码存在”“配置登记”“测试通过”都不等于机制已在真实路径生效。最终以真实入口的判据行、产物内容和端到端证据为准。
- 不确定来源之间谁对时，不静默选一个；先核对代码、运行入口和落盘证据，仍无法确认就问用户。
- 项目以开源贡献为方向，主线是训练框架、SFT/RL/async/OPD、推理引擎、MoE、算子/量化、通信与权重传递实验。先跑真实训练入口观察速度瓶颈，再按证据做定位、改动并回原入口A/B。主模型固定一个MoE和一个dense，优先复用Volume；小型合成模型仅作局部诊断，不替代真实负载。速度优先，不要求业务能力提升。实验组负责背景调查、复现、定位、实验性修复、测试和结论；疑似上游问题按 `docs/upstream/README.md` 留 DRAFT，由 upstream 负责人完成正式 PR 考据、提交与跟进。
- 原业务场景的数据构建、教师扩写、清洗、模型选点和全链学习排期停止；保留必要证据，不恢复造数；用户已授权清理确认不会复用的大产物，按 Compute 的保留/清理规则执行。首次探索只保留输入/模型身份、真实工作量、非零有效更新/权重交接和运行健康；全面数值及质量验证在形成优化方案后按受影响机制补齐，未回归不称可采用。旧业务质量/供给不是探针通用前置；使用旧业务入口时仍遵守其现有硬闸。
- 训练与推理 infra 是本仓库主线。`harness-lab/` 与 `sandbox-rl-MOPD-lab/` 默认完全独立，各有环境、数据、预算、资源与排期；Harness/tool runtime 由独立 Lab 负责，未来交互/合并另行约定。不再维护 `MAINLINE-INFRA`、HANDOFF、REPLY 或“信件”文档。跨线事项只进入唯一负责方的 TASKS，另一条线只链接依赖；负责人之间按第 3 节使用 Codex 独立任务消息工具直接核对。
- infra 线若队列为空，不自行复活实验。只有用户明确要求，或权威文档登记的复活条件已经出现，才重新开线。
- `scripts/` 只放固定入口、临时调度 shell 和环境/行为探针；可复用 Python 组件必须放进 `syncopate/`，由 `python -m syncopate...` 调用，不得反向 import `scripts/`。具体分类见 `scripts/README.md`。

## 3. 交接与不确定性

- 直接以当前文档、代码和落盘证据确认现场；用户要求前任或其他任务核对时使用真实任务消息，不把旧聊天当作实时确认。
- 新任务先阅读、核对范围，再汇报具体计划。当前任务已有明确批准时按批准范围执行。
- 事实冲突先查真实入口与证据；仍无法确定、会实质改变路线时向用户说明问题。
- 只有用户要求与另一个独立 Codex 任务协调时才使用独立任务消息工具；子智能体消息不能冒充跨任务通信。
- 不把投递成功或旧聊天当作对方实际确认，也不创建交接信件文档。

## 4. 两份 `00-START.md` 的共同核心守则

以下是必须每次执行的底线，并集中保存在本文件；两份 `00-START.md` 只保留各自导航和少量开工提醒，不再复制历史代价。

1. **判据放在两个本应相同的东西之间。** 优先逐字节、逐 token、跨 rank、开关两态或来源/产物相等；少用容易随基线漂移的宽范围阈值。
2. **每个具体实验先充分检索最新官方资料、GitHub 源码与 issue/PR、现有修法，记录日期与 SHA。首次真实场景探索调查支持、容量、入口和已知阻塞，不要求先证明存在未解决问题。优化任务必须来自实际瓶颈或明确功能需求；已有方案先核验，无异常/无收益也可关闭。背景调查闸见 `docs/infra_exp/06-EXPERIMENTS.md`。所有关键假设写成断言。** 保护逻辑提成单一共享函数，所有入口共用，不能在多个脚本里各抄一份。
3. **判据必须量到正确对象。** 判据行没出现就视为机制没有生效；要有已知必然发生的正对照、终态读数和必要的噪声地板。修复后现象一字不变，先怀疑改错路径。
4. **匹配不到就报告没有，不要猜。** 文件、模型、审计、checkpoint 或配置的模糊匹配不得自动选一个“看起来像”的对象。
5. **先测量再动手、先看输入再怪模型。** 先看模型实际看到的输入、拿到的观测、被要求填写的契约，以及是否被截断；没有证据时不先加惩罚或护栏。
6. **分开验工程、速度和学习效果。** 探索保留真实工作量/更新和运行健康，以速度画像优先；优化方案形成后定向验数值/产物/输出，再做原入口同工作量重复A/B。业务学习不是当前门槛；没有收益或没有复现如实记录。
7. **施工完成不等于验收通过。** 空门槛表示无法判断，不表示通过。门禁约束“晋级/发布”，不要阻止用于诊断的短跑，但短跑不能冒充候选。
8. **实验或施工前先注册计划和判据。** 写清每一步做什么、产物落哪里、用哪条命令读哪个数、阈值是什么；跑完再定阈值属于自证。每个门槛先检查可测、可达、阶段归属。
9. **计划先固定探索范围和观测，不预造优化结论。** 模型/入口/预算等开跑条件查清；后续优化按真实画像安排，未知瓶颈不算计划缺陷。
10. **固定入口优先，禁止参数副本。** 业务训练仍使用保留的 runbook；场景探索使用预注册的真实框架训练入口；共享微探针仅在瓶颈定位需要时使用。每个实验的配置、模型/adapter、预算和硬件从唯一配置来源派生，不在临时命令复制。新的 base/卡型入口未实现前不得宣称已支持。
11. **停止和晋级由预注册判据决定，不由随意步数或主观感觉决定。**
12. **输入必须符合被测路径的真实契约。** 原业务回放才逐项对照 runtime 的 prompt、历史、工具、观测、think 与截断；纯 tensor/base 探针按登记的输入契约验证，不强加业务工具菜单，不推动两个 Lab 改接口。
13. **数据隔离必须靠硬机制。** 复用旧业务数据仍遵守三桶、`source_case_ids` 与写盘隔离断言；新探针单独登记输入来源、用途和 SHA，不跨实验覆盖，不为沿用旧检查重造业务数据。
14. **机制要验证是否接上。** 注册了工具、补丁、开关或配置不算落地；必须从真实调用路径看到它被消费，并检查其内容而不只是耗时或存在性。
15. **当前结论默认就地改写；每个独立实验的唯一 REPORT 简洁保留必要背景、设置/判据摘要、逐次结果和结论；详细背景/问题/方案写 upstream，精确复现与检索细目写小型索引，失败批次按 run-id 留证。其他文档不追加流水历史。** 动笔前通读目标章节；旧结论同次删除或改掉；同一事实只在一处写全，其他地方只放链接；历史交给 git。新文档先证明现有权威文档容不下。
16. **写大白话。** 术语第一次出现时解释，读者是明天接手的人，不是评审。
17. **保护共享工作树。** 提交或暂存只按具体路径操作，不用 `git add -A`、`git add .` 或 `git commit -a`；不修改、清理或覆盖别人的未提交文件和 `_audit/` 产物。除非用户明确要求，不提交、不推送。
18. **耐久目标只在方案已定且用户明确要求时使用。** 目标必须包含可测终态、证明方法、不许动的范围和停止条件；不能为了达标而放宽判据。

## 5. Infra 线额外守则

- RL框架仅限verl/AReaL/ROLL/NeMo-RL/Miles/slime，以verl为主；推理仅限vLLM/SGLang/TensorRT-LLM（后者用于低精度/NVIDIA特性）。名单不代表组合全支持。训练引擎锁定 FSDP2 与 Megatron。普通 HF 前向只作参考，不冒充真实 trainer；其他训练引擎不纳入当前实验。
- 先真实训练、后按证据排优化任务，不再强制每批三个预设探针；G地图仅为定位参考。外部 Modal 取消/重派在确认本实验旧写者退出后继续；实际程序错误从首次确认该错误起记录时间，持续排查20分钟仍未解决就暂停该项汇报，不再按重试次数停止。取消来源不明不得归因给用户，不停止其他任务。

- 先确认实验服务哪条真实兑现物；没有被测量出来的需求，就没有优化目标。
- 性能 baseline 使用当前官方稳定、适合生产的优化配置，不用“全部关闭”制造稻草人；但官方也不等于正确或更快。首次画像不要求逐一认证所有官方默认开关；新引入或修改的机制才用真实命中、OFF/ON、端到端收益和定向数值/输出回归完成采用验收。未复验的本地机制默认关闭，全关闭臂只作排错。
- 先在真实训练观察接缝和瓶颈，再保留触发组合降维成秒级最小复现；不从底层猜测开局，也不以完整长训练作为每轮调试循环。补丁先确认装进了哪个进程。
- `setup_worker` 阶段不要 import 会触碰 CUDA 的模块；Ray 分卡前碰 CUDA 会让多个 rank 挤到同一张卡。
- 分布式包装下不得擅自绕过框架要求的根模块调用路径；是否归约必须用当前栈的跨 rank 梯度/权重判据证明，不能从旧 FSDP1 经验推断。
- fully-async timing 覆盖几个 global step 必须实测；`--steps` 表示更新次数，不等于数据量。前提、默认值或单位改变后，旧指标不能直接比较。
- 单因素没复现时，先把真实运行的全部因素照抄出来复现，再逐个删减；不要过早宣布问题不可复现。
- 框架行为反常时先查上游 issue 和当前版本源码，再写本地补丁。
- 真正跑训练前必须通过对应机器所需的硬件/接口运行健康检查，不将全套数值探针当首次画像前置；监视器必须有“进程已退出”的兜底。保留必要审计证据；实验结束后核对恢复、复验、upstream 复现及其他任务的引用。确认不会复用且没有活动读写者的 checkpoint 和临时大产物直接清理，先记精确身份/大小，删后复核并登记；不动共享基模或归属不明文件。
- 第三方一次性工具放隔离环境，禁止 `uv pip install` 进生产 venv。依赖恢复用冻结锁；更换 flash-attn 或机器后先跑反向正确性检查。
- 把框架 `UserWarning` 当信号读；注释和报告中分开“事实”与“推断”，未验证推断要显式标记。
- 旧 FSDP1、LoRA 同步、PrefixGrouper、`mb=8`和 NCCL 默认值都只是历史证据；没有在所选当前硬件/框架路径上重新接线、正负对照和回归，不得继承为默认。
- 新 infra 实验按 `docs/infra_exp/06-EXPERIMENTS.md` 在独立 infra-probes 系列从 B00 起编号，一真实场景或独立优化一 REPORT；G0～G7 是参考分组，B 编号不代表卡型。原始证据在各实验的持久存储独立路径，简洁 REPORT/索引在 `docs/infra_exp/experiments/Bxx-主题/`，原始证据在登记 Volume 的 `/vol/_audit/infra-probes/Bxx/<run-id>/`；旧 `_audit/infra/Bxx/` 不构成新系列依赖或默认基线。

## 6. 资源、独立探针与保留的 v16 入口

- **主要计算平台是 Modal。** 本轮上限8张B200，含同时运行的所有训练/推理/教师角色；默认本组并发总占卡也≤8。只用保留真实并行所需卡数，可测合理TP/EP组合，不强制缩成双卡。其他卡型留历史参考，未来调整范围再启用；每run核对容量/拓扑/时限/费用，8卡不等于无限预算。Mac 主要做控制和 CPU 检查；5090 可能承载其他独立 Lab 负载，不能假定空闲。
- 运行要在用户批准范围内，并预先登记卡数、时限和证据路径；同一范围的授权持续有效。Candidate、发布、故障注入和扩大范围另行确认。
- **权威与入口：**机器看 `docs/syncopate/05-COMPUTE.md`，数据看 `03-DATA.md`，
  训练和门槛看 `04-TRAINING.md`，当前实验顺序只看 `docs/infra_exp/01-TASKS.md`。以下是保留的业务代码入口说明，不是新实验通用命令；业务执行只走
  `scripts/v16_pipeline.sh [--dry-run] [--profile smoke|candidate] [--gate-mode observe|strict] [--run-id ID] <stage|all>`。
  默认是 `smoke/observe`；candidate 和 strict 必须显式选择。observe 只允许质量/读数缺口记 WARN 后继续，模型或数据身份错误、越桶、NaN、程序异常、坏产物和零真实更新仍须立即停止。
  stage 数量与命令以脚本当前内容为准，不抄历史档案里的数量。
- **上云前先在本机验：**独立实验先跑相关语法、配置、输入和接口测试，再补目标 CPU；不为探针启动业务供给或建库。明确复现旧业务入口时才跑其 runbook dry-run、`supply` 和所需结构闸。DRY 跳过的数量闸或教师文本闸必须单独验证，不能用 DRY 全绿代替候选全绿。
- 原业务建库已停止，以下仅为将来明确恢复后的保留操作，当前不得执行：建库闸红时可先用 `modal run --detach modal_app/stack_probe.py --steps build_v16 --build-gates report` 收齐所有红项并分成真 bug、结构性限制、需用户裁定三类；最终正式产物必须在 strict 模式全绿，且画廊经过人工逐条查看。staging 未全绿不能搬进正式目录。
- **网络重活默认放 Modal 容器。** 依赖解锁、HF 权重下载、大文件和编译缓存放容器/Volume；本机主要负责编辑、版本管理和读取 `_audit/`。如果网络或云权限受限，申请授权，不绕过边界。
- **新实验先核对最新稳定版与上游 main。** 每实验冻结支持目标硬件的依赖；现有 B200 锁是历史基线，不强加给其他卡型。 不是最新必须在依赖表登记被谁钉住以及原因，并用 `modal_app/stack_probe.py --steps versions` 机器核对。硬件能力随卡型变化；sm_120 的结论不能套到 sm_100/B200，B200 的速度读数也不能无拓扑指纹跨次比较。
- **一个写者。** 两个并发任务不得同时写同一个 Volume 仓库、parquet、缓存或审计路径。共享内容只读；每个容器从 `/vol/repo.git` 到自己的 `/tmp/repo` checkout，每个 A/B 臂写自己的 `/vol/_audit/<exp>/<arm>/`。
- 实验按 TASKS 逐项推进；优化阶段相关回归通过后，同一实验互不依赖的 A/B 在总卡上限内并行，否则顺序交叉运行，使用相同源码、镜像、工作量和尺子；每臂记录拓扑和噪声。不同机器要做交叉复核，不能把机器差异算成优化收益。
- TASKS 按 P 优先级与稳定 Q ID 管理活动队列，默认逐项推进；G 只作分类，跨实验 gate 只认适用的具体证据。结项移出队列、报告保留，下游依赖改成报告链接；新异常以有界新任务入队。每实验 REPORT 只写必要背景、设置/判据摘要、逐次测试结果与结论；机器索引保存完整复现身份，upstream 材料展开详细背景/问题/方案，避免重复。
- Modal GPU 函数可能被抢占，因此每步必须幂等，阶段之间用文件交接，长任务结果持续写入 Volume。判据和最终结果写 JSON/文件，不从 stdout 最后一行猜结果。
- `Secret`、`Volume`、`Image` 等 Modal 对象不得按环境变量条件定义；可选 secret 使用占位对象并在代码中无条件引用。密钥只放 Modal secret 或本机受控配置，绝不写进仓库、日志或文档。
- 遵守 Volume 分层：bare repo、每容器 checkout、数据、模型、缓存、审计各在约定路径；并发写共享 git 需要锁。缓存必须绑定数据切分 SHA 与构造器版本，自动失效，不能靠“记得清缓存”。
- 能装 AOT/预编译资产就不要重复 JIT；必须 JIT 时把缓存放 Volume。子进程 `PATH` 必须包含对应 venv 的 `bin`。
- 操作资源前先核对 App/run-id、PID、启动时间、进程树、工作目录、设备和所属任务；归属不明就询问，不能凭进程名杀掉别人的负载。只收掉本实验旧 APIServer/EngineCore/Worker 并确认自身占用回到底线；不要粗暴使用会匹配自身命令行的 `pkill -f`，先查 PID 再精确处理。
- 停 Modal app 使用 `modal app stop <id> --yes`，随后再 `modal app list` 确认消失。删除整个 Volume 需要 `--yes`；`modal volume rm` 不接受 `--yes`。任何删除都先核对精确目标，删后用列表确认。
- 未经用户明确要求不要推送；获准推送时优先使用已配置的 SSH remote，因为本机 HTTPS push 有已知挂死记录。
- 新栈 API 或线格式变化时全库扫描所有旧假设。Qwen3.5 工具调用默认是 XML；Transformers 5 的模板返回类型变化必须走项目共享适配函数，不能在新调用点手搓兼容。

## 7. 完成与汇报

- 每项完成声明都要附可复查证据：执行了什么、读到什么、产物在哪里、哪些内容没有验证。
- 失败也要保存有用证据，区分代码错误、环境错误、判据量错对象和权限/外部依赖问题。
- 最终汇报先说最简单的现状和结论，再说风险或下一步；不得把未验、冒烟或链路通写成生产验收通过。
