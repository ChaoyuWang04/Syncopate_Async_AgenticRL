# K2（2026-09-02）：schema 的唯一真相 = syncopate/runtime/migrations/versions/ 迁移链。
# 连接串不写死在这里：env.py 从 SYNCOPATE_PG_DSN（或测试注入的 syncopate.dsn）取，
# 与业务代码 db.py 同一个环境变量 —— 不留第二份连接配置。
[alembic]
script_location = syncopate/runtime/migrations
prepend_sys_path = .
file_template = %%(rev)s_%%(slug)s
